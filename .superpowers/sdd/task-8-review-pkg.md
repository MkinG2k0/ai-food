# Review package Task 8 (re-review)
BASE: f6d90a6100d285edde0f3ffdd9ab4095d751b2d7
HEAD: ecacf83cf6302d4a7f1cc11de7582cc869b63cca
## Commits
ecacf83 fix(ai-food): wait for auth hydration in consent gate 37945c9 feat(ai-food): data consent gate after login
## Stat
 apps/ai-food/src/app/ConsentGuard.test.tsx         |  77 ++++++++++++++  apps/ai-food/src/app/ConsentGuard.tsx              |  27 +++++  apps/ai-food/src/app/router.tsx                    |  90 ++++++++++++++--  .../src/features/auth/api/signInWithDemo.test.ts   |  13 ++-  .../src/features/auth/api/signInWithDemo.ts        |   7 +-  .../auth/api/signInWithTelegramBot.test.ts         |   7 +-  .../src/features/auth/api/signInWithTelegramBot.ts |   7 +-  .../src/features/auth/api/submitDataConsent.ts     |  43 ++++++++  apps/ai-food/src/features/auth/index.ts            |   3 +  .../src/features/auth/model/dataConsentVersion.ts  |   1 +  .../src/features/auth/model/useAuthHydrated.ts     |  25 +++++  .../src/features/auth/model/useAuthStore.ts        |  34 ++++++-  apps/ai-food/src/pages/consent/index.ts            |   1 +  apps/ai-food/src/pages/consent/ui/ConsentPage.tsx  | 113 +++++++++++++++++++++  14 files changed, 431 insertions(+), 17 deletions(-)
## Diff
```
diff --git a/apps/ai-food/src/app/ConsentGuard.test.tsx b/apps/ai-food/src/app/ConsentGuard.test.tsx new file mode 100644 index 0000000..69a65e0 --- /dev/null +++ b/apps/ai-food/src/app/ConsentGuard.test.tsx @@ -0,0 +1,77 @@ +import { beforeEach, describe, expect, it, vi } from 'vitest'; +import { render, screen } from '@testing-library/react'; +import { MemoryRouter, Route, Routes } from 'react-router-dom'; +import { useAuthStore } from '@/features/auth'; +import { ConsentGuard } from './ConsentGuard'; + +const mockHydrated = vi.fn(() => true); + +vi.mock('@/features/auth', async (importOriginal) => ({ +  ...(await importOriginal<typeof import('@/features/auth')>()), +  useAuthHydrated: () => mockHydrated(), +})); + +function renderGuard() { +  return render( +    <MemoryRouter initialEntries={['/settings']}> +      <Routes> +        <Route +          path="/settings" +          element={ +            <ConsentGuard> +              <div>protected</div> +            </ConsentGuard> +          } +        /> +        <Route path="/consent" element={<div>consent</div>} /> +      </Routes> +    </MemoryRouter>, +  ); +} + +describe('ConsentGuard', () => { +  beforeEach(() => { +    mockHydrated.mockReset().mockReturnValue(true); +    useAuthStore.setState({ +      session: null, +      userToken: null, +      dataConsentAt: null, +      dataConsentVersion: null, +    }); +  }); + +  it('renders nothing before auth store hydration finishes', () => { +    mockHydrated.mockReturnValue(false); +    useAuthStore.setState({ userToken: 'jwt-token' }); + +    renderGuard(); + +    expect(screen.queryByText('consent')).not.toBeInTheDocument(); +    expect(screen.queryByText('protected')).not.toBeInTheDocument(); +  }); + +  it('redirects authenticated users without consent', () => { +    useAuthStore.setState({ userToken: 'jwt-token' }); + +    renderGuard(); + +    expect(screen.getByText('consent')).toBeInTheDocument(); +  }); + +  it('renders children for authenticated users with consent', () => { +    useAuthStore.setState({ +      userToken: 'jwt-token', +      dataConsentAt: '2026-08-06T00:00:00.000Z', +    }); + +    renderGuard(); + +    expect(screen.getByText('protected')).toBeInTheDocument(); +  }); + +  it('renders children for logged-out users', () => { +    renderGuard(); + +    expect(screen.getByText('protected')).toBeInTheDocument(); +  }); +}); diff --git a/apps/ai-food/src/app/ConsentGuard.tsx b/apps/ai-food/src/app/ConsentGuard.tsx new file mode 100644 index 0000000..5604d56 --- /dev/null +++ b/apps/ai-food/src/app/ConsentGuard.tsx @@ -0,0 +1,27 @@ +import { Navigate, useLocation } from 'react-router-dom'; +import { useAuthHydrated, useAuthStore } from '@/features/auth'; + +interface ConsentGuardProps { +  children: React.ReactNode; +} + +export function ConsentGuard({ children }: ConsentGuardProps) { +  const hydrated = useAuthHydrated(); +  const userToken = useAuthStore((state) => state.userToken); +  const dataConsentAt = useAuthStore((state) => state.dataConsentAt); +  const location = useLocation(); + +  if (!hydrated) return null; + +  if (userToken && !dataConsentAt) { +    return ( +      <Navigate +        to="/consent" +        replace +        state={{ from: location.pathname }} +      /> +    ); +  } + +  return <>{children}</>; +} diff --git a/apps/ai-food/src/features/auth/api/signInWithDemo.test.ts b/apps/ai-food/src/features/auth/api/signInWithDemo.test.ts index ad78f17..554e315 100644 --- a/apps/ai-food/src/features/auth/api/signInWithDemo.test.ts +++ b/apps/ai-food/src/features/auth/api/signInWithDemo.test.ts @@ -21,11 +21,16 @@ import { getDeviceId } from '@/shared/lib';  import { useAuthStore } from '../model/useAuthStore';  import { signInWithDemo } from './signInWithDemo';    describe('signInWithDemo', () => {    beforeEach(() => { -    useAuthStore.setState({ session: null, userToken: null }); +    useAuthStore.setState({ +      session: null, +      userToken: null, +      dataConsentAt: null, +      dataConsentVersion: null, +    });      vi.unstubAllEnvs();      vi.stubEnv('VITE_AI_GATEWAY_URL', 'http://gateway.test');      vi.restoreAllMocks();      vi.mocked(getDeviceId).mockResolvedValue('device-demo');      vi.spyOn(globalThis, 'fetch'); @@ -41,10 +46,12 @@ describe('signInWithDemo', () => {              telegramId: '100000001',              username: 'demo_user',              firstName: '╨Ф╨╡╨╝╨╛',              lastName: '╨┐╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М',              photoUrl: null, +            dataConsentAt: '2026-08-06T00:00:00.000Z', +            dataConsentVersion: '2026-08-06',            },          }),          { status: 200, headers: { 'Content-Type': 'application/json' } },        ),      ); @@ -65,10 +72,14 @@ describe('signInWithDemo', () => {        id: 'user-demo',        username: 'demo_user',        telegramId: 100000001,      });      expect(useAuthStore.getState().userToken).toBe('jwt-demo'); +    expect(useAuthStore.getState().dataConsentAt).toBe( +      '2026-08-06T00:00:00.000Z', +    ); +    expect(useAuthStore.getState().dataConsentVersion).toBe('2026-08-06');      expect(useAuthStore.getState().session?.name).toContain('╨Ф╨╡╨╝╨╛');    });      it('throws when VITE_AI_GATEWAY_URL is missing', async () => {      vi.stubEnv('VITE_AI_GATEWAY_URL', ''); diff --git a/apps/ai-food/src/features/auth/api/signInWithDemo.ts b/apps/ai-food/src/features/auth/api/signInWithDemo.ts index 589e2af..f9c4c56 100644 --- a/apps/ai-food/src/features/auth/api/signInWithDemo.ts +++ b/apps/ai-food/src/features/auth/api/signInWithDemo.ts @@ -9,10 +9,12 @@ type DemoGatewayUser = {    username?: string | null;    firstName?: string | null;    lastName?: string | null;    photoUrl?: string | null;    name?: string | null; +  dataConsentAt?: string | null; +  dataConsentVersion?: string | null;  };    export async function signInWithDemo(opts?: {    signal?: AbortSignal;  }): Promise<TelegramSession> { @@ -41,8 +43,11 @@ export async function signInWithDemo(opts?: {        body.message ?? `╨Ф╨╡╨╝╨╛-╨▓╤Е╨╛╨┤ ╨╜╨╡ ╤Г╨┤╨░╨╗╤Б╤П (${res.status})`,      );    }      const session = mapTelegramUserToSession(body.user); -  useAuthStore.getState().signIn(session, body.token); +  useAuthStore.getState().signIn(session, body.token, { +    dataConsentAt: body.user.dataConsentAt ?? null, +    dataConsentVersion: body.user.dataConsentVersion ?? null, +  });    return session;  } diff --git a/apps/ai-food/src/features/auth/api/signInWithTelegramBot.test.ts b/apps/ai-food/src/features/auth/api/signInWithTelegramBot.test.ts index bea4953..05f772b 100644 --- a/apps/ai-food/src/features/auth/api/signInWithTelegramBot.test.ts +++ b/apps/ai-food/src/features/auth/api/signInWithTelegramBot.test.ts @@ -30,10 +30,12 @@ describe('signInWithTelegramBot', () => {        id: 'user-1',        telegramId: '42',        username: 'ada',        name: 'Ada Lovelace',        photoUrl: 'https://example.com/ada.png', +      dataConsentAt: '2026-08-06T00:00:00.000Z', +      dataConsentVersion: '2026-08-06',      };      const fetchMock = vi        .fn<typeof fetch>()        .mockResolvedValueOnce(          new Response( @@ -75,11 +77,14 @@ describe('signInWithTelegramBot', () => {      expect(fetchMock).toHaveBeenNthCalledWith(        3,        'https://gateway.example/auth/telegram/status?challengeId=challenge-1',        { signal: undefined },      ); -    expect(signIn).toHaveBeenCalledWith(session, 'jwt-token'); +    expect(signIn).toHaveBeenCalledWith(session, 'jwt-token', { +      dataConsentAt: '2026-08-06T00:00:00.000Z', +      dataConsentVersion: '2026-08-06', +    });      expect(session).toMatchObject({        id: 'user-1',        name: 'Ada Lovelace',        username: 'ada',        telegramId: 42, diff --git a/apps/ai-food/src/features/auth/api/signInWithTelegramBot.ts b/apps/ai-food/src/features/auth/api/signInWithTelegramBot.ts index c25d1dd..2f083df 100644 --- a/apps/ai-food/src/features/auth/api/signInWithTelegramBot.ts +++ b/apps/ai-food/src/features/auth/api/signInWithTelegramBot.ts @@ -9,10 +9,12 @@ type TelegramGatewayUser = {    username?: string | null;    firstName?: string | null;    lastName?: string | null;    photoUrl?: string | null;    name?: string | null; +  dataConsentAt?: string | null; +  dataConsentVersion?: string | null;  };    type TelegramBotLoginOptions = {    signal?: AbortSignal;    openLink?: (url: string) => void; @@ -68,11 +70,14 @@ export async function signInWithTelegramBot(        message?: string;      };        if (status.status === 'ok' && status.token && status.user) {        const session = mapTelegramUserToSession(status.user); -      useAuthStore.getState().signIn(session, status.token); +      useAuthStore.getState().signIn(session, status.token, { +        dataConsentAt: status.user.dataConsentAt ?? null, +        dataConsentVersion: status.user.dataConsentVersion ?? null, +      });        return session;      }      if (status.status === 'expired') {        throw new Error('╨б╨╡╤Б╤Б╨╕╤П ╨▓╤Е╨╛╨┤╨░ ╨╕╤Б╤В╨╡╨║╨╗╨░. ╨Я╨╛╨┐╤А╨╛╨▒╤Г╨╣╤В╨╡ ╤Б╨╜╨╛╨▓╨░.');      } diff --git a/apps/ai-food/src/features/auth/api/submitDataConsent.ts b/apps/ai-food/src/features/auth/api/submitDataConsent.ts new file mode 100644 index 0000000..23a89c0 --- /dev/null +++ b/apps/ai-food/src/features/auth/api/submitDataConsent.ts @@ -0,0 +1,43 @@ +import { DATA_CONSENT_VERSION } from '../model/dataConsentVersion'; +import { useAuthStore } from '../model/useAuthStore'; + +type ConsentResponse = { +  dataConsentAt: string | null; +  dataConsentVersion: string | null; +  message?: string; +}; + +export async function submitDataConsent(): Promise<void> { +  const gatewayUrl = import.meta.env.VITE_AI_GATEWAY_URL as string | undefined; +  if (!gatewayUrl?.trim()) { +    throw new Error('VITE_AI_GATEWAY_URL ╨╜╨╡ ╨╖╨░╨┤╨░╨╜'); +  } + +  const userToken = useAuthStore.getState().userToken; +  if (!userToken) { +    throw new Error('╨Ф╨╗╤П ╨┐╨╛╨┤╤В╨▓╨╡╤А╨╢╨┤╨╡╨╜╨╕╤П ╤Б╨╛╨│╨╗╨░╤Б╨╕╤П ╨╜╨╡╨╛╨▒╤Е╨╛╨┤╨╕╨╝╨╛ ╨▓╨╛╨╣╤В╨╕'); +  } + +  const response = await fetch( +    `${gatewayUrl.replace(/\/$/, '')}/auth/consent`, +    { +      method: 'POST', +      headers: { +        'Content-Type': 'application/json', +        'X-User-Token': userToken, +      }, +      body: JSON.stringify({ version: DATA_CONSENT_VERSION }), +    }, +  ); +  const body = (await response.json().catch(() => ({}))) as ConsentResponse; + +  if (!response.ok || !body.dataConsentAt) { +    throw new Error( +      body.message ?? `╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╤Б╨╛╤Е╤А╨░╨╜╨╕╤В╤М ╤Б╨╛╨│╨╗╨░╤Б╨╕╨╡ (${response.status})`, +    ); +  } + +  useAuthStore +    .getState() +    .setDataConsent(body.dataConsentAt, body.dataConsentVersion); +} diff --git a/apps/ai-food/src/features/auth/index.ts b/apps/ai-food/src/features/auth/index.ts index 9bffb7d..7715ba5 100644 --- a/apps/ai-food/src/features/auth/index.ts +++ b/apps/ai-food/src/features/auth/index.ts @@ -1,14 +1,17 @@  export type { TelegramSession } from './model/telegramSession';  export { useAuthStore } from './model/useAuthStore'; +export { useAuthHydrated } from './model/useAuthHydrated';  export {    isAuthMockEnabled,    signOut,  } from './model/mockTelegramAuth';  export { getQuotaHeaders, type UsageKindHeader } from './model/quotaHeaders';  export { resolveAnalyzeUsageKind } from './model/resolveAnalyzeUsageKind'; +export { DATA_CONSENT_VERSION } from './model/dataConsentVersion';  export { recordUsageEvent } from './api/recordUsageEvent'; +export { submitDataConsent } from './api/submitDataConsent';  export {    fetchUsage,    getCachedUsage,    hydrateUsageCache,    createDefaultGuestUsage, diff --git a/apps/ai-food/src/features/auth/model/dataConsentVersion.ts b/apps/ai-food/src/features/auth/model/dataConsentVersion.ts new file mode 100644 index 0000000..3aa5724 --- /dev/null +++ b/apps/ai-food/src/features/auth/model/dataConsentVersion.ts @@ -0,0 +1 @@ +export const DATA_CONSENT_VERSION = '2026-08-06'; diff --git a/apps/ai-food/src/features/auth/model/useAuthHydrated.ts b/apps/ai-food/src/features/auth/model/useAuthHydrated.ts new file mode 100644 index 0000000..2a89035 --- /dev/null +++ b/apps/ai-food/src/features/auth/model/useAuthHydrated.ts @@ -0,0 +1,25 @@ +import { useEffect, useState } from 'react'; +import { useAuthStore } from './useAuthStore'; + +/** True after async Capacitor Preferences rehydration finishes. */ +export function useAuthHydrated(): boolean { +  const [hydrated, setHydrated] = useState(() => +    useAuthStore.persist.hasHydrated() +  ); + +  useEffect(() => { +    const unsubHydrate = useAuthStore.persist.onHydrate(() => +      setHydrated(false) +    ); +    const unsubFinish = useAuthStore.persist.onFinishHydration(() => +      setHydrated(true) +    ); +    setHydrated(useAuthStore.persist.hasHydrated()); +    return () => { +      unsubHydrate(); +      unsubFinish(); +    }; +  }, []); + +  return hydrated; +} diff --git a/apps/ai-food/src/features/auth/model/useAuthStore.ts b/apps/ai-food/src/features/auth/model/useAuthStore.ts index 2677f23..8fa599f 100644 --- a/apps/ai-food/src/features/auth/model/useAuthStore.ts +++ b/apps/ai-food/src/features/auth/model/useAuthStore.ts @@ -5,23 +5,51 @@ import type { TelegramSession } from './telegramSession';    interface AuthState {    session: TelegramSession | null;    /** Gateway user JWT for X-User-Token (real Telegram login). */    userToken: string | null; -  signIn: (session: TelegramSession, userToken?: string | null) => void; +  dataConsentAt: string | null; +  dataConsentVersion: string | null; +  signIn: ( +    session: TelegramSession, +    userToken?: string | null, +    consent?: { +      dataConsentAt: string | null; +      dataConsentVersion: string | null; +    }, +  ) => void; +  setDataConsent: (at: string | null, version: string | null) => void;    signOut: () => void;    isAuthenticated: () => boolean; +  hasDataConsent: () => boolean;  }    export const useAuthStore = create<AuthState>()(    persist(      (set, get) => ({        session: null,        userToken: null, -      signIn: (session, userToken = null) => set({ session, userToken }), -      signOut: () => set({ session: null, userToken: null }), +      dataConsentAt: null, +      dataConsentVersion: null, +      signIn: (session, userToken = null, consent) => +        set({ +          session, +          userToken, +          dataConsentAt: consent?.dataConsentAt ?? null, +          dataConsentVersion: consent?.dataConsentVersion ?? null, +        }), +      setDataConsent: (dataConsentAt, dataConsentVersion) => +        set({ dataConsentAt, dataConsentVersion }), +      signOut: () => +        set({ +          session: null, +          userToken: null, +          dataConsentAt: null, +          dataConsentVersion: null, +        }),        isAuthenticated: () => get().session !== null, +      hasDataConsent: () => Boolean(get().dataConsentAt),      }),      {        name: 'ai-food-auth',        storage: createJSONStorage(() => capacitorStorage),      }, diff --git a/apps/ai-food/src/pages/consent/index.ts b/apps/ai-food/src/pages/consent/index.ts new file mode 100644 index 0000000..1223486 --- /dev/null +++ b/apps/ai-food/src/pages/consent/index.ts @@ -0,0 +1 @@ +export { ConsentPage } from './ui/ConsentPage'; diff --git a/apps/ai-food/src/pages/consent/ui/ConsentPage.tsx b/apps/ai-food/src/pages/consent/ui/ConsentPage.tsx new file mode 100644 index 0000000..f6f37d9 --- /dev/null +++ b/apps/ai-food/src/pages/consent/ui/ConsentPage.tsx @@ -0,0 +1,113 @@ +import { useState } from 'react'; +import { Navigate, useLocation, useNavigate } from 'react-router-dom'; +import { toast } from 'sonner'; +import { +  submitDataConsent, +  useAuthHydrated, +  useAuthStore, +} from '@/features/auth'; +import { getLegalUrl } from '@/shared/lib'; +import { Button } from '@/shared/ui'; + +type ConsentLocationState = { +  from?: string; +}; + +export function ConsentPage() { +  const navigate = useNavigate(); +  const location = useLocation(); +  const hydrated = useAuthHydrated(); +  const userToken = useAuthStore((state) => state.userToken); +  const dataConsentAt = useAuthStore((state) => state.dataConsentAt); +  const [accepted, setAccepted] = useState(false); +  const [submitting, setSubmitting] = useState(false); +  const privacyUrl = getLegalUrl('/privacy'); + +  if (!hydrated) return null; + +  if (!userToken) { +    return <Navigate to="/login" replace />; +  } +  if (dataConsentAt) { +    return <Navigate to="/" replace />; +  } + +  const handleSubmit = async () => { +    if (!accepted || submitting) return; +    setSubmitting(true); +    try { +      await submitDataConsent(); +      const from = (location.state as ConsentLocationState | null)?.from; +      navigate(from && from !== '/consent' ? from : '/', { replace: true }); +    } catch (error) { +      toast.error( +        error instanceof Error +          ? error.message +          : '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╤Б╨╛╤Е╤А╨░╨╜╨╕╤В╤М ╤Б╨╛╨│╨╗╨░╤Б╨╕╨╡', +      ); +      setSubmitting(false); +    } +  }; + +  return ( +    <main className="flex min-h-dvh flex-col bg-zinc-50 px-5 py-8"> +      <div className="my-auto space-y-6"> +        <header className="space-y-2"> +          <h1 className="text-2xl font-semibold tracking-tight"> +            ╨б╨╛╨│╨╗╨░╤Б╨╕╨╡ ╨╜╨░ ╨╛╨▒╤А╨░╨▒╨╛╤В╨║╤Г ╨┤╨░╨╜╨╜╤Л╤Е +          </h1> +          <p className="text-sm text-muted-foreground"> +            ╨Ф╨╗╤П ╤А╨░╨▒╨╛╤В╤Л ╨░╨║╨║╨░╤Г╨╜╤В╨░ ╨╝╤Л ╨╛╨▒╤А╨░╨▒╨░╤В╤Л╨▓╨░╨╡╨╝: +          </p> +        </header> + +        <ul className="list-disc space-y-2 pl-5 text-sm"> +          <li>Telegram-╨░╨║╨║╨░╤Г╨╜╤В</li> +          <li>deviceId</li> +          <li> +            ╤Б╤В╨░╤В╨╕╤Б╤В╨╕╨║╤Г ╨┤╨╡╨╣╤Б╤В╨▓╨╕╨╣ (╤Д╨╛╤В╨╛, ╤В╨╡╨║╤Б╤В, ╤А╤Г╤З╨╜╨╛╨╣ ╨▓╨▓╨╛╨┤, ╤И╤В╤А╨╕╤Е╨║╨╛╨┤ ╨╕ ╤Г╤В╨╛╤З╨╜╨╡╨╜╨╕╤П) +          </li> +          <li>╨┐╨╗╨░╤В╨╡╨╢╨╕ ╨╕ ╨┐╨╛╨┤╨┐╨╕╤Б╨║╤Г</li> +          <li>╤В╨╡╤Е╨╜╨╕╤З╨╡╤Б╨║╨╕╨╡ ╨╗╨╛╨│╨╕</li> +        </ul> + +        <p className="rounded-md border border-border bg-card p-4 text-sm"> +          ╨Ф╨╜╨╡╨▓╨╜╨╕╨║ ╨╕ ╨Ъ╨С╨Ц╨г ╨╛╤Б╤В╨░╤О╤В╤Б╤П ╨╜╨░ ╤Г╤Б╤В╤А╨╛╨╣╤Б╤В╨▓╨╡. +        </p> + +        {privacyUrl ? ( +          <a +            className="inline-block text-sm font-medium text-primary underline underline-offset-4" +            href={privacyUrl} +            target="_blank" +            rel="noopener noreferrer" +          > +            ╨Я╨╛╨╗╨╕╤В╨╕╨║╨░ ╨║╨╛╨╜╤Д╨╕╨┤╨╡╨╜╤Ж╨╕╨░╨╗╤М╨╜╨╛╤Б╤В╨╕ +          </a> +        ) : ( +          <span className="text-sm text-muted-foreground"> +            ╨Я╨╛╨╗╨╕╤В╨╕╨║╨░ ╨║╨╛╨╜╤Д╨╕╨┤╨╡╨╜╤Ж╨╕╨░╨╗╤М╨╜╨╛╤Б╤В╨╕ +          </span> +        )} + +        <label className="flex cursor-pointer items-start gap-3 rounded-md border border-border bg-card p-4 text-sm"> +          <input +            type="checkbox" +            className="mt-0.5 h-4 w-4 accent-primary" +            checked={accepted} +            onChange={(event) => setAccepted(event.target.checked)} +          /> +          <span>╨б╨╛╨│╨╗╨░╤Б╨╡╨╜ ╨╜╨░ ╨╛╨▒╤А╨░╨▒╨╛╤В╨║╤Г ╤Г╨║╨░╨╖╨░╨╜╨╜╤Л╤Е ╨┤╨░╨╜╨╜╤Л╤Е</span> +        </label> + +        <Button +          className="w-full" +          disabled={!accepted || submitting} +          onClick={() => void handleSubmit()} +        > +          {submitting ? '╨б╨╛╤Е╤А╨░╨╜╤П╨╡╨╝тАж' : '╨Я╤А╨╛╨┤╨╛╨╗╨╢╨╕╤В╤М'} +        </Button> +      </div> +    </main> +  ); +}
```
