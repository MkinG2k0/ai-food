# Review package Task 6
BASE: c562e3e8126815772b0d3fbf3fc81d22255fc180
HEAD: 6adcfbc14c1af2de3cc28a5a90426ba003593820

## Commits
6adcfbc feat(ai-web): admin users list and detail pages

## Stat
 apps/ai-web/src/app/admin/users/[id]/page.tsx      | 291 +++++++++++++++++++++  apps/ai-web/src/app/admin/users/page.tsx           | 199 ++++++++++++++  .../src/app/api/admin/gateway/users/[id]/route.ts  |  10 +  apps/ai-web/src/components/AdminShell.tsx          |   7 +  4 files changed, 507 insertions(+)

## Diff
```
diff --git a/apps/ai-web/src/app/admin/users/[id]/page.tsx b/apps/ai-web/src/app/admin/users/[id]/page.tsx new file mode 100644 index 0000000..e4337cc --- /dev/null +++ b/apps/ai-web/src/app/admin/users/[id]/page.tsx @@ -0,0 +1,291 @@ +'use client'; + +import { useParams, useRouter } from 'next/navigation'; +import { useQuery } from '@tanstack/react-query'; +import type { ColumnsType } from 'antd/es/table'; +import { +  Alert, +  Avatar, +  Button, +  Card, +  Col, +  Descriptions, +  Row, +  Statistic, +  Table, +  Tag, +  Typography, +} from 'antd'; + +import { PageHeader } from '@/components/PageHeader'; +import { adminApi } from '@/lib/adminApi'; + +type UsageCounts = { +  analyze_photo: number; +  analyze_text: number; +  analyze_photo_text: number; +  refine: number; +  manual: number; +  barcode: number; +  analyze: number; +}; + +type AdminUser = { +  id: string; +  telegramId: string; +  username: string | null; +  firstName: string | null; +  lastName: string | null; +  photoUrl: string | null; +  subscriptionStatus: string; +  subscriptionExpiresAt: string | null; +  hasActiveSubscription: boolean; +  dataConsentAt: string | null; +  dataConsentVersion: string | null; +  createdAt?: string; +}; + +type PaymentStatus = 'pending' | 'confirmed' | 'rejected' | 'refunded'; + +type Payment = { +  id: string; +  amount: number; +  status: PaymentStatus; +  paidAt: string | null; +  createdAt: string; +  tbankPaymentId: string | null; +  tbankOrderId: string; +}; + +type UsageEvent = { +  id: string; +  kind: string; +  deviceId: string; +  createdAt: string; +}; + +type UserDetailResponse = { +  user: AdminUser; +  usageCounts: UsageCounts; +  payments: Payment[]; +  recentEvents: UsageEvent[]; +}; + +const statusColor: Record<PaymentStatus, string> = { +  pending: 'processing', +  confirmed: 'success', +  rejected: 'error', +  refunded: 'warning', +}; + +const statusLabel: Record<PaymentStatus, string> = { +  pending: '╨Ю╨╢╨╕╨┤╨░╨╡╤В', +  confirmed: '╨Я╨╛╨┤╤В╨▓╨╡╤А╨╢╨┤╤С╨╜', +  rejected: '╨Ю╤В╨║╨╗╨╛╨╜╤С╨╜', +  refunded: '╨Т╨╛╨╖╨▓╤А╨░╤Й╤С╨╜', +}; + +const usageLabels: Record<keyof UsageCounts, string> = { +  analyze_photo: '╨д╨╛╤В╨╛', +  analyze_text: '╨в╨╡╨║╤Б╤В', +  analyze_photo_text: '╨д╨╛╤В╨╛ + ╤В╨╡╨║╤Б╤В', +  refine: '╨г╤В╨╛╤З╨╜╨╡╨╜╨╕╤П', +  manual: '╨а╤Г╤З╨╜╨╛╨╣ ╨▓╨▓╨╛╨┤', +  barcode: '╨и╤В╤А╨╕╤Е╨║╨╛╨┤╤Л', +  analyze: 'Legacy', +}; + +const formatRubles = (kopecks: number) => +  new Intl.NumberFormat('ru-RU', { +    currency: 'RUB', +    maximumFractionDigits: 2, +    style: 'currency', +  }).format(kopecks / 100); + +const formatDate = (value?: string | null) => +  value +    ? new Intl.DateTimeFormat('ru-RU', { +        dateStyle: 'medium', +        timeStyle: 'short', +      }).format(new Date(value)) +    : 'тАФ'; + +export default function UserDetailPage() { +  const params = useParams<{ id: string }>(); +  const router = useRouter(); +  const id = params.id; +  const userQuery = useQuery({ +    queryKey: ['admin', 'users', id], +    queryFn: () => +      adminApi<UserDetailResponse>(`users/${encodeURIComponent(id)}`), +    enabled: Boolean(id), +  }); + +  const paymentColumns: ColumnsType<Payment> = [ +    { +      dataIndex: 'createdAt', +      key: 'createdAt', +      render: formatDate, +      title: '╨б╨╛╨╖╨┤╨░╨╜', +      width: 180, +    }, +    { +      dataIndex: 'amount', +      key: 'amount', +      render: (amount: number) => formatRubles(amount), +      title: '╨б╤Г╨╝╨╝╨░', +      width: 140, +    }, +    { +      dataIndex: 'status', +      key: 'status', +      render: (status: PaymentStatus) => ( +        <Tag color={statusColor[status]}>{statusLabel[status]}</Tag> +      ), +      title: '╨б╤В╨░╤В╤Г╤Б', +      width: 140, +    }, +    { +      dataIndex: 'tbankOrderId', +      key: 'tbankOrderId', +      title: 'ID ╨╖╨░╨║╨░╨╖╨░', +    }, +    { +      dataIndex: 'paidAt', +      key: 'paidAt', +      render: formatDate, +      title: '╨Ю╨┐╨╗╨░╤З╨╡╨╜', +      width: 180, +    }, +  ]; + +  const eventColumns: ColumnsType<UsageEvent> = [ +    { +      dataIndex: 'kind', +      key: 'kind', +      title: '╨в╨╕╨┐', +    }, +    { +      dataIndex: 'deviceId', +      key: 'deviceId', +      title: 'Device ID', +    }, +    { +      dataIndex: 'createdAt', +      key: 'createdAt', +      render: formatDate, +      title: '╨б╨╛╨╖╨┤╨░╨╜╨╛', +      width: 180, +    }, +  ]; + +  const data = userQuery.data; +  const user = data?.user; +  const name = user +    ? [user.firstName, user.lastName].filter(Boolean).join(' ') +    : ''; + +  return ( +    <> +      <PageHeader +        extra={<Button onClick={() => router.push('/admin/users')}>╨Ъ ╤Б╨┐╨╕╤Б╨║╤Г</Button>} +        subtitle={user ? name || user.username || user.telegramId : '╨Ъ╨░╤А╤В╨╛╤З╨║╨░ ╨░╨║╨║╨░╤Г╨╜╤В╨░'} +        title="╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М" +      /> +      {userQuery.error ? ( +        <Alert +          description={userQuery.error.message} +          message="╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╖╨░╨│╤А╤Г╨╖╨╕╤В╤М ╨┐╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤П" +          showIcon +          type="error" +        /> +      ) : null} +      <Card loading={userQuery.isLoading} title="╨Я╤А╨╛╤Д╨╕╨╗╤М"> +        {user ? ( +          <Descriptions bordered column={{ lg: 2, md: 2, sm: 1, xs: 1 }}> +            <Descriptions.Item label="╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М"> +              <Avatar src={user.photoUrl} style={{ marginRight: 8 }}> +                {(user.firstName || user.username || '?').slice(0, 1)} +              </Avatar> +              {name || '╨С╨╡╨╖ ╨╕╨╝╨╡╨╜╨╕'} +            </Descriptions.Item> +            <Descriptions.Item label="Username"> +              {user.username ? `@${user.username}` : 'тАФ'} +            </Descriptions.Item> +            <Descriptions.Item label="Telegram ID"> +              {user.telegramId} +            </Descriptions.Item> +            <Descriptions.Item label="╨Т╨╜╤Г╤В╤А╨╡╨╜╨╜╨╕╨╣ ID"> +              {user.id} +            </Descriptions.Item> +            <Descriptions.Item label="╨Я╨╛╨┤╨┐╨╕╤Б╨║╨░"> +              <Tag color={user.hasActiveSubscription ? 'success' : 'default'}> +                {user.hasActiveSubscription ? '╨Р╨║╤В╨╕╨▓╨╜╨░' : '╨Э╨╡ ╨░╨║╤В╨╕╨▓╨╜╨░'} +              </Tag> +              ╨┤╨╛ {formatDate(user.subscriptionExpiresAt)} +            </Descriptions.Item> +            <Descriptions.Item label="╨б╨╛╨│╨╗╨░╤Б╨╕╨╡"> +              {user.dataConsentAt +                ? `╨Ф╨░, ${formatDate(user.dataConsentAt)} (${user.dataConsentVersion ?? '╨▓╨╡╤А╤Б╨╕╤П ╨╜╨╡ ╤Г╨║╨░╨╖╨░╨╜╨░'})` +                : '╨Э╨╡╤В'} +            </Descriptions.Item> +            <Descriptions.Item label="╨б╨╛╨╖╨┤╨░╨╜"> +              {formatDate(user.createdAt)} +            </Descriptions.Item> +          </Descriptions> +        ) : null} +      </Card> + +      <div> +        <Typography.Title className="admin-section-title" level={4}> +          ╨б╤В╨░╤В╨╕╤Б╤В╨╕╨║╨░ ╨│╨╡╨╜╨╡╤А╨░╤Ж╨╕╨╣ +        </Typography.Title> +        <Row gutter={[16, 16]}> +          {(Object.keys(usageLabels) as Array<keyof UsageCounts>).map((kind) => ( +            <Col key={kind} lg={6} md={8} sm={12} xs={24}> +              <Card className="admin-stat-card" size="small"> +                <Statistic +                  loading={userQuery.isLoading} +                  title={usageLabels[kind]} +                  value={data?.usageCounts[kind] ?? 0} +                /> +              </Card> +            </Col> +          ))} +        </Row> +      </div> + +      <div> +        <Typography.Title className="admin-section-title" level={4}> +          ╨Я╨╗╨░╤В╨╡╨╢╨╕ +        </Typography.Title> +        <Table<Payment> +          columns={paymentColumns} +          dataSource={data?.payments ?? []} +          loading={userQuery.isLoading} +          locale={{ emptyText: '╨Я╨╗╨░╤В╨╡╨╢╨╡╨╣ ╨╜╨╡╤В' }} +          pagination={{ pageSize: 10 }} +          rowKey="id" +          scroll={{ x: 900 }} +          size="middle" +        /> +      </div> + +      <div> +        <Typography.Title className="admin-section-title" level={4}> +          ╨Я╨╛╤Б╨╗╨╡╨┤╨╜╨╕╨╡ ╤Б╨╛╨▒╤Л╤В╨╕╤П +        </Typography.Title> +        <Table<UsageEvent> +          columns={eventColumns} +          dataSource={data?.recentEvents ?? []} +          loading={userQuery.isLoading} +          locale={{ emptyText: '╨б╨╛╨▒╤Л╤В╨╕╨╣ ╨╜╨╡╤В' }} +          pagination={{ pageSize: 10 }} +          rowKey="id" +          scroll={{ x: 700 }} +          size="middle" +        /> +      </div> +    </> +  ); +} diff --git a/apps/ai-web/src/app/admin/users/page.tsx b/apps/ai-web/src/app/admin/users/page.tsx new file mode 100644 index 0000000..25882ff --- /dev/null +++ b/apps/ai-web/src/app/admin/users/page.tsx @@ -0,0 +1,199 @@ +'use client'; + +import { useState } from 'react'; +import { useRouter } from 'next/navigation'; +import { useQuery } from '@tanstack/react-query'; +import type { ColumnsType } from 'antd/es/table'; +import { Input, Table, Tag, Typography } from 'antd'; + +import { PageHeader } from '@/components/PageHeader'; +import { adminApi } from '@/lib/adminApi'; + +type UsageCounts = { +  analyze_photo: number; +  analyze_text: number; +  analyze_photo_text: number; +  refine: number; +  manual: number; +  barcode: number; +  analyze: number; +}; + +type AdminUser = { +  id: string; +  telegramId: string; +  username: string | null; +  firstName: string | null; +  lastName: string | null; +  photoUrl: string | null; +  subscriptionStatus: string; +  subscriptionExpiresAt: string | null; +  hasActiveSubscription: boolean; +  dataConsentAt: string | null; +  dataConsentVersion: string | null; +  createdAt?: string; +  usageCounts: UsageCounts; +}; + +type UsersResponse = { +  users: AdminUser[]; +}; + +const formatDate = (value?: string | null) => +  value +    ? new Intl.DateTimeFormat('ru-RU', { +        dateStyle: 'medium', +        timeStyle: 'short', +      }).format(new Date(value)) +    : 'тАФ'; + +export default function UsersPage() { +  const router = useRouter(); +  const [query, setQuery] = useState(''); +  const usersQuery = useQuery({ +    queryKey: ['admin', 'users', query], +    queryFn: () => +      adminApi<UsersResponse>(`users?q=${encodeURIComponent(query)}`), +  }); + +  const columns: ColumnsType<AdminUser> = [ +    { +      key: 'name', +      render: (_, user) => { +        const name = [user.firstName, user.lastName].filter(Boolean).join(' '); +        return ( +          <Typography.Text strong> +            {name || user.username || '╨С╨╡╨╖ ╨╕╨╝╨╡╨╜╨╕'} +          </Typography.Text> +        ); +      }, +      title: '╨Ш╨╝╤П', +      width: 180, +    }, +    { +      dataIndex: 'username', +      key: 'username', +      render: (username: string | null) => (username ? `@${username}` : 'тАФ'), +      title: 'Username', +      width: 160, +    }, +    { +      dataIndex: 'telegramId', +      key: 'telegramId', +      title: 'Telegram ID', +      width: 160, +    }, +    { +      key: 'subscription', +      render: (_, user) => ( +        <Tag color={user.hasActiveSubscription ? 'success' : 'default'}> +          {user.hasActiveSubscription ? '╨Р╨║╤В╨╕╨▓╨╜╨░' : '╨Э╨╡ ╨░╨║╤В╨╕╨▓╨╜╨░'} +        </Tag> +      ), +      title: '╨Я╨╛╨┤╨┐╨╕╤Б╨║╨░', +      width: 130, +    }, +    { +      key: 'consent', +      render: (_, user) => ( +        <> +          <Tag color={user.dataConsentAt ? 'success' : 'default'}> +            {user.dataConsentAt ? '╨Ф╨░' : '╨Э╨╡╤В'} +          </Tag> +          {user.dataConsentAt ? formatDate(user.dataConsentAt) : null} +        </> +      ), +      title: '╨б╨╛╨│╨╗╨░╤Б╨╕╨╡', +      width: 220, +    }, +    { +      dataIndex: ['usageCounts', 'analyze_photo'], +      key: 'analyze_photo', +      title: '╨д╨╛╤В╨╛', +      width: 80, +    }, +    { +      dataIndex: ['usageCounts', 'analyze_text'], +      key: 'analyze_text', +      title: '╨в╨╡╨║╤Б╤В', +      width: 80, +    }, +    { +      dataIndex: ['usageCounts', 'analyze_photo_text'], +      key: 'analyze_photo_text', +      title: '╨д+╨в', +      width: 70, +    }, +    { +      dataIndex: ['usageCounts', 'refine'], +      key: 'refine', +      title: 'Refine', +      width: 80, +    }, +    { +      dataIndex: ['usageCounts', 'manual'], +      key: 'manual', +      title: '╨а╤Г╤З╨╜.', +      width: 80, +    }, +    { +      dataIndex: ['usageCounts', 'barcode'], +      key: 'barcode', +      title: '╨и╨Ъ', +      width: 65, +    }, +    { +      dataIndex: ['usageCounts', 'analyze'], +      key: 'analyze', +      title: 'Legacy', +      width: 80, +    }, +    { +      dataIndex: 'createdAt', +      key: 'createdAt', +      render: formatDate, +      title: '╨б╨╛╨╖╨┤╨░╨╜', +      width: 180, +    }, +  ]; + +  const users = usersQuery.data?.users ?? []; + +  return ( +    <> +      <PageHeader +        subtitle="╨Р╨║╨║╨░╤Г╨╜╤В╤Л, ╤Б╨╛╨│╨╗╨░╤Б╨╕╨╡ ╨╕ ╤Б╤В╨░╤В╨╕╤Б╤В╨╕╨║╨░ ╨│╨╡╨╜╨╡╤А╨░╤Ж╨╕╨╣" +        title="╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╨╕" +      /> +      <Input.Search +        allowClear +        enterButton="╨Э╨░╨╣╤В╨╕" +        onSearch={(value) => setQuery(value.trim())} +        placeholder="ID, Telegram ID ╨╕╨╗╨╕ ╨╕╨╝╤П ╨┐╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤П" +        style={{ maxWidth: 420, width: '100%' }} +      /> +      <Table<AdminUser> +        columns={columns} +        dataSource={users} +        loading={usersQuery.isLoading} +        locale={{ +          emptyText: usersQuery.error +            ? usersQuery.error.message +            : '╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╨╕ ╨╜╨╡ ╨╜╨░╨╣╨┤╨╡╨╜╤Л', +        }} +        onRow={(user) => ({ +          onClick: () => router.push(`/admin/users/${user.id}`), +          style: { cursor: 'pointer' }, +        })} +        pagination={{ +          pageSize: 10, +          showSizeChanger: true, +          showTotal: (total) => `╨Т╤Б╨╡╨│╨╛ ${total}`, +        }} +        rowKey="id" +        scroll={{ x: 1700 }} +        size="middle" +      /> +    </> +  ); +} diff --git a/apps/ai-web/src/app/api/admin/gateway/users/[id]/route.ts b/apps/ai-web/src/app/api/admin/gateway/users/[id]/route.ts new file mode 100644 index 0000000..4988212 --- /dev/null +++ b/apps/ai-web/src/app/api/admin/gateway/users/[id]/route.ts @@ -0,0 +1,10 @@ +import { proxyGatewayAdmin } from '@/lib/gatewayAdmin'; + +export async function GET( +  _request: Request, +  context: { params: Promise<{ id: string }> }, +) { +  const { id } = await context.params; + +  return proxyGatewayAdmin(`users/${encodeURIComponent(id)}`); +} diff --git a/apps/ai-web/src/components/AdminShell.tsx b/apps/ai-web/src/components/AdminShell.tsx index 8d0f085..478f6cb 100644 --- a/apps/ai-web/src/components/AdminShell.tsx +++ b/apps/ai-web/src/components/AdminShell.tsx @@ -4,32 +4,39 @@ import { useState } from 'react';  import { usePathname, useRouter } from 'next/navigation';  import {    BarChartOutlined,    CreditCardOutlined,    LogoutOutlined,    TagsOutlined, +  UserOutlined,    WalletOutlined,  } from '@ant-design/icons';  import { Button, Layout, Menu, Typography } from 'antd';    const { Content, Header, Sider } = Layout;    const menuItems = [    { icon: <BarChartOutlined />, key: '/admin', label: '╨Ю╨▒╨╖╨╛╤А' },    { icon: <TagsOutlined />, key: '/admin/pricing', label: '╨ж╨╡╨╜╤Л' }, +  { +    icon: <UserOutlined />, +    key: '/admin/users', +    label: '╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╨╕', +  },    { icon: <WalletOutlined />, key: '/admin/payments', label: '╨Я╨╗╨░╤В╨╡╨╢╨╕' },    {      icon: <CreditCardOutlined />,      key: '/admin/subscriptions',      label: '╨Я╨╛╨┤╨┐╨╕╤Б╨║╨╕',    },  ];    const pageTitles: Record<string, string> = {    '/admin': '╨Ю╨▒╨╖╨╛╤А',    '/admin/pricing': '╨ж╨╡╨╜╤Л', +  '/admin/users': '╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╨╕',    '/admin/payments': '╨Я╨╗╨░╤В╨╡╨╢╨╕',    '/admin/subscriptions': '╨Я╨╛╨┤╨┐╨╕╤Б╨║╨╕',  };    export function AdminShell({ children }: { children: React.ReactNode }) {    const pathname = usePathname();
```
