---
phase: quick-260716-4dd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
  - apps/mobile/src/entities/meal/ui/MealCard.tsx
autonomous: true
requirements:
  - QUICK-4dd

must_haves:
  truths:
    - "Ready MealCard shows large calories text plus a single non-wrapping row of colored letter circles Б/Ж/У/К with gram values — not pill badges."
    - "Title truncates on one line with time flush right; macro row never wraps to a second line."
    - "Analyzing MealCard skeleton mirrors the compact calories + circles layout (no pill-shaped skeletons)."
    - "FoodItemDisplayCard still renders the existing colored pill badges via FoodMacrosBadges default."
  artifacts:
    - apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
    - apps/mobile/src/entities/meal/ui/MealCard.tsx
  key_links:
    - "MealCard ready branch → FoodMacrosBadges density=compact (large kcal + circles)"
    - "FoodItemDisplayCard → FoodMacrosBadges default (pills unchanged)"
    - "MealCard analyzing branch → compact skeleton matching calories + four/five circle slots"
---

<objective>
Give MealCard the compact reference layout: large calories + colored letter circles for macros (Б/Ж/У/К) per locked D-01, while keeping pill badges for FoodItemDisplayCard.

Purpose: Pill badges wrap on narrow cards and break the diary list look the user confirmed from the reference screenshot.

Output: density-aware FoodMacrosBadges + MealCard ready/analyzing UI using compact mode.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/mobile/src/entities/meal/ui/MealCard.tsx
@apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
@apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx
@apps/mobile/src/entities/meal/index.ts

## Locked decision (NON-NEGOTIABLE)

- **D-01 Compact reference layout:** MealCard uses large calories text + colored letter circles for macros (Б/Ж/У/К), NOT the current colored pill badges that wrap. User confirmed: «Как на первом скрине: крупные калории + цветные кружки с буквами».

## Constraints

- Do not change FoodItemDisplayCard visual — pills stay default.
- Russian letters only: Б / Ж / У / К (клетчатка short letter «К»; full «Кл» remains on pills only).
- Title truncate + time on right (already present) — keep; ensure macro row `flex-nowrap` / `overflow-hidden` so it cannot wrap.
- Analyzing and error states stay compact (no tall pill skeleton row).
- FSD: keep component in `entities/meal`; no new cross-slice imports needed.
- Prefer optional `density` prop on existing `FoodMacrosBadges` over a second public component (Claude discretion): `density?: 'badges' | 'compact'` default `'badges'`.
- No new dependencies. No barrel API break if exports unchanged.

## Visual contract (compact)

Row layout inside MealCard content (below title/time):

1. **Calories** — large tabular-nums number (`text-lg` or `text-xl font-semibold`) + muted «ккал» beside or under as fits the row; must read larger than macro grams.
2. **Macros** — for protein/fat/carbs/fiber: fixed-size colored circle (`h-5 w-5` or `h-6 w-6 rounded-full`) with white/contrasting letter, then gram value (`tabular-nums text-xs` or `text-sm`) immediately after; gap between groups small and even.
3. Colors reuse current badge palette: Б blue, Ж red, У amber, К teal; kcal accent emerald for number or «ккал» label.
4. Entire macros strip: single horizontal flex, `flex-nowrap`, `min-w-0`, items vertically centered; do not use `flex-wrap`.
5. Values: `Math.round` like today; fiber `(fiber ?? 0)`.

## Analyzing skeleton contract

Replace the five `rounded-full` pill skeletons with: one short wide bar for kcal + four small circle skeletons (optional fifth for fiber if compact shows fiber — yes, show fiber circle in ready and analyzing for parity with D-01 Б/Ж/У/К).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact density to FoodMacrosBadges</name>
  <files>apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx</files>
  <action>
Extend `FoodMacrosBadgesProps` with optional `density?: 'badges' | 'compact'` defaulting to `'badges'` (per D-01 MealCard needs compact; FoodItemDisplayCard must keep pills).

When `density === 'badges'` (default): keep the current Badge pill UI unchanged (calories ккал + Б/Ж/У/Кл grams). Do not regress FoodItemDisplayCard.

When `density === 'compact'`: render a single non-wrapping horizontal row — large calories (`Math.round(calories)`) with «ккал» label, then four letter circles Б/Ж/У/К with gram values for protein/fat/carbs/fiber. Circles are `rounded-full` with the existing color families (blue/red/amber/teal backgrounds, letter contrast). No Badge pills, no `flex-wrap`. Use Russian letters only (not Latin P/F/C). Fiber circle letter is «К» (not «Кл»).

Keep `MacroDigits` or equivalent tabular-nums sizing for compact grams so values stay aligned without expanding the row.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec tsc --noEmit</automated>
  </verify>
  <done>
FoodMacrosBadges supports density default badges + compact circles; default call sites still get pills; compact mode matches D-01 (large kcal + Б/Ж/У/К circles, no wrap).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire MealCard ready and analyzing to compact</name>
  <files>apps/mobile/src/entities/meal/ui/MealCard.tsx</files>
  <action>
In the ready branch, pass `density="compact"` to `FoodMacrosBadges` (same calorie/totals props). Preserve title truncate + time on the right.

In the analyzing branch, replace the wrapping pill-shaped Skeleton row with a compact skeleton that mirrors compact density: one kcal-sized skeleton + four circle skeletons in a `flex-nowrap` row (per D-01 visual parity while loading). Keep the existing bounce dots + «Анализ еды…» and title skeleton.

Error branch stays a single compact line (destructive message + time) — no pill row.

Do not edit FoodItemDisplayCard or entities/meal/index.ts unless barrel types need exporting (prefer no barrel change).
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec tsc --noEmit</automated>
  </verify>
  <done>
MealCard ready uses compact macros; analyzing skeleton is compact circles not pills; FoodItemDisplayCard unchanged and still pills.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure presentational change over already-local Meal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4dd-01 | Information disclosure | MealCard aria-label / visible macros | low | accept | Same meal nutrition already shown; no new data surfaces |
| T-4dd-02 | Tampering | N/A | low | accept | Client-only UI; no persistence or API changes |
| T-4dd-SC | Tampering | npm installs | high | accept | No new packages in this plan |
</threat_model>

<verification>
- `pnpm --filter @ai-food/mobile exec tsc --noEmit` passes
- Grep: MealCard calls FoodMacrosBadges with density compact; FoodItemDisplayCard call has no density (or badges)
- Ready card: large kcal + circles; no Badge pills on MealCard
- Analyzing: no flex-wrap pill skeletons
</verification>

<success_criteria>
- D-01 delivered: MealCard looks like reference (large calories + colored letter circles Б/Ж/У/К)
- Macro row never wraps; title still truncates with time on right
- FoodItemDisplayCard pill badges unchanged
- Type-check clean
</success_criteria>

<output>
Create `.planning/quick/260716-4dd-mealcard/260716-4dd-SUMMARY.md` when done
</output>
