---
phase: 260819-218-merge-camera-and-camera-description-into
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ai-food/src/pages/scan/ui/ScanPage.tsx
  - apps/ai-food/src/features/add-food/ui/AddFoodSheet.tsx
  - apps/ai-food/src/shared/lib/addFoodDeepLink.ts
  - apps/ai-food/src/shared/lib/addFoodDeepLink.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Food camera chrome is four slots in order: torch, main shutter, smaller pen capture, gallery (D-01, D-02)"
    - "Main shutter in food mode captures and starts analysis immediately; pen capture shows the existing pending preview plus voice textarea plus submit (D-04, D-07)"
    - "In barcode mode the pen capture control is not usable and does not take a food photo (D-05)"
    - "Add-food sheet keeps camera/barcode and text-only describe; it no longer has a separate camera-plus-text row (D-03)"
    - "aifood://add/scan-describe and the existing Android Камера+ widget still work and open unified /scan, not a describe-only shutter mode (D-06)"
  artifacts:
    - path: apps/ai-food/src/pages/scan/ui/ScanPage.tsx
      provides: "Four-slot chrome; shutter vs pen capture intents; barcode hides pen slot without collapsing layout"
    - path: apps/ai-food/src/features/add-food/ui/AddFoodSheet.tsx
      provides: "Single camera/barcode entry to /scan"
    - path: apps/ai-food/src/shared/lib/addFoodDeepLink.ts
      provides: "scan-describe token aliases to /scan"
    - path: apps/ai-food/src/shared/lib/addFoodDeepLink.test.ts
      provides: "Alias mapping coverage for scan-describe"
  key_links:
    - from: ScanPage food shutter
      to: useSaveMeal({ image })
      via: "existing jpeg snapshot + navigate home (D-07)"
    - from: ScanPage pen button
      to: pendingPhoto SubpageShell with TextareaWithVoice
      via: "same capture lock + jpegFileFromCanvas as today's describe shutter (D-04)"
    - from: parseAddFoodDeepLink scan-describe
      to: /scan
      via: "ROUTE_BY_ACTION alias; Android widget Java unchanged (D-06)"
---

<objective>
Merge the two camera entry points into one scan screen: shutter analyzes the photo immediately, a smaller PenLine button to the right of the shutter opens the existing photo-plus-description form, and add-food / widgets no longer keep a separate describe-only camera mode (D-01 D-02 D-03 D-04 D-05 D-06 D-07).

Purpose: One camera surface instead of two menu rows that opened the same viewfinder with different shutter behavior.
Output: ScanPage four-slot chrome + AddFoodSheet without the extra camera row + scan-describe deep link aliased to `/scan`.
</objective>

<execution_context>
@C:/Users/mk/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/mk/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260819-218-merge-camera-and-camera-description-into/260819-218-CONTEXT.md
@apps/ai-food/src/pages/scan/ui/ScanPage.tsx
@apps/ai-food/src/pages/scan/lib/createCaptureLock.ts
@apps/ai-food/src/features/add-food/ui/AddFoodSheet.tsx
@apps/ai-food/src/shared/lib/addFoodDeepLink.ts
@apps/ai-food/src/shared/lib/addFoodDeepLink.test.ts
@.cursor/rules/index-reexports.mdc

## Locked (D-01 … D-07)

- D-01: Bottom bar four slots, left to right: torch | main shutter | photo-plus-description | gallery. Description control sits immediately right of the shutter, not at the far right.
- D-02: Description control is smaller than the main shutter (not a second 4.5rem shutter). Icon is lucide `PenLine` (same icon already used in AddFoodSheet).
- D-03: Remove the extra camera-plus-text outline row from AddFoodSheet. Keep «Камера / Штрихкод» and the text-only «Описать» row. Sheet gallery and scan-screen gallery both stay.
- D-04: After pen capture, keep the existing pending-photo SubpageShell (preview image + TextareaWithVoice + Отправить). Do not merge that form into the live viewfinder.
- D-05: Barcode mode: hide or disable the pen control (food-photo only). Prefer keeping the slot in layout (`invisible` / `opacity-0`, not `hidden`/display-none) so shutter position does not jump.
- D-06: Keep token `scan-describe` and Android ScanDescribeWidgetProvider as aliases. They must open unified `/scan`. Do not keep a describe-only camera mode as the default path. Do not edit Android Java/XML in this plan.
- D-07: Main shutter (food): photo → analyze immediately (today’s `/scan` path). Pen: photo → pending description form (today’s query-flag shutter path). Gallery on ScanPage stays immediate-analyze in food mode (no description form).
</context>

<tasks>

<task type="tracer">
  <name>End-to-end food scan: shutter analyzes, pen opens description form</name>
  <files>apps/ai-food/src/pages/scan/ui/ScanPage.tsx</files>
  <action>
    In ScanPage only (no new FSD slice, no new barrel export). Per D-01 insert a fourth control in the existing bottom chrome row (`flex w-full max-w-sm items-center justify-between`): torch, main shutter, pen, gallery. Per D-02 the pen button is about h-10 w-10 rounded-full bg-black/45 (smaller than the 4.5rem shutter and not a duplicate shutter ring). Import PenLine from lucide-react. Add aria-label «Сфотографировать с описанием».

    Stop driving capture mode from a URL query flag. Per D-07 the food shutter path must always take the current immediate-analyze branch (snapshot via captureLockRef + jpegFileFromCanvas + submitFood({ image }) + stopStream + navigate home). Add a separate click handler for the pen control that reuses the same capture lock and snapshot helpers, then the current pending-photo branch of acceptFoodFile (setPendingPhoto, object URL preview, clear description). Do not send the pen capture to analyze until the existing Отправить handler runs. Per D-04 leave the pending SubpageShell (preview, TextareaWithVoice, Отправить, back-to-camera) as it is; do not move that form onto the live viewfinder.

    Refactor acceptFoodFile so gallery in food mode always uses the immediate-analyze path (D-07). Barcode shutter, barcode gallery, torch, mode toggle, and ML Kit overlay stay as they are.

    Per D-05 when mode is barcode: pen control disabled, aria-hidden, and visually gone while still occupying space (`invisible pointer-events-none`, not display-none) so the four-slot geometry does not collapse. Also disable pen when cameraError or capturing, same as shutter.

    Do not change AddFoodSheet or deep links in this task. Cross-slice imports stay through barrels (`@/shared/ui`, `@/shared/lib`, `@/features/save-meal`).
  </action>
  <verify>
    <automated>cd apps/ai-food && pnpm type-check && node -e "const fs=require('fs'); const p='src/pages/scan/ui/ScanPage.tsx'; const s=fs.readFileSync(p,'utf8'); const code=s.split(/\n/).filter(l=>!/^\\s*(\\/\\/|\\*)/.test(l)).join('\\n'); if(!code.includes('PenLine')) process.exit(1); if(code.includes(\"get('describe')\")) process.exit(1); if(!code.includes('Сфотографировать с описанием')) process.exit(1); if(!code.includes('invisible')) process.exit(1);"</automated>
    <human-check>Open /scan in food mode: chrome is torch | large shutter | smaller pen | gallery. Shutter still sends the photo to analyze. Pen freezes the frame into the existing preview+textarea screen. Switch to barcode: pen slot empty/unusable, shutter still reads barcodes.</human-check>
  </verify>
  <done>
    Unified /scan food chrome has four slots (D-01, D-02). Shutter analyzes immediately; pen uses the existing pending description screen (D-04, D-07). Barcode does not offer pen capture (D-05). Capture mode is not a URL query flag.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Drop duplicate sheet row; alias scan-describe to /scan</name>
  <files>apps/ai-food/src/shared/lib/addFoodDeepLink.test.ts, apps/ai-food/src/shared/lib/addFoodDeepLink.ts, apps/ai-food/src/features/add-food/ui/AddFoodSheet.tsx</files>
  <behavior>
    - parseAddFoodDeepLink('aifood://add/scan-describe') equals { kind: 'route', path: '/scan' }
    - parseAddFoodDeepLink('aifood://add/scan') still equals { kind: 'route', path: '/scan' }
    - ADD_FOOD_DEEP_LINK_ACTIONS still includes the scan-describe token (widget contract unchanged)
  </behavior>
  <action>
    Per D-06 update ROUTE_BY_ACTION so the scan-describe token uses the same path string as scan (`/scan`). Keep the token in ADD_FOOD_DEEP_LINK_ACTIONS. Rewrite the existing test that currently expects a describe query so it expects `/scan`. Do not add new action tokens. Do not modify Android Java, XML, strings, or widget providers — ScanDescribeWidgetProvider already emits scan-describe.

    Per D-03 delete the extra outline Button (and its navigate helper) that currently opens scan with a describe query. Keep handleCameraClick → navigate('/scan'), the text-only Описать flow (setMode describe), gallery, manual, and favorites. PenLine remains on the text-only row. Sheet gallery picker stays.
  </action>
  <verify>
    <automated>cd apps/ai-food && pnpm test -- src/shared/lib/addFoodDeepLink.test.ts && node -e "const fs=require('fs'); const sheet=fs.readFileSync('src/features/add-food/ui/AddFoodSheet.tsx','utf8'); const link=fs.readFileSync('src/shared/lib/addFoodDeepLink.ts','utf8'); const sheetCode=sheet.split(/\\n/).filter(l=>!/^\\s*(\\/\\/|\\*)/.test(l)).join('\\n'); const linkCode=link.split(/\\n/).filter(l=>!/^\\s*(\\/\\/|\\*)/.test(l)).join('\\n'); if(sheetCode.includes('handleCameraDescribeClick')) process.exit(1); if(!sheetCode.includes('Камера / Штрихкод')) process.exit(1); if(!sheetCode.includes('Описать')) process.exit(1); if(!linkCode.includes(\"'scan-describe': '/scan'\")) process.exit(1);"</automated>
  </verify>
  <done>
    Add-food menu has one camera entry (D-03). scan-describe deep link (and therefore the untouched Android widget) opens unified /scan (D-06). Tests lock the alias.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan UI → save-meal / barcode lookup | Photo bytes and optional text stay on-device until existing analyze/save paths |
| Custom-scheme deep link → router | Untrusted URL only selects an in-app route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260819-01 | Tampering | parseAddFoodDeepLink scan-describe | low | mitigate | Alias to `/scan` only (D-06); no extra query that forces a hidden capture mode |
| T-260819-02 | Information disclosure | ScanPage pending photo object URL | low | accept | Existing revokeObjectURL + local Filesystem; this plan does not add network upload |
| T-260819-03 | Elevation of privilege | Add-food widgets / aifood://add/* | low | accept | Same six tokens and same in-app routes as today; widget Java not widened |
| T-260819-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd apps/ai-food && pnpm test -- src/shared/lib/addFoodDeepLink.test.ts`
- `cd apps/ai-food && pnpm type-check`
- Node structural asserts in task verify blocks pass
- Human: /scan four-slot chrome; sheet has no duplicate camera-plus-text row
</verification>

<success_criteria>
- Food scan: torch | shutter | smaller PenLine | gallery (D-01, D-02)
- Shutter analyzes now; pen uses existing preview+textarea submit (D-04, D-07)
- Barcode: pen unused, layout stable (D-05)
- Add-food sheet: camera/barcode + text-only describe; no extra camera-plus-text row (D-03)
- `aifood://add/scan-describe` → `/scan`; Android widget files untouched (D-06)
</success_criteria>

<output>
Create `.planning/quick/260819-218-merge-camera-and-camera-description-into/260819-218-SUMMARY.md` when execution completes
</output>
