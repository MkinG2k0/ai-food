---
phase: 260813-0w7-ai-app-ai-web
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ai-app/src/lib/adminStatsSeries.ts
  - apps/ai-app/src/lib/adminStatsSeries.test.ts
  - apps/ai-app/src/lib/adminRuntimeHealth.ts
  - apps/ai-app/src/lib/adminRuntimeHealth.test.ts
  - apps/ai-app/src/routes/admin.ts
  - apps/ai-web/src/app/api/admin/gateway/health/route.ts
  - apps/ai-web/src/app/admin/health/page.tsx
  - apps/ai-web/src/components/AdminShell.tsx
autonomous: true
requirements:
  - REL-A
  - REL-B
---

<objective>
Ship admin reliability dashboard (variant E): GatewayRequest latency/error daily trends (A) plus live process/DB health (B) on new ai-web page `/admin/health`.

Purpose: Operators see stability and speed of ai-app without APM/alerts.
Output: Extended `/admin/stats/series`, new `GET /admin/health`, ai-web proxy + page + nav entry.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260813-0w7-ai-app-ai-web/260813-0w7-CONTEXT.md
@apps/ai-app/src/lib/adminStatsSeries.ts
@apps/ai-app/src/lib/adminStatsSeries.test.ts
@apps/ai-app/src/lib/gatewayRequestStats.ts
@apps/ai-app/src/lib/percentile.ts
@apps/ai-app/src/routes/admin.ts
@apps/ai-app/src/routes/health.ts
@apps/ai-web/src/app/admin/requests/page.tsx
@apps/ai-web/src/components/AdminShell.tsx
@apps/ai-web/src/lib/adminApi.ts
@apps/ai-web/src/lib/gatewayAdmin.ts
@apps/ai-web/src/app/api/admin/gateway/stats/series/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend stats/series with daily reliability metrics</name>
  <files>apps/ai-app/src/lib/adminStatsSeries.ts, apps/ai-app/src/lib/adminStatsSeries.test.ts, apps/ai-app/src/routes/admin.ts</files>
  <behavior>
    - Empty gateway input → reliability series length equals `days`, all zeros/nulls
    - Mixed ok/error same UTC day → total, errorCount, errorRate correct (rate = errorCount/total)
    - Successful rows with durationMs/ttfbMs → daily p50/p95 match `p50`/`p95` from percentile.js (ok rows only for latency)
    - Existing `series.users|payments|usage|requests` shapes unchanged (backward compatible)
  </behavior>
  <action>
    Per CONTEXT variant E (A): extend `BuildAdminStatsSeriesInput.gatewayRequests` items to include `ok`, `ttfbMs`, `durationMs` (keep `type`, `at`). Add `series.reliability: Array&lt;{ date, total, errorCount, errorRate, p50DurationMs, p95DurationMs, p50TtfbMs, p95TtfbMs }&gt;` using existing `utcDayKey` / day enumeration and `p50`/`p95` from `percentile.js`. Latency percentiles only over ok rows with non-null field values; `errorRate` is 0 when total is 0.

    Update `GET /admin/stats/series` in `admin.ts` to select `ok`, `ttfbMs`, `durationMs` on `gatewayRequest.findMany` and pass them into `buildAdminStatsSeries`. Do not change public `GET /health` (stays minimal). Do not add alerts/SLO or HTTP middleware metrics.
  </action>
  <verify>
    <automated>cd apps/ai-app && pnpm test -- src/lib/adminStatsSeries.test.ts</automated>
  </verify>
  <done>
    `/admin/stats/series` response includes `series.reliability` daily points with volume, error rate, p50/p95 duration and TTFB; prior series keys still work; tests cover aggregation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin-only live runtime health endpoint</name>
  <files>apps/ai-app/src/lib/adminRuntimeHealth.ts, apps/ai-app/src/lib/adminRuntimeHealth.test.ts, apps/ai-app/src/routes/admin.ts</files>
  <behavior>
    - With mocked DB ping success + lag below thresholds → status `ok`, db.ok true, db.latencyMs number
    - DB ping throw / unreachable → status `degraded`, db.ok false
    - Synthetic lag above cutoff → status `degraded` even if DB ok
    - Response always includes uptimeSec, memory.rssBytes, memory.heapUsedBytes, memory.heapTotalBytes, eventLoopLagMs
  </behavior>
  <action>
    Per CONTEXT health full set (B) and admin-only preference: create `collectAdminRuntimeHealth({ pingDb, measureEventLoopLag?, now? })` returning JSON-ready object:
    - `status`: `ok` | `degraded`
    - `uptimeSec`: Math.floor(process.uptime())
    - `memory`: rss/heapUsed/heapTotal from process.memoryUsage()
    - `db`: `{ ok, latencyMs }` via lightweight `SELECT 1` (or prisma `$queryRaw` equivalent) timed with Date.now(); if DB not configured or ping fails → ok false, latencyMs null
    - `eventLoopLagMs`: measure with short setTimeout(0) delta (expected vs actual); inject/measure helper for tests

    Degraded thresholds (Claude discretion, document in helper constants): DB not ok OR db.latencyMs &gt; 500 OR eventLoopLagMs &gt; 100.

    Add `GET /admin/health` on `adminRouter` (already behind `requireAdminKey`). Public `routes/health.ts` remains `{ status: 'ok' }` only.
  </action>
  <verify>
    <automated>cd apps/ai-app && pnpm test -- src/lib/adminRuntimeHealth.test.ts</automated>
  </verify>
  <done>
    Authenticated `GET /admin/health` returns full live metrics + ok/degraded; public `/health` unchanged; unit tests cover ok/degraded paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: ai-web /admin/health page, proxy, and nav</name>
  <files>apps/ai-web/src/app/api/admin/gateway/health/route.ts, apps/ai-web/src/app/admin/health/page.tsx, apps/ai-web/src/components/AdminShell.tsx</files>
  <action>
    Per CONTEXT UI `/admin/health`: add Next route `GET` handler mirroring `stats/route.ts` that calls `proxyGatewayAdmin('health')`.

    Create client page `apps/ai-web/src/app/admin/health/page.tsx` using existing patterns from `requests/page.tsx` / overview:
    - `useQuery` + `adminApi` for `health` (live) and `stats/series?days=7` (trends)
    - Live section: Ant Design Statistic/Tag for status, uptime, DB ping ms, RSS/heap, event-loop lag
    - Trends section: SparklineCard (or equivalent) for daily total volume, errorRate, p50DurationMs, p95DurationMs (TTFB optional if series present)
    - Russian labels consistent with admin UI; loading/error Alert like requests page

    Update `AdminShell.tsx`: menu item + pageTitles for `/admin/health` with HeartOutlined (or DashboardOutlined) icon, label «Стабильность» or «Здоровье», placed after «Запросы».
  </action>
  <verify>
    <automated>cd apps/ai-web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    Logged-in admin can open `/admin/health` from sidebar and see live health + 7-day reliability sparklines fed by gateway proxy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → ai-web `/api/admin/gateway/*` | Session cookie required |
| ai-web → ai-app `/admin/*` | `X-Admin-Key`; untrusted without key |
| Public `/health` | Unauthenticated; must stay minimal (no memory/lag) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260813-01 | Information disclosure | GET /admin/health | medium | mitigate | Keep detailed metrics on adminRouter only; public health unchanged |
| T-260813-02 | Information disclosure | GET /admin/stats/series reliability | low | mitigate | Existing requireAdminKey; no new public surface |
| T-260813-03 | Denial of service | DB ping / event-loop measure | low | accept | Single SELECT 1 + one setTimeout per request; admin-only traffic |
| T-260813-SC | Tampering | npm installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd apps/ai-app && pnpm test -- src/lib/adminStatsSeries.test.ts src/lib/adminRuntimeHealth.test.ts`
- `cd apps/ai-web && pnpm exec tsc --noEmit`
- Manual smoke (optional): open `/admin/health` with admin session — live cards + sparklines render
</verification>

<success_criteria>
- Variant E delivered: latency/error trends from GatewayRequest + live full health on `/admin/health`
- Public `/health` still minimal
- No alerts/SLO, APM, or all-route HTTP middleware
</success_criteria>

<output>
Create `.planning/quick/260813-0w7-ai-app-ai-web/260813-0w7-SUMMARY.md` when execution completes
</output>
