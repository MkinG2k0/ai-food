---
phase: quick-260716-j45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx
  - apps/mobile/src/features/onboarding/ui/OnboardingPage.tsx
autonomous: true
requirements:
  - QUICK-j45

must_haves:
  truths:
    - "После шагов профиля последний экран онбординга показывает блок советов по съёмке еды (русский текст) перед кнопкой «Начать»."
    - "Советы покрывают освещение, полный кадр тарелки, отсутствие лишнего в кадре и достаточную близость к еде — чтобы Vision легче распознал блюдо."
    - "Цели КБЖУ и кнопка «Начать» (finish → Home) сохраняют текущее поведение; новые шаги онбординга и изменения analyze/API не добавляются."
  artifacts:
    - apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx
  key_links:
    - "OnboardingPage isResult → OnboardingResult → tips list + Button onStart=finish"
    - "Tips copy aligned with AddFoodSheet camera/gallery photo flow (no ImagePicker — capture via file input)"
---

<objective>
На последнем шаге онбординга (`OnboardingResult`) показать короткие рекомендации, как фотографировать еду, чтобы ИИ лучше распознавал блюдо.

Purpose: Пользователь сразу после расчёта целей понимает, как снимать еду перед первым анализом — меньше «пустых»/нечитаемых фото.

Output: Обновлённый `OnboardingResult` с русским блоком советов; при необходимости — прокрутка экрана результата, чтобы CTA оставался доступным.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx
@apps/mobile/src/features/onboarding/ui/OnboardingPage.tsx
@apps/mobile/src/features/onboarding/model/useOnboarding.ts
@apps/mobile/src/features/add-food/ui/AddFoodSheet.tsx

## Current last step

- `OnboardingPage`: `TOTAL_STEPS = 7`; при `step > 7` рендерится `OnboardingResult` с `targets` и `onStart={finish}`.
- `OnboardingResult`: заголовок «Ваши цели рассчитаны!», ккал + макросы, кнопка «Начать 🚀». Советов по фото нет.
- Съёмка еды: `AddFoodSheet` — `<input type="file">` камера (`capture="environment"`) и галерея (не отдельный ImagePicker).

## Locked scope

- **D-01:** Только UI последнего шага онбординга — советы по фото. Не трогать vitamin units, analyze-food errors, backend, shared-types, AddFoodSheet.
- **D-02:** Копирайт на русском; тон как у существующего онбординга (emoji/короткие фразы ок).
- **D-03:** Не добавлять 8-й step в `TOTAL_STEPS` / `useOnboarding` — tips живут внутри `OnboardingResult`.
- **D-04:** Не менять `finish()`, persist профиля, micronutrient API, навигацию на `/`.

## Discretion

- 4–5 пунктов достаточно; иконки lucide-react или emoji в стиле экрана — на усмотрение executor.
- Константу списка tips можно держать в том же файле (нет смысла в отдельном slice).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Блок советов по фото в OnboardingResult</name>
  <files>apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx</files>
  <action>
    Per D-01–D-03: в `OnboardingResult.tsx` между списком макросов и кнопкой «Начать» добавить секцию рекомендаций по съёмке еды для AI Vision.

    1. Заголовок секции (русский), например «Как фотографировать еду» или «Советы для точного анализа», с коротким подзаголовком вроде «Так ИИ лучше поймёт, что на фото».
    2. Список из 4–5 конкретных tips (все на русском). Обязательный смысл (формулировки можно слегка отредактировать для краткости):
       - хорошее освещение (дневной свет / без сильной тени и бликов);
       - вся тарелка / всё блюдо в кадре, без обрезки краёв;
       - без лишнего в кадре (руки, салфетки, упаковки, посторонние предметы);
       - достаточно близко / сверху над едой, крупный план;
       - по желанию 5-й пункт: один приём пищи за раз или ровная камера без сильного размытия.
    3. Стиль: Tailwind + существующие токены (`border-border`, `bg-background` / `muted`, `text-muted-foreground`), список читаемый на mobile; не превращать в тяжёлую «dashboard»-карточку с несколькими конкурирующими CTA.
    4. Per D-04: не менять props (`targets`, `onStart`), не менять расчёт целей, не трогать `useOnboarding` / store / API.
    5. Кнопка «Начать 🚀» остаётся единственным CTA завершения онбординга внизу секции.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec tsc --noEmit</automated>
  </verify>
  <done>
    На экране результата онбординга видны русские tips по фото; макросы и «Начать» на месте; type-check зелёный.
  </done>
</task>

<task type="auto">
  <name>Task 2: Прокрутка экрана результата</name>
  <files>apps/mobile/src/features/onboarding/ui/OnboardingPage.tsx, apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx</files>
  <action>
    После добавления tips контент `OnboardingResult` выше типичного viewport на телефоне.

    1. В `OnboardingPage` (и/или корне `OnboardingResult`) обеспечить вертикальную прокрутку на экране результата: `overflow-y-auto` / `min-h-0` на flex-контейнере, чтобы длинный контент не обрезался и кнопка «Начать» была достижима скроллом.
    2. Не возвращать progress bar на result-шаг (текущее `!isResult` оставить).
    3. Не менять шаги 1–7 и логику `back` / `next` / `finish`.
    4. Per D-01: не трогать `AddFoodSheet`, analyze APIs, настройки, дневник.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec tsc --noEmit && pnpm --filter @ai-food/mobile exec vitest run src/features/onboarding/model/useOnboarding.test.ts</automated>
  </verify>
  <done>
    Result-экран прокручивается; CTA доступен; существующие тесты `useOnboarding` зелёные; type-check зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI copy only | Нет нового сетевого I/O; только локальный экран онбординга |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j45-01 | Information Disclosure | OnboardingResult tips | low | accept | Статичный UX-текст без PII и без API keys |
| T-j45-02 | Tampering | OnboardingPage layout | low | accept | Клиентский UI; профиль по-прежнему пишется только через существующий `finish()` |
| T-j45-SC | Tampering | npm installs | low | accept | Новых пакетов в этом плане нет |
</threat_model>

<verification>
- `tsc --noEmit` для `@ai-food/mobile`
- `vitest run` для `useOnboarding.test.ts`
- Визуально: после StepDiet → экран целей + tips + «Начать»
</verification>

<success_criteria>
- Последний шаг онбординга содержит русские рекомендации по фото еды
- Tips покрывают свет, полный кадр, чистоту кадра, близость
- Flow finish → Home не сломан
- Нет изменений вне onboarding UI (analyze/vitamins/errors out of scope)
</success_criteria>

<output>
Create `.planning/quick/260716-j45-onboarding-photo-tips/260716-j45-SUMMARY.md` when done
</output>
