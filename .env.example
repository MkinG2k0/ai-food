# AI Food — environment stubs (copy to .env and fill locally; do not commit secrets)

# Telegram Login Widget bot username (no @). Real widget not wired yet.
# VITE_TELEGRAM_BOT_USERNAME=

# Client mock auth: unset or true = demo sign-in enabled; false = mock button disabled.
# Login Widget domain (BotFather): ai-food-mobile.vercel.app
VITE_AUTH_MOCK=true
