{
  "name": "ai-food-base",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:food": "turbo run dev --filter=ai-food",
    "dev:app": "turbo run dev --filter=openrouter-gateway",
    "build": "turbo run build",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "lint": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=9"
  }
}
