---
phase: 260818-07v-fix-food-analyze-prompt-so-screenshots-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/food/prompts.ts
  - src/food/prompts.screenshot.test.ts
autonomous: true
requirements: [FOOD-SCREENSHOT-01, FOOD-SCREENSHOT-02]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Vision system prompts (Gemini and non-Gemini) treat a screenshot of a dish/product as food to analyze, not as noFood (D-01)"
    - "When composition is on, a screenshot that lists dish ingredients is instructed to become separate items (D-02)"
    - "True noFood remains for people/landscapes/non-food, blurry photos with no food, empty/thrown-away packaging, and a restaurant menu that lists names only with no identifiable dish/product (D-03)"
    - "Physical packaged products (yogurt/juice/bar) still stay one item and are not split from the ingredients list on the physical label (D-04)"
    - "When composition is off, screenshot-of-food is still analyzed, but as a single item (existing feature swap still works)"
    - "No client/parse/UX files under apps/ai-food are modified (D-05)"
  artifacts:
    - src/food/prompts.ts
    - src/food/prompts.screenshot.test.ts
  key_links:
    - "NO_FOOD_PROMPT_RULE and GEMINI_NO_FOOD_PROMPT_RULE are interpolated into LEGACY_SYSTEM_PROMPT / GEMINI_SYSTEM_PROMPT and reached via selectAnalyzeSystemPrompt(hasImage=true)"
    - "COMPOSITION_PROMPT_RULE / GEMINI_COMPOSITION_PROMPT_RULE are the composition-on strings swapped by applyAnalyzeFeaturesToPrompt — screenshot ingredient-split text must live inside those strings so composition-off replaces them"
    - "buildAnalyzeMessages vision path uses selectAnalyzeSystemPrompt + ANALYSIS_PROMPT / ANALYSIS_PROMPT_MULTI"
---

<objective>
Fix food vision analyze prompts so screenshots of food are analyzed (not noFood), and so a visible dish composition on that screenshot becomes items when composition is on.

Purpose: Users log meals from delivery-app / calorie-tracker / dish-card screenshots. The current rules list screenshots as noFood, so those images never produce nutrition. (D-01, D-02)
Output: Updated vision prompt constants in src/food/prompts.ts plus a focused vitest file that locks screenshot vs true-noFood vs composition-on/off behavior. No client changes (D-05).
</objective>

<execution_context>
@C:/Users/mk/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/mk/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/food/prompts.ts
@src/food/analyzeFeatures.ts
@src/food/buildMessages.ts
@src/food/modelConfig.ts

Locked product intent (treat as D-01..D-05; do not reopen):
- D-01: Screenshots of food MUST be analyzed (not noFood). Typical inputs: Yandex Eda / Delivery Club dish pages (photo + ingredient list + calories), calorie-tracker screens, restaurant dish cards, nutrition labels on screenshots.
- D-02: If the screenshot shows composition/ingredients (состав), process that composition into items.
- D-03: Keep true noFood for empty/thrown-away packaging, people/landscapes/non-food, blurry photos with no food, a restaurant menu that lists names only with no identifiable dish/product to log.
- D-04: Do not change prompt semantics beyond this screenshot + composition-on-screenshot behavior. Packaged homogeneous products stay one item (do not split milk/sugar off a yogurt label).
- D-05: Prompts stay in apps/ai-app. Do not edit apps/ai-food.

Planner discretion (do not invent extra features):
- Edit existing vision constants rather than adding a new prompt-assembly branch.
- Put screenshot-is-food in noFood rules, packaged last-line, vision edge-cases, and ANALYSIS_PROMPT / ANALYSIS_PROMPT_MULTI.
- Put screenshot-ingredient-split ONLY inside COMPOSITION_PROMPT_RULE and GEMINI_COMPOSITION_PROMPT_RULE so applyAnalyzeFeaturesToPrompt still swaps it out when composition is false.
- Do not change text-only noFood sections (LEGACY_TEXT_SYSTEM_PROMPT / GEMINI_TEXT_SYSTEM_PROMPT edge cases) except insofar as shared constants (PACKAGED_FOOD_PROMPT_RULE, COMPOSITION_PROMPT_RULE) are reused there.
- Do not change refine/ask/settings prompt behavior except the shared composition/packaged strings.
- Do not add npm packages. Do not call a live LLM in tests.

<interfaces>
From src/food/prompts.ts:
- export const NO_FOOD_PROMPT_RULE
- export const GEMINI_NO_FOOD_PROMPT_RULE
- export const PACKAGED_FOOD_PROMPT_RULE
- export const COMPOSITION_PROMPT_RULE
- export const GEMINI_COMPOSITION_PROMPT_RULE
- export const ANALYSIS_PROMPT
- export const ANALYSIS_PROMPT_MULTI
- export function selectAnalyzeSystemPrompt(hasImage: boolean, model?: string, features?: AnalyzeFeatures): string
- export function buildAnalyzeVisionUserText(imageCount: number, description?: string): string

From src/food/analyzeFeatures.ts:
- export const DEFAULT_ANALYZE_FEATURES = { vitamins: true, healthiness: true, composition: true }
- export const SINGLE_ITEM_COMPOSITION_RULE
- export const GEMINI_SINGLE_ITEM_COMPOSITION_RULE
- applyAnalyzeFeaturesToPrompt replaces compositionOnRule with compositionOffRule via exact string split/join when features.composition is false. Therefore any new screenshot-split sentence MUST be a substring of COMPOSITION_PROMPT_RULE / GEMINI_COMPOSITION_PROMPT_RULE (the values passed as compositionOn).

From src/food/buildMessages.ts:
- buildAnalyzeMessages({ images, model, features }) — when images.length > 0, system text is selectAnalyzeSystemPrompt(true, model, features)

From src/food/modelConfig.ts:
- isGeminiModel(model) is true when /gemini/i matches. Default food model is google/gemini-3-flash-preview (Gemini path). Use a non-gemini model string such as openai/gpt-4.1-mini for the legacy path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end: vision prompt treats a food screenshot as food, not noFood</name>
  <files>src/food/prompts.screenshot.test.ts, src/food/prompts.ts</files>
  <behavior>
    - selectAnalyzeSystemPrompt(true, google/gemini-3-flash-preview) and selectAnalyzeSystemPrompt(true, openai/gpt-4.1-mini) both instruct that a screenshot of a dish/product (delivery-app card, calorie tracker, restaurant dish card, on-screen nutrition label) is food and must be analyzed, not noFood (D-01)
    - Those same vision prompts still list true noFood cases: people/animals/landscapes/non-food, blurry photo with no food, empty plate, trash, empty/thrown-away packaging, and a restaurant menu that is only a list of names with no identifiable dish/product to log (D-03)
    - Those vision prompts do not classify screenshots as noFood as a blanket category (the old pairing of screenshot with noFood in the noFood-case list and in the packaged last-line and in vision edge-cases is gone)
    - buildAnalyzeMessages with images: ['data:image/png;base64,xx'] and a Gemini model includes the screenshot-is-food instruction in the system text
    - buildAnalyzeVisionUserText(1) and buildAnalyzeVisionUserText(2) tell the model a dish/product screenshot is food and to use on-screen composition/KBJU when visible (D-01)
    - selectAnalyzeSystemPrompt(false, ...) text-only prompts are not given a new screenshot-is-food section (D-04); shared packaged-constant cleanup is allowed
  </behavior>
  <action>
    Create src/food/prompts.screenshot.test.ts with vitest describe/it/expect (same style as src/routes/food.test.ts: import from vitest, ESM .js specifiers). Import selectAnalyzeSystemPrompt, ANALYSIS_PROMPT, ANALYSIS_PROMPT_MULTI, buildAnalyzeVisionUserText from ./prompts.js and buildAnalyzeMessages from ./buildMessages.js.

    Write the failing tests first (RED), then edit src/food/prompts.ts until they pass (GREEN). Do not edit apps/ai-food (D-05). Do not change XML schema, micronutrient rules, diet rules, refine offTopic, or healthiness scoring (D-04).

    Prompt edits, all in src/food/prompts.ts, per D-01 and D-03:

    1. NO_FOOD_PROMPT_RULE and GEMINI_NO_FOOD_PROMPT_RULE: in the noFood-cases sentence, remove screenshots as a noFood category. Replace that slot with: restaurant menu that is only a list of names with no specific dish or product to log. Keep people, animals, landscapes, non-food objects, unclear/blurry photo with no food, empty plate, trash/dirt, empty/thrown-away packaging. In the not-noFood sentence, add that a screenshot of food or drink (delivery-app dish card, calorie tracker, restaurant dish card, on-screen KBJU label) is food — analyze the visible dish and on-screen data. Keep the existing closed-package-is-food sentence.

    2. PACKAGED_FOOD_PROMPT_RULE last noFood line: drop the screenshot/menu clause. Keep empty/thrown-away packaging and clearly non-food object. Do not add screenshot analysis into this packaged section (packaged OCR priority 1–5 stays as-is, D-04).

    3. Vision-only edge-case bullets in LEGACY_SYSTEM_PROMPT and GEMINI_SYSTEM_PROMPT (the Краевые случаи blocks that currently lump menu, screenshot, and empty packaging into one noFood line): split into three bullets — (a) screenshot of a dish/product → analyze, not noFood; (b) restaurant menu that is only names with no identifiable dish/photo/product card → noFood; (c) empty/thrown-away packaging with no product → noFood. Do not change the food+person or physical-package bullets. Do not add equivalent screenshot bullets to LEGACY_TEXT_SYSTEM_PROMPT or GEMINI_TEXT_SYSTEM_PROMPT (D-04).

    4. ANALYSIS_PROMPT and ANALYSIS_PROMPT_MULTI: append one short Russian clause that a screenshot of a dish/product is food (not noFood) and that visible composition and KBJU on screen should be used.

    Optional: add one short vision Example D XML (foodName + a few items) labeled as a delivery-app dish-card screenshot, only in LEGACY_SYSTEM_PROMPT examples. Skip if it would require changing the Gemini prompt example structure; Gemini has no example block today — do not add a large Gemini example section (D-04).

    Do not put screenshot-ingredient-split instructions in NO_FOOD_* or edge-cases; that is task 2 so composition-off still swaps cleanly.
  </action>
  <verify>
    <automated>npm test -- src/food/prompts.screenshot.test.ts</automated>
  </verify>
  <done>Gemini and non-Gemini vision prompts assembled by selectAnalyzeSystemPrompt(true, ...) and the vision user text instruct screenshot-of-food analysis (D-01), retain true noFood for name-only menus / empty packaging / non-food (D-03), and the new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Screenshot ingredient list becomes items when composition is on</name>
  <files>src/food/prompts.ts, src/food/prompts.screenshot.test.ts</files>
  <behavior>
    - When composition is true, both Gemini and non-Gemini vision prompts instruct: if a screenshot shows a dish composition/ingredient list (delivery-app / dish card), split items by that list rather than one item named after the dish (D-02)
    - When composition is true, both prompts still forbid splitting a homogeneous packaged product (yogurt/juice/milk/bar) into ingredients from the physical label (D-04)
    - When composition is false, selectAnalyzeSystemPrompt(true, gemini) contains GEMINI_SINGLE_ITEM_COMPOSITION_RULE and does not contain the screenshot-split sentence from GEMINI_COMPOSITION_PROMPT_RULE; selectAnalyzeSystemPrompt(true, openai/gpt-4.1-mini) contains SINGLE_ITEM_COMPOSITION_RULE and does not contain the screenshot-split sentence from COMPOSITION_PROMPT_RULE
    - When composition is false, screenshot-is-food (not noFood) from task 1 is still present (D-01)
  </behavior>
  <action>
    Extend the tests in src/food/prompts.screenshot.test.ts with the composition on/off cases above, then update COMPOSITION_PROMPT_RULE and GEMINI_COMPOSITION_PROMPT_RULE in src/food/prompts.ts (D-02).

    Add one explicit Russian sentence to each composition-on rule: if the image is a screenshot and a dish ingredient/composition list is readable (delivery app, dish card), break items according to that list. Keep the existing rule that layered real-food photos still split by visible layers. Keep the existing rule that a homogeneous packaged product is one item and is not split using the ingredients list on the package label (D-04) — contrast screenshot-of-dish-card vs physical packaged label so the model does not start splitting yogurt into milk and sugar.

    Do not put that screenshot-split sentence in NO_FOOD_*, PACKAGED_FOOD_PROMPT_RULE, ANALYSIS_PROMPT, or edge-case bullets. applyAnalyzeFeaturesToPrompt does an exact replace of the full composition-on constant; the new sentence must be inside those two exported constants so composition-off removes it automatically. Do not modify src/food/analyzeFeatures.ts unless the exact-string swap breaks (it must not if you only change the exported rule strings).

    Do not weaken packaged OCR (KBJU / net weight) or itemCount rules (D-04). Do not touch client parse/UX (D-05).
  </action>
  <verify>
    <automated>npm test -- src/food/prompts.screenshot.test.ts</automated>
  </verify>
  <done>Composition-on vision prompts instruct splitting a screenshot dish composition into items (D-02); composition-off still yields a single item while screenshot-of-food remains not noFood; packaged-label non-split is unchanged (D-04); tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /v1/food/analyze | Untrusted image (including screenshots) and optional description; prompts are server-side only |
| Gateway → OpenRouter | Prompt + image leave the trust boundary; model output is untrusted XML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07v-01 | Spoofing | Vision prompt / screenshot of fake KBJU | medium | accept | User is logging their own meal; on-screen KBJU is the intended source when readable. Existing packaged OCR already trusted visible labels the same way. |
| T-07v-02 | Tampering | Prompt injection via screenshot UI chrome / overlay text | medium | accept | Pre-existing vision risk; this change does not add new user-controlled prompt fields. Name-only menus stay noFood (D-03) so a list of dish names does not invent a logged meal. |
| T-07v-03 | Information Disclosure | System prompt sent to OpenRouter | low | accept | No new PII in prompts; images already flowed to the provider. |
| T-07v-04 | Denial of Service | Longer prompts | low | accept | Added sentences are small relative to existing system prompts; no new retry/loop. |
| T-07v-05 | Elevation of Privilege | Prompt files | low | accept | No auth, quota, or client-injected model/messages changes (food.test.ts already rejects client model/messages). |
| T-07v-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan. |
</threat_model>

<verification>
From apps/ai-app:
- npm test -- src/food/prompts.screenshot.test.ts
- npm test
- npm run type-check

Confirm by inspection that apps/ai-food has no edits (D-05).

<human-check>
After deploy/restart of ai-app, analyze a Yandex Eda / Delivery Club dish-card screenshot: expect a normal nutrition XML (no noFood). If the card lists ingredients, expect multiple items when composition is on. A photo of a name-only restaurant menu should still be noFood.
</human-check>
</verification>

<success_criteria>
- Vision Gemini and legacy prompts no longer treat food screenshots as noFood (D-01)
- Screenshot dish composition is processed into items when composition is on (D-02)
- Name-only menus, empty packaging, people/landscapes, and foodless blurry photos remain noFood (D-03)
- Packaged homogeneous products remain one item; no extra prompt-domain changes (D-04)
- Client app untouched (D-05)
- src/food/prompts.screenshot.test.ts passes; full npm test and type-check pass
</success_criteria>

<output>
Create `.planning/quick/260818-07v-fix-food-analyze-prompt-so-screenshots-o/260818-07v-SUMMARY.md` when done
</output>
