---
phase: 260718-5fj-openrouter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/openai.ts
  - lib/errors.ts
  - api/v1/chat/completions.ts
  - api/v1/embeddings.ts
  - api/v1/models.ts
  - api/gateway.test.ts
  - .env.example
  - package.json
  - README.md
autonomous: true
requirements: [OPENROUTER-01]
user_setup:
  - service: openrouter
    why: "Upstream LLM provider (OpenAI-compatible API)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "https://openrouter.ai/keys"

must_haves:
  truths:
    - "getOpenAIClient() targets https://openrouter.ai/api/v1 with OPENROUTER_API_KEY"
    - "/v1/chat/completions, /v1/embeddings, /v1/models still return upstream JSON unchanged"
    - "npm test and npm run type-check both pass"
    - "No user-facing string or doc refers to OpenAI as the upstream provider"
  artifacts:
    - lib/openai.ts
    - .env.example
    - README.md
  key_links:
    - "getOpenAIClient reads OPENROUTER_API_KEY + baseURL and is called via runOpenAI in all three /v1 handlers"
    - "gateway.test.ts sets OPENROUTER_API_KEY so getOpenAIClient() does not throw"
---

<objective>
Adapt the gateway to use OpenRouter as the upstream provider instead of OpenAI, while keeping the OpenAI-compatible surface (endpoints, request/response shapes, error format) intact.

Purpose: Route all traffic through OpenRouter's OpenAI-compatible API so any model provider (openai/*, anthropic/*, etc.) is reachable through one gateway with a single key.
Output: OpenRouter-configured client, updated env/config/docs, provider-neutral error and log strings, tests green against the new env var.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/openai.ts
@lib/errors.ts
@lib/queue.ts
@api/v1/chat/completions.ts
@api/v1/embeddings.ts
@api/v1/models.ts
@api/gateway.test.ts
@.env.example
@package.json
@README.md

Naming decision (planner discretion, per task intent): keep existing identifiers
`getOpenAIClient`, `runOpenAI`, `mapOpenAIError`, and `OPENAI_CONCURRENCY`. Renaming
them would touch every /v1 handler plus both test files for zero behavior gain (noisy
churn). Only user-facing strings, env vars, config, and docs change. Keep the `openai`
npm package — OpenRouter is OpenAI-compatible; do NOT add a separate SDK.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the client to OpenRouter</name>
  <files>lib/openai.ts</files>
  <action>
In getOpenAIClient(), read the key from process.env.OPENROUTER_API_KEY (throw
'OPENROUTER_API_KEY is not configured' when missing). Construct the OpenAI client with
baseURL 'https://openrouter.ai/api/v1', the apiKey, and the existing timeout of 30_000.
Add an optional defaultHeaders object for OpenRouter app attribution: include
'HTTP-Referer' from process.env.OPENROUTER_HTTP_REFERER and 'X-Title' from
process.env.OPENROUTER_APP_TITLE, adding each header only when its env var is set (do not
send empty-string headers). Keep the import of createLimiter/OPENAI_CONCURRENCY, the
module-level limiter, and the runOpenAI wrapper exactly as-is (names unchanged). Update
the runOpenAI doc comment to say the pool guards OpenRouter upstream work.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>getOpenAIClient targets OpenRouter base URL, reads OPENROUTER_API_KEY, and attaches HTTP-Referer/X-Title only when their env vars are set; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Make error and log strings provider-neutral</name>
  <files>lib/errors.ts, api/v1/chat/completions.ts, api/v1/embeddings.ts, api/v1/models.ts</files>
  <action>
In lib/errors.ts, keep the mapOpenAIError function name and the OpenAI.* error-class
instanceof checks (those come from the openai package and are correct). Only update the
human-readable message strings that name the upstream provider: change the RateLimitError
message to reference OpenRouter, and change the BadRequestError message so it no longer
says "processed by OpenAI" (use OpenRouter or "upstream" instead). Leave status codes and
error codes unchanged. In each of the three /v1 handlers, update the console.error prefix
strings that read "OpenAI ..." so they name OpenRouter (e.g. the chat/embeddings/models
error log labels); no logic changes.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>No error message or log prefix presents OpenAI as the upstream provider; instanceof error-class checks and all status/code values are unchanged; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update env, package name, docs, and tests</name>
  <files>.env.example, package.json, README.md, api/gateway.test.ts</files>
  <action>
.env.example: replace OPENAI_API_KEY with OPENROUTER_API_KEY (placeholder value), and add
two commented optional lines for OPENROUTER_HTTP_REFERER and OPENROUTER_APP_TITLE
explaining they are for openrouter.ai app attribution. Keep the optional API_KEY gateway
secret line.
package.json: change "name" from "openai-gateway" to "openrouter-gateway". Keep the openai
dependency and all other fields untouched.
README.md: retitle to reflect an OpenRouter gateway; describe the upstream as OpenRouter;
in the env-vars table replace OPENAI_API_KEY with OPENROUTER_API_KEY (source: openrouter.ai
keys) and document the two optional attribution vars; update the quick-start note to set
OPENROUTER_API_KEY; change the RATE_LIMITED / upstream error-table wording from "OpenAI" to
"OpenRouter"; update curl/model examples to use provider-prefixed model IDs such as
openai/gpt-4o-mini for chat and keep an embeddings example valid for OpenRouter. Do not
claim streaming support (still unsupported).
api/gateway.test.ts: rename the OpenAI-key env references to OPENROUTER_API_KEY — the
ORIGINAL_OPENAI_KEY capture/restore, the beforeEach that sets process.env.*, and the
afterEach restore — so getOpenAIClient() finds a key. Keep vi.mock('openai') and the mock
implementation as-is (OpenRouter is called through the same SDK), and leave the test
model-id literals unchanged since the client is mocked.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>.env.example, package.json, and README.md describe OpenRouter (not OpenAI) as upstream; gateway.test.ts sets OPENROUTER_API_KEY; full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller app → gateway | Untrusted request bodies/headers cross here (validated by zod + optional API_KEY) |
| gateway → OpenRouter | Server-held OPENROUTER_API_KEY authenticates upstream; key never leaves server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5fj-01 | Information Disclosure | OPENROUTER_API_KEY in lib/openai.ts / .env | high | mitigate | Key read only from env server-side; .env stays gitignored; .env.example uses placeholder only |
| T-5fj-02 | Information Disclosure | mapOpenAIError messages | low | mitigate | Return generic provider-neutral messages; never echo raw upstream error bodies to callers |
| T-5fj-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies added (openai package retained); nothing to install |
</threat_model>

<verification>
- `npm run type-check` passes (no type regressions from client/config changes).
- `npm test` passes (all gateway + queue tests green with OPENROUTER_API_KEY).
- Grep confirms no remaining user-facing "OpenAI" upstream references in lib/errors.ts, the three /v1 handlers, .env.example, package.json name, and README (OpenAI.* SDK class references in code are expected and allowed).
</verification>

<success_criteria>
- Client targets https://openrouter.ai/api/v1 using OPENROUTER_API_KEY with optional attribution headers.
- All three /v1 endpoints keep working against the mocked SDK; response/error shapes unchanged.
- Concurrency pool (runOpenAI / OPENAI_CONCURRENCY, limit 5) preserved.
- Env, package name, and docs describe OpenRouter; tests pass.
</success_criteria>

<output>
Create `.planning/quick/260718-5fj-openrouter/260718-5fj-SUMMARY.md` when done.
</output>
