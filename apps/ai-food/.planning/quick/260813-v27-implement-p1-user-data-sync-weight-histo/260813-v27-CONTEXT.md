# CONTEXT — 260813-v27 P1 weight + favorites sync

## Locked decisions

1. Mirror P0 diary sync: `X-User-Token`, guests local-only, LWW `clientUpdatedAt`, soft-delete, no subscription gate.
2. Separate bulk endpoints: `POST /user/weights/sync` and `POST /user/favorites/sync` (same body shape as meals).
3. Immediate sync on add/remove favorite and weight add/update (goalKg changes sync with entries or as part of weight store payload).
4. Full sync after login + auth hydrate alongside diary.
5. Favorites: imageUri/imageUris stubs only — no blob upload.
6. Weight: entries[] + goalKg — include goalKg on User or on sync payload (prefer `goalKg` field on User or WeightMeta in sync response; simplest: store `goalKg` on User as optional Float, entries as WeightEntry rows).
7. signOut already wipes local — no change required beyond ensuring sync hooks no-op without token.

## Out of scope

Settings, micronutrientTargets, photo blobs, diary changes.
