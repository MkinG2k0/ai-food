---
phase: 260813-v27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ai-app/prisma/schema.prisma
  - apps/ai-app/prisma/migrations/20260813220000_weight_favorite_sync/migration.sql
  - apps/ai-app/src/lib/weightSync.ts
  - apps/ai-app/src/lib/weightSync.test.ts
  - apps/ai-app/src/lib/favoriteSync.ts
  - apps/ai-app/src/lib/favoriteSync.test.ts
  - apps/ai-app/src/routes/userWeights.ts
  - apps/ai-app/src/routes/userWeights.sync.test.ts
  - apps/ai-app/src/routes/userFavorites.ts
  - apps/ai-app/src/routes/userFavorites.sync.test.ts
  - apps/ai-app/src/app.ts
  - apps/ai-food/src/features/stats/model/useWeightStore.ts
  - apps/ai-food/src/features/favorites/model/favoriteFood.ts
  - apps/ai-food/src/features/favorites/model/useFavoritesStore.ts
  - apps/ai-food/src/features/weight-sync/api/syncWeightsApi.ts
  - apps/ai-food/src/features/weight-sync/model/weightSyncMerge.ts
  - apps/ai-food/src/features/weight-sync/model/weightSyncMerge.test.ts
  - apps/ai-food/src/features/weight-sync/model/syncWeightHistory.ts
  - apps/ai-food/src/features/weight-sync/index.ts
  - apps/ai-food/src/features/favorites-sync/api/syncFavoritesApi.ts
  - apps/ai-food/src/features/favorites-sync/model/favoriteSyncMerge.ts
  - apps/ai-food/src/features/favorites-sync/model/favoriteSyncMerge.test.ts
  - apps/ai-food/src/features/favorites-sync/model/syncFavorites.ts
  - apps/ai-food/src/features/favorites-sync/index.ts
  - apps/ai-food/src/app/DiarySyncOnAuthHydrate.tsx
  - apps/ai-food/src/pages/login/ui/LoginPage.tsx
  - apps/ai-food/src/features/onboarding/ui/steps/StepGender.tsx
  - apps/ai-food/docs/USER-DATA-SYNC.md
autonomous: true
requirements:
  - QUICK-260813-v27
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Authenticated user can POST /user/weights/sync and /user/favorites/sync with X-User-Token; guests stay local-only (D-01, D-02)"
    - "LWW by clientUpdatedAt + soft-delete tombstones reuse mealSync helpers; no new LWW invent (D-01)"
    - "Weight add/update and favorite add/remove push immediately when token present (D-03)"
    - "After login and auth hydrate, full weight + favorites sync runs alongside diary (D-04)"
    - "Favorites imageUri/imageUris are stubs only; goalKg stored on User and returned in weight sync (D-05, D-06)"
    - "Sync hooks no-op without token; signOut wipe already covers local weight/favorites (D-07)"
  artifacts:
    - path: apps/ai-app/prisma/schema.prisma
      provides: "WeightEntry + Favorite models; User.goalKg Float?"
    - path: apps/ai-app/src/lib/weightSync.ts
      provides: "applyWeightSync reusing mealSync LWW helpers"
    - path: apps/ai-app/src/lib/favoriteSync.ts
      provides: "applyFavoriteSync reusing mealSync LWW helpers"
    - path: apps/ai-app/src/routes/userWeights.ts
      provides: "POST /user/weights/sync"
    - path: apps/ai-app/src/routes/userFavorites.ts
      provides: "POST /user/favorites/sync"
    - path: apps/ai-food/src/features/weight-sync/model/syncWeightHistory.ts
      provides: "client push/pull weight + goalKg"
    - path: apps/ai-food/src/features/favorites-sync/model/syncFavorites.ts
      provides: "client push/pull favorites + pendingDeletes"
    - path: apps/ai-food/docs/USER-DATA-SYNC.md
      provides: "P1 / Phase C marked done with sync triggers"
  key_links:
    - from: X-User-Token JWT sub
      to: WeightEntry.userId / Favorite.userId / User.goalKg
      via: "ownership only from token; never body userId"
    - from: useWeightStore.entries + goalKg
      to: POST /user/weights/sync
      via: "syncWeightHistory + syncWeightsApi"
    - from: useFavoritesStore.favorites + pendingDeletes
      to: POST /user/favorites/sync
      via: "syncFavorites + syncFavoritesApi"
    - from: LoginPage / DiarySyncOnAuthHydrate
      to: full weight + favorites sync
      via: "alongside syncDiaryMeals (D-04)"
---

<objective>
Implement P1 weight history + favorites sync (server + client) mirroring P0 diary meal sync, per locked CONTEXT decisions D-01–D-07.

Purpose: Cross-device restore of weight entries/goalKg and favorites after login without reinventing LWW and without subscription gating.

Output: Prisma WeightEntry + Favorite + User.goalKg; POST /user/weights/sync and /user/favorites/sync; client weight-sync + favorites-sync features with immediate + login/hydrate wires; USER-DATA-SYNC.md Phase C / P1 done.
</objective>

<execution_context>
@C:/Users/mk/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/mk/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/ai-food/.planning/STATE.md
@apps/ai-food/.planning/quick/260813-v27-implement-p1-user-data-sync-weight-histo/260813-v27-CONTEXT.md
@apps/ai-food/docs/USER-DATA-SYNC.md
@apps/ai-app/prisma/schema.prisma
@apps/ai-app/src/lib/mealSync.ts
@apps/ai-app/src/lib/mealSync.test.ts
@apps/ai-app/src/routes/userMeals.ts
@apps/ai-app/src/routes/userMeals.sync.test.ts
@apps/ai-app/src/app.ts
@apps/ai-food/src/features/diary-sync/api/syncMealsApi.ts
@apps/ai-food/src/features/diary-sync/model/mealSyncMerge.ts
@apps/ai-food/src/features/diary-sync/model/syncDiaryMeals.ts
@apps/ai-food/src/app/DiarySyncOnAuthHydrate.tsx
@apps/ai-food/src/pages/login/ui/LoginPage.tsx
@apps/ai-food/src/features/stats/model/useWeightStore.ts
@apps/ai-food/src/features/favorites/model/useFavoritesStore.ts
@apps/ai-food/src/features/favorites/model/favoriteFood.ts
@apps/ai-food/src/features/auth/model/clearLocalUserDataOnSignOut.ts

## Locked decisions (CONTEXT — do not revisit)

- **D-01** Mirror P0 diary sync: `X-User-Token`, guests local-only, LWW `clientUpdatedAt`, soft-delete, no subscription gate.
- **D-02** Separate bulk endpoints: `POST /user/weights/sync` and `POST /user/favorites/sync` (same body shape as meals: `{ since?, upserts, deletes }`).
- **D-03** Immediate sync on add/remove favorite and weight add/update (goalKg changes sync with entries or as part of weight store payload).
- **D-04** Full sync after login + auth hydrate alongside diary.
- **D-05** Favorites: imageUri/imageUris stubs only — no blob upload.
- **D-06** Weight: entries[] + goalKg — store `goalKg` on User as optional Float; entries as WeightEntry rows; include goalKg on sync response.
- **D-07** signOut already wipes local weight/favorites — no wipe changes; sync hooks must no-op without token.

## Out of scope

Settings, micronutrientTargets, photo blobs, diary changes.

## Patterns to mirror (do not reinvent LWW)

- Import `lwwWins`, `shouldApplyUpsert`, `shouldApplyDelete` from `apps/ai-app/src/lib/mealSync.ts` (or extract to `syncLww.ts` and re-export from mealSync — discretionary; do not duplicate clock logic).
- Route auth: copy `userMeals.ts` (header `x-user-token`, `verifyUserToken`, ownership = `payload.sub`).
- Client: copy `diary-sync` slice layout (`api/` + `model/` + barrel); fire-and-forget `void sync…().catch(console.warn)`.
- Tests: vitest unit for schemas/LWW apply; route tests like `userMeals.sync.test.ts` (401/400/upsert/ignore-older/tombstone; subscription irrelevant).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Backend WeightEntry + Favorite sync (Prisma, LWW reuse, routes)</name>
  <files>apps/ai-app/prisma/schema.prisma, apps/ai-app/prisma/migrations/20260813220000_weight_favorite_sync/migration.sql, apps/ai-app/src/lib/weightSync.ts, apps/ai-app/src/lib/weightSync.test.ts, apps/ai-app/src/lib/favoriteSync.ts, apps/ai-app/src/lib/favoriteSync.test.ts, apps/ai-app/src/routes/userWeights.ts, apps/ai-app/src/routes/userWeights.sync.test.ts, apps/ai-app/src/routes/userFavorites.ts, apps/ai-app/src/routes/userFavorites.sync.test.ts, apps/ai-app/src/app.ts</files>
  <action>
  Per D-01, D-02, D-05, D-06 — thin end-to-end server path mirroring meal sync:

  1. Prisma schema: add optional `goalKg Float?` on User; models `WeightEntry` and `Favorite`; relations `weightEntries WeightEntry[]` and `favorites Favorite[]` on User.
     - WeightEntry: `id` String @id (client uuid), `userId` FK cascade, `date` String (YYYY-MM-DD), `kg` Float, `clientUpdatedAt` DateTime, `deletedAt` DateTime?, `createdAt`/`updatedAt`. Indexes: `@@index([userId, date])`, `@@index([userId, clientUpdatedAt])`.
     - Favorite: `id` String @id (client uuid), `userId` FK cascade, `sourceMealId` String, `name` String, `items` Json, `totalCalories` Float, `portions` Float?, `imageUri` String?, `imageUris` Json?, `healthiness` Float?, `confidence` Float?, `micronutrients` Json?, `createdAtClient` DateTime? (map client FavoriteFood.createdAt), `clientUpdatedAt` DateTime, `deletedAt` DateTime?, `createdAt`/`updatedAt`. Indexes: `@@index([userId, clientUpdatedAt])`.
     - Migration folder `prisma/migrations/20260813220000_weight_favorite_sync/` (or next free timestamp); run prisma generate.

  2. `weightSync.ts`: Zod body same shape as meals `{ since?, upserts, deletes: { id, clientUpdatedAt }[] }` plus optional `goalKg: number | null`. WeightPayload = `{ id, date, kg, clientUpdatedAt }`. Reuse `shouldApplyUpsert` / `shouldApplyDelete` from mealSync (D-01 — do not copy LWW). `applyWeightSync(prisma, userId, body)`: LWW upsert/soft-delete like `applyMealSync`; when `goalKg` key present on body, write `User.goalKg`; response `{ weights: WeightPayload[], tombstones: string[], goalKg: number | null }` (active rows only in weights; goalKg from User after merge). Scope all queries to userId from JWT.

  3. `favoriteSync.ts`: same body shape (no goalKg). FavoritePayload mirrors FavoriteFood fields + required `clientUpdatedAt`; imageUri/imageUris optional stubs (D-05). `applyFavoriteSync` same LWW/tombstone/ownership pattern as meals. Response `{ favorites: FavoritePayload[], tombstones: string[] }`.

  4. Routers: `userWeightsRouter` POST `/sync`, `userFavoritesRouter` POST `/sync` — clone userMeals auth/validation (401 missing/invalid token, 400 bad body, 200 merge). Mount in app.ts: `app.use('/user/weights', userWeightsRouter)` and `app.use('/user/favorites', userFavoritesRouter)`. Do not check subscriptionStatus (D-01).

  5. Tests: unit schemas + apply helpers (older upsert ignored, newer undeletes tombstone, delete clock wins); route tests for both endpoints covering 401, 400, upsert round-trip, older ignored, tombstone, and goalKg round-trip on weights.
  </action>
  <verify>
    <automated>cd apps/ai-app && pnpm exec vitest run src/lib/weightSync.test.ts src/lib/favoriteSync.test.ts src/routes/userWeights.sync.test.ts src/routes/userFavorites.sync.test.ts</automated>
  </verify>
  <done>
  Both sync endpoints work with X-User-Token, LWW reused from mealSync, WeightEntry/Favorite/User.goalKg migrated, tests green (D-01, D-02, D-05, D-06).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client weight-sync + favorites-sync + login/hydrate wires</name>
  <files>apps/ai-food/src/features/stats/model/useWeightStore.ts, apps/ai-food/src/features/favorites/model/favoriteFood.ts, apps/ai-food/src/features/favorites/model/useFavoritesStore.ts, apps/ai-food/src/features/weight-sync/api/syncWeightsApi.ts, apps/ai-food/src/features/weight-sync/model/weightSyncMerge.ts, apps/ai-food/src/features/weight-sync/model/weightSyncMerge.test.ts, apps/ai-food/src/features/weight-sync/model/syncWeightHistory.ts, apps/ai-food/src/features/weight-sync/index.ts, apps/ai-food/src/features/favorites-sync/api/syncFavoritesApi.ts, apps/ai-food/src/features/favorites-sync/model/favoriteSyncMerge.ts, apps/ai-food/src/features/favorites-sync/model/favoriteSyncMerge.test.ts, apps/ai-food/src/features/favorites-sync/model/syncFavorites.ts, apps/ai-food/src/features/favorites-sync/index.ts, apps/ai-food/src/app/DiarySyncOnAuthHydrate.tsx, apps/ai-food/src/pages/login/ui/LoginPage.tsx, apps/ai-food/src/features/onboarding/ui/steps/StepGender.tsx</files>
  <behavior>
    - weightSyncMerge / favoriteSyncMerge: newer clientUpdatedAt wins; tombstone ids remove local rows; older remote does not overwrite newer local.
    - syncWeightHistory / syncFavorites no-op when useAuthStore.userToken is null (D-01, D-07).
    - build payloads include clientUpdatedAt; favorites pass imageUri/imageUris as stubs; weight body includes goalKg from store.
    - After successful response, local stores match merged active sets; pendingDeletes cleared for synced ids.
  </behavior>
  <action>
  Per D-01, D-03, D-04, D-05, D-06, D-07:

  1. Stores: add optional `clientUpdatedAt?: string` on WeightEntry and FavoriteFood; bump to `new Date().toISOString()` on every mutating write (addEntry / setGoalKg / seedFromOnboarding; addFavorite / removeFavorite). Add `pendingDeletes: { id, clientUpdatedAt }[]` on favorites store (mirror diary); on removeFavorite, push tombstone clock before filtering out. Weight soft-delete is discretionary if UI has no delete — still support applying server tombstones on merge; if no client delete UI, pendingDeletes on weight may stay empty. Persist keys stay `ai-food-weight` / `ai-food-favorites`.

  2. Feature `features/weight-sync` (FSD barrel): `syncWeightsApi` POST `{gateway}/user/weights/sync` with X-User-Token (clone syncMealsApi). Pure `weightSyncMerge` + vitest (clone mealSyncMerge patterns). `syncWeightHistory({ mode: 'full' | 'upsert', entryIds?: string[] })`: no-op without token; body upserts + goalKg from store; apply `{ weights, tombstones, goalKg }` into useWeightStore (set entries + goalKg). Discretion for same-day collision after multi-device merge: keep LWW-by-id; optionally collapse duplicate `date` keeping newer clientUpdatedAt after merge.

  3. Feature `features/favorites-sync`: same pattern against `/user/favorites/sync`; modes `full` | `upsert` | `delete`; clear pendingDeletes after successful deletes/tombstones. imageUri/imageUris stubs only (D-05).

  4. Immediate sync (D-03): after weight mutations (addEntry, setGoalKg, seedFromOnboarding) call fire-and-forget full or upsert weight sync when token present; after addFavorite / removeFavorite / toggleFavorite paths call favorites sync (upsert or delete). Prefer thin wrappers `queueWeightSync` / `queueFavoritesSync` like queueDiarySync so store files stay readable.

  5. Full sync alongside diary (D-04): in LoginPage.handleLoginSuccess and StepGender login success (next to syncDiaryMeals), also call syncWeightHistory({ mode: 'full' }) and syncFavorites({ mode: 'full' }). Extend DiarySyncOnAuthHydrate: after auth + relevant persist hydration (weight + favorites + diary), run all three full syncs once per session when token present. Guests never hit APIs (D-01). Do not change clearLocalUserDataOnSignOut beyond ensuring pendingDeletes reset if added (D-07 — wipe already clears favorites/weight).
  </action>
  <verify>
    <automated>cd apps/ai-food && pnpm exec vitest run src/features/weight-sync src/features/favorites-sync</automated>
  </verify>
  <done>
  Authenticated weight/favorite mutations and login/hydrate trigger sync; merge helpers tested; guests never call sync endpoints (D-01, D-03, D-04, D-07).
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark USER-DATA-SYNC P1 / Phase C done</name>
  <files>apps/ai-food/docs/USER-DATA-SYNC.md</files>
  <action>
  Per D-01–D-06 docs alignment:

  1. Update inventory: Prisma now has WeightEntry + Favorite; User.goalKg; remove “Нет … WeightEntry / Favorite” contradiction.
  2. Mark Gaps table P1 as Done (weight history + favorites).
  3. Mark Phase C in phased rollout as Done with one-line outcome (bulk sync endpoints + client features).
  4. Add **Sync triggers (P1 weight + favorites)** section parallel to P0: immediate weight add/update + goalKg; immediate favorite add/remove; full sync after login/auth hydrate alongside diary; guests local-only; LWW clientUpdatedAt; soft-delete; image stubs; no subscription gate; endpoints POST /user/weights/sync and /user/favorites/sync.
  5. Narrow “Out of scope for P0” / remaining gaps so settings / micronutrientTargets / photo blobs remain future; do not claim diary changes.
  </action>
  <verify>
    <automated>cd apps/ai-app && pnpm exec vitest run src/lib/weightSync.test.ts src/lib/favoriteSync.test.ts src/routes/userWeights.sync.test.ts src/routes/userFavorites.sync.test.ts && cd ../ai-food && pnpm exec vitest run src/features/weight-sync src/features/favorites-sync</automated>
  </verify>
  <done>
  USER-DATA-SYNC.md shows P1/Phase C done with accurate inventory, triggers, and remaining P2/P3 gaps only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /user/weights/sync | Untrusted body; auth only via X-User-Token |
| Client → POST /user/favorites/sync | Untrusted body; auth only via X-User-Token |
| JWT sub → WeightEntry.userId / Favorite.userId / User.goalKg | Server must ignore any client-claimed owner |
| Guest device | No weight/favorites sync network path without token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v27-01 | Elevation | weights/favorites sync | high | mitigate | Bind all reads/writes to verifyUserToken payload.sub; never take userId from body |
| T-v27-02 | Tampering | LWW clientUpdatedAt | medium | mitigate | Reuse mealSync shouldApply*; ignore older clocks; return winner snapshot |
| T-v27-03 | Information disclosure | sync response rows | medium | mitigate | Query WeightEntry/Favorite only where userId = sub; goalKg only for that User |
| T-v27-04 | Denial of service | bulk upserts size | low | accept | Authenticated clients; favorites already capped at 50 client-side |
| T-v27-05 | Elevation | subscription bypass | low | accept | Intentional: sync remains free (D-01) |
| T-v27-SC | Tampering | npm installs | high | accept | No new packages — reuse express/zod/prisma/vitest/supertest |
</threat_model>

<verification>
- ai-app: weightSync + favoriteSync unit tests and both route sync tests pass
- ai-food: weight-sync + favorites-sync merge tests pass
- Docs: P1/Phase C marked done; inventory matches schema
- Manual smoke notes in SUMMARY: guest weight/favorite stays local; demo login restores both; add weight / toggle favorite while logged in hits APIs
</verification>

<success_criteria>
- Authenticated multi-device restore of weight entries + goalKg and favorites via the two bulk sync endpoints
- D-01–D-07 implemented; LWW not reinvented; guests and signOut behavior preserved
- USER-DATA-SYNC.md Phase C / P1 done; settings/photos still future
</success_criteria>

<output>
Create `apps/ai-food/.planning/quick/260813-v27-implement-p1-user-data-sync-weight-histo/260813-v27-SUMMARY.md` when done
</output>
