---
phase: quick-260804-3gx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - d:/Project/Main/ai-app/prisma/schema.prisma
  - d:/Project/Main/ai-app/src/lib/subscription.ts
  - d:/Project/Main/ai-app/src/lib/subscription.test.ts
  - d:/Project/Main/ai-app/src/lib/tbank.ts
  - d:/Project/Main/ai-app/src/lib/tbank.test.ts
  - d:/Project/Main/ai-app/src/lib/quota.ts
  - d:/Project/Main/ai-app/src/lib/quota.test.ts
  - d:/Project/Main/ai-app/src/middleware/quota.ts
  - d:/Project/Main/ai-app/src/middleware/quota.test.ts
  - d:/Project/Main/ai-app/src/routes/auth.ts
  - d:/Project/Main/ai-app/src/routes/billing.ts
  - d:/Project/Main/ai-app/src/routes/billing.test.ts
  - d:/Project/Main/ai-app/src/routes/usage.ts
  - d:/Project/Main/ai-app/src/app.ts
  - d:/Project/Main/ai-app/.env.example
  - src/features/billing/api/billingApi.ts
  - src/features/billing/api/billingApi.test.ts
  - src/features/billing/model/useBillingStatus.ts
  - src/features/billing/index.ts
  - src/pages/subscribe/ui/SubscribePage.tsx
  - src/pages/subscribe/index.ts
  - src/app/router.tsx
  - src/pages/settings/ui/SettingsPage.tsx
  - src/features/auth/api/fetchUsage.ts
  - src/features/auth/model/useAuthStore.ts
  - src/features/analyze-food/api/streamChatCompletions.ts
  - src/features/analyze-food/api/refineMealApi.ts
  - docs/AI-GATEWAY.md
  - docs/SUBSCRIPTION.md
  - .env.example
autonomous: true
requirements:
  - QUICK-260804-3gx
user_setup:
  - service: tbank-acquiring
    why: "Реальные платежи требуют TerminalKey/Password из ЛК T-Bank; локально достаточно TBANK_MOCK=true"
    env_vars:
      - name: TBANK_TERMINAL_KEY
        source: "T-Bank Acquiring ЛК → Терминал"
      - name: TBANK_PASSWORD
        source: "T-Bank Acquiring ЛК → Пароль терминала"
      - name: TBANK_API_URL
        source: "https://securepay.tinkoff.ru (prod) или sandbox URL из ЛК"
      - name: TBANK_MOCK
        source: "true для local/dev без ключей"
      - name: PUBLIC_APP_URL
        source: "Публичный origin ai-food (Success/Fail/Notification base)"
      - name: SUBSCRIPTION_PRICE_KOPECKS
        source: "199000 (= 1990.00 RUB)"
      - name: SUBSCRIPTION_DURATION_DAYS
        source: "365"

must_haves:
  truths:
    - "Гость с исчерпанной квотой получает 402; логин сам по себе не снимает лимит"
    - "Auth без active лицензии ходит через guest-квоту устройства; после лимита 402 ведёт на /subscribe"
    - "POST /billing/subscribe (JWT) создаёт Payment и возвращает paymentUrl; при TBANK_MOCK=true без реальных ключей"
    - "Notification CONFIRMED идемпотентно ставит subscriptionExpiresAt=now+365d и hasActiveSubscription=true"
    - "С active лицензией analyze/refine unlimited до expiry; diary/manual/barcode/stats бесплатны всегда"
    - "Страница /subscribe показывает 1990 ₽/год и CTA Оплатить; Settings показывает статус/срок/купить"
  artifacts:
    - d:/Project/Main/ai-app/prisma/schema.prisma
    - d:/Project/Main/ai-app/src/routes/billing.ts
    - d:/Project/Main/ai-app/src/lib/subscription.ts
    - d:/Project/Main/ai-app/src/middleware/quota.ts
    - src/features/billing/index.ts
    - src/pages/subscribe/ui/SubscribePage.tsx
    - docs/SUBSCRIPTION.md
  key_links:
    - "enforceChatQuota → hasActiveSubscription(user) → skip guest quota OR assertGuestQuotaOrThrow"
    - "POST /billing/subscribe → tbank Init (PayType=O) → Payment.pending → paymentUrl"
    - "POST /billing/tbank/notification Token-verify → Payment.confirmed → User.subscriptionExpiresAt"
    - "402 QUOTA_EXCEEDED + userToken → navigate /subscribe; без token → /login"
    - "SubscribePage CTA → billingApi.subscribe → window.location/Browser open paymentUrl"
---

<objective>
Годовая лицензия (one-time, без рекуррента) через T-Bank эквайринг в `ai-app`, затем paywall воронка free→login→subscribe в `ai-food`. Логин больше не даёт unlimited — только `hasActiveSubscription` (D-01–D-04).

Purpose: Заменить placeholder «login = unlimited until subscription» на реальную разовую покупку 1990 ₽ / 365 дней с привязкой к User.
Output: Prisma Payment + subscriptionExpiresAt, billing routes + quota gate, `/subscribe` + Settings soft paywall + docs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260804-3gx-one-time-t-bank-backend-ai-app-paywall-a/260804-3gx-CONTEXT.md
@.cursor/rules/ai-gateway.mdc
@docs/AI-GATEWAY.md
@d:/Project/Main/ai-app/prisma/schema.prisma
@d:/Project/Main/ai-app/src/middleware/quota.ts
@d:/Project/Main/ai-app/src/lib/quota.ts
@d:/Project/Main/ai-app/src/routes/auth.ts
@d:/Project/Main/ai-app/src/app.ts
@d:/Project/Main/ai-app/docs/superpowers/specs/2026-08-04-optional-auth-quota-design.md
@src/features/auth/index.ts
@src/features/auth/model/useAuthStore.ts
@src/features/auth/api/fetchUsage.ts
@src/pages/settings/ui/SettingsPage.tsx
@src/app/router.tsx

## Locked decisions (honor exactly)

- **D-01 Product:** Разовая лицензия на 365 дней, не рекуррент. Цена MVP `1990.00 RUB` = `199000` копеек через `SUBSCRIPTION_PRICE_KOPECKS` / `SUBSCRIPTION_DURATION_DAYS=365`.
- **D-02 Funnel:** Guest 50 AI → login Telegram → login НЕ unlimited → без лицензии guest-квота устройства → hard paywall → T-Bank → active до expiry → после expiry снова paywall.
- **D-03 Free forever:** дневник, ручной ввод, штрихкод, статистика, онбординг, настройки, просмотр приёмов — без лицензии.
- **D-04 Paid:** AI analyze / AI refine после free-квоты требуют `hasActiveSubscription`.
- **D-05 T-Bank:** `POST /v2/Init`, `PayType=O`, без `Recurrent`, `CustomerKey=user.id`, `OrderId=Payment.id`, NotificationURL `/billing/tbank/notification`, Success/Fail → фронт `/subscribe/success` и `/subscribe/fail` (или query на `/subscribe`). Активация только по notification `CONFIRMED` (PayType=O). Идемпотентность по `tbankPaymentId`.
- **D-06 Mock:** Без ключей терминала Init → `503` понятный; mock Init только при `TBANK_MOCK=true`.
- **D-07 Data/API:** `User.subscriptionExpiresAt`; `Payment` model; auth responses + `hasActiveSubscription`; `POST /billing/subscribe`, `POST /billing/tbank/notification`, `GET /billing/status`; опционально `POST /billing/sync` GetState.
- **D-08 Quota:** unlimited только при `hasActiveSubscription`; auth без лицензии = guest rules по `X-Device-Id`; `402 QUOTA_EXCEEDED` — клиент: guest→`/login`, auth→`/subscribe`.
- **D-09 Frontend:** `/subscribe` (цена, состав, CTA), Settings soft block, hard 402 routing, success poll `/billing/status` или `/auth/me`, RU UI в стиле приложения.
- **D-10 Order:** backend ai-app first → frontend ai-food → docs. Commits: ai-app в sibling repo; ai-food в этом.

## Discretion (allowed)

- Оставить enum `SubscriptionStatus`; active валиден только если `expiresAt > now`; опционально lazy-update status при проверке.
- 54-ФЗ Receipt / Cancel / refunds — вне MVP (поле `refunded` в enum статуса Payment на будущее ок).
- Цена через env — placeholder.

## Cross-repo

- Backend paths absolute under `d:/Project/Main/ai-app/…`
- Frontend paths relative under `ai-food` (`src/…`, `docs/…`)
- Run ai-app tests from `d:/Project/Main/ai-app` (`npm test`); ai-food via `pnpm exec vitest …`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ai-app — schema, subscription helpers, quota gate</name>
  <files>d:/Project/Main/ai-app/prisma/schema.prisma, d:/Project/Main/ai-app/src/lib/subscription.ts, d:/Project/Main/ai-app/src/lib/subscription.test.ts, d:/Project/Main/ai-app/src/lib/quota.ts, d:/Project/Main/ai-app/src/lib/quota.test.ts, d:/Project/Main/ai-app/src/middleware/quota.ts, d:/Project/Main/ai-app/src/middleware/quota.test.ts, d:/Project/Main/ai-app/src/routes/auth.ts, d:/Project/Main/ai-app/src/routes/usage.ts, d:/Project/Main/ai-app/.env.example</files>
  <behavior>
    - hasActiveSubscription true only when subscriptionStatus suggests active AND subscriptionExpiresAt is in the future (D-01, D-07)
    - expired active (expiresAt in past) treated as inactive; optional lazy status flip to none/canceled is ok
    - enforceChatQuota: with valid JWT, load user; if hasActiveSubscription → skip guest quota (shouldRecord optional analytics); else require X-Device-Id and assertGuestQuotaOrThrow (D-02, D-04, D-08)
    - getUsageSnapshot for auth without subscription returns guest remaining for device; with subscription remaining null / unlimited
    - /auth/telegram and /auth/me include subscriptionExpiresAt and hasActiveSubscription boolean (D-07)
  </behavior>
  <action>
    1. Per D-07: extend Prisma `User` with `subscriptionExpiresAt DateTime?` and `payments Payment[]`. Add `Payment` model: `id` cuid, `userId`, `amount` Int (kopecks), `status` enum `pending|confirmed|rejected|refunded`, `tbankPaymentId String?` unique optional, `tbankOrderId String` unique (= Payment.id or dedicated order id), `paidAt DateTime?`, timestamps. Run migrate/generate in ai-app.
    2. Per D-01: create `src/lib/subscription.ts` — `getSubscriptionDurationDays()`, `getSubscriptionPriceKopecks()` (default 199000), `hasActiveSubscription(user)`, `subscriptionPublicFields(user)` returning `{ subscriptionStatus, subscriptionExpiresAt, hasActiveSubscription }`, `activateYearLicense(prisma, userId, paidAt)` setting status active + expiresAt = paidAt+duration (idempotent caller responsibility).
    3. Per D-08: rewrite `enforceChatQuota` so JWT path no longer auto-skips quota. After verifyUserToken, load User; if `hasActiveSubscription` → set quota state and next(); else fall through to device guest quota (same assertGuestQuotaOrThrow). Update 402 message to mention subscribe when authenticated. Update `getUsageSnapshot` / usage route accordingly (auth without license ≠ remaining null).
    4. Wire auth login + `/me` responses with `subscriptionExpiresAt` + `hasActiveSubscription` via `subscriptionPublicFields` (D-07).
    5. Update `.env.example` in ai-app: `SUBSCRIPTION_PRICE_KOPECKS`, `SUBSCRIPTION_DURATION_DAYS`, `TBANK_*`, `TBANK_MOCK`, `PUBLIC_APP_URL`.
    6. Tests: subscription helper unit tests; quota middleware tests covering guest, auth-no-sub (quota), auth-with-sub (unlimited). Commits in ai-app repo only for these files.
  </action>
  <verify>
    <automated>cd d:/Project/Main/ai-app &amp;&amp; npm test -- --run src/lib/subscription.test.ts src/lib/quota.test.ts src/middleware/quota.test.ts</automated>
  </verify>
  <done>
    Schema migrated; hasActiveSubscription drives unlimited; auth without license uses guest device quota; auth responses expose expiry + boolean flag.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ai-app — T-Bank billing routes + mock Init</name>
  <files>d:/Project/Main/ai-app/src/lib/tbank.ts, d:/Project/Main/ai-app/src/lib/tbank.test.ts, d:/Project/Main/ai-app/src/routes/billing.ts, d:/Project/Main/ai-app/src/routes/billing.test.ts, d:/Project/Main/ai-app/src/app.ts</files>
  <behavior>
    - POST /billing/subscribe requires X-User-Token; creates Payment pending; returns { paymentUrl, paymentId }
    - TBANK_MOCK=true: Init skipped; paymentUrl points to PUBLIC_APP_URL/subscribe/success?mock=1&amp;paymentId=…; activating via mock notification or sync path still works in tests
    - Without TBANK_MOCK and missing TerminalKey/Password: subscribe returns 503 clear code
    - Real Init: Amount=SUBSCRIPTION_PRICE_KOPECKS, OrderId=Payment.id, PayType=O, CustomerKey=user.id, no Recurrent field, NotificationURL/SuccessURL/FailURL set (D-05, D-06)
    - Notification verifies Token (T-Bank SHA-256 password scheme); on CONFIRMED idempotently confirms Payment and activateYearLicense; duplicate callback does not extend expiry again
    - GET /billing/status (JWT) returns subscription snapshot + latest payment status
    - Optional POST /billing/sync (JWT) GetState for pending payment when notification lost
  </behavior>
  <action>
    1. Per D-05: implement `src/lib/tbank.ts` — `buildTbankToken(params, password)`, `initPayment(...)`, `getPaymentState(...)`, URL from `TBANK_API_URL` default `https://securepay.tinkoff.ru`. Never send Recurrent. Document Token algorithm in short comment referencing T-Bank docs.
    2. Per D-06/D-07: `src/routes/billing.ts` with JWT helper mirroring auth (X-User-Token). `POST /subscribe` creates Payment, calls Init or mock, stores `tbankPaymentId` from response. `POST /tbank/notification` public: verify Token, map Status; CONFIRMED → confirm payment + activate license idempotently (if already confirmed for same tbankPaymentId, no-op 200 OK). `GET /status`. Optional `POST /sync`.
    3. Mount `app.use('/billing', billingRouter)` in `src/app.ts` (CORS already allows needed headers).
    4. Integration/unit tests with mocked fetch for Init/GetState and Token verify; mock-mode subscribe without env keys. Commits in ai-app.
  </action>
  <verify>
    <automated>cd d:/Project/Main/ai-app &amp;&amp; npm test -- --run src/lib/tbank.test.ts src/routes/billing.test.ts</automated>
  </verify>
  <done>
    Billing API live: subscribe Init/mock, notification activates 365d license idempotently, status/sync available; no Recurrent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ai-food — paywall UI, 402 routing, docs</name>
  <files>src/features/billing/api/billingApi.ts, src/features/billing/api/billingApi.test.ts, src/features/billing/model/useBillingStatus.ts, src/features/billing/index.ts, src/pages/subscribe/ui/SubscribePage.tsx, src/pages/subscribe/index.ts, src/app/router.tsx, src/pages/settings/ui/SettingsPage.tsx, src/features/auth/api/fetchUsage.ts, src/features/auth/model/useAuthStore.ts, src/features/analyze-food/api/streamChatCompletions.ts, src/features/analyze-food/api/refineMealApi.ts, docs/AI-GATEWAY.md, docs/SUBSCRIPTION.md, .env.example</files>
  <behavior>
    - /subscribe shows 1990 ₽/год, what is included (AI analyze/refine unlimited year), free forever list (D-03), CTA Оплатить requiring auth (else redirect /login)
    - CTA calls POST /billing/subscribe with X-User-Token; opens paymentUrl (window.location.assign web; Capacitor Browser if already used in project else location)
    - /subscribe/success polls GET /billing/status or /auth/me until hasActiveSubscription or timeout; /subscribe/fail shows retry
    - Settings: soft block — license status, expiresAt date, Купить/Продлить → /subscribe; guest usage counter unchanged; logged-in without license shows remaining guest quota not «Безлимит»
    - On ApiError 402 QUOTA_EXCEEDED: if userToken → navigate /subscribe; else → /login (D-08, D-09)
    - Diary/manual/barcode/stats routes untouched (D-03)
  </behavior>
  <action>
    1. Per D-09/D-10: create FSD `features/billing` — `billingApi.ts` (subscribe, status, optional sync) against `VITE_AI_GATEWAY_URL` + `getQuotaHeaders`/userToken; barrel export. Co-located vitest with mocked fetch.
    2. Pages: `SubscribePage` + success/fail variants (separate routes or query). Register in `router.tsx` outside ProfileGuard like `/login`. Russian copy matching app tone — not a separate marketing landing.
    3. Settings: extend account/usage section with license status from `/auth/me` or billing status; remove/replace any «Безлимит solely because logged in» copy (align `fetchUsage` / `useUsage` with new backend semantics — remaining for auth-without-sub).
    4. Wire hard paywall: centralize 402 handling so analyze/refine UI paths navigate correctly (toast + navigate). Prefer small helper used where QUOTA_EXCEEDED is mapped today.
    5. Docs per D-10: `docs/SUBSCRIPTION.md` (funnel, env, mock, API table); update `docs/AI-GATEWAY.md` auth/quota/billing rows and note login≠unlimited. `.env.example` stubs if needed (no secrets).
    6. Commits in ai-food for frontend/docs only.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/features/billing src/features/auth/api/fetchUsage.test.ts &amp;&amp; pnpm type-check</automated>
  </verify>
  <done>
    Paywall UX complete: /subscribe + Settings + 402 routing; usage UI reflects license; docs updated; free features remain ungated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → ai-app `/billing/*` | JWT user identity; payment initiation |
| T-Bank → `/billing/tbank/notification` | Untrusted webhook; Token must bind Password |
| Client → `/v1/chat/completions` | API_KEY + optional user JWT + device id; quota gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3gx-01 | Spoofing | `/billing/tbank/notification` | high | mitigate | Verify T-Bank Token with Terminal Password; reject bad token with 403 |
| T-3gx-02 | Tampering | Payment activation | high | mitigate | Activate only on CONFIRMED; idempotent by tbankPaymentId; never trust SuccessURL alone |
| T-3gx-03 | Elevation | quota middleware | medium | mitigate | Unlimited only via hasActiveSubscription server-side; client flags not trusted |
| T-3gx-04 | Information | TBANK_PASSWORD / keys | high | mitigate | Server-only env; never VITE_*; TBANK_MOCK for local |
| T-3gx-05 | Fraud | Init amount | medium | mitigate | Amount always from SUBSCRIPTION_PRICE_KOPECKS server config, not client body |
| T-3gx-SC | Tampering | npm installs | low | accept | No new payment SDK packages expected; use fetch to T-Bank REST |
</threat_model>

<verification>
- ai-app: subscription + quota + billing tests green; TBANK_MOCK subscribe → notification/mock confirm → hasActiveSubscription
- ai-food: billing tests + type-check; manual smoke: guest 402→login, auth 402→subscribe, mock pay→unlimited AI
- Free paths (manual/barcode/stats/diary) never require license
</verification>

<success_criteria>
- D-01–D-10 implemented across both repos
- Login alone does not grant unlimited AI
- One-time T-Bank (or TBANK_MOCK) activates 365-day license idempotently
- Frontend paywall funnel and Settings soft upsell work in Russian UI
</success_criteria>

## Source Audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | One-time yearly T-Bank license + paywall free vs paid | 01 tasks 1–3 | COVERED |
| CONTEXT | D-01 | 1990 RUB / 365d one-time | 01-T1/T2 | COVERED |
| CONTEXT | D-02 | Funnel guest→login→paywall | 01-T1/T3 | COVERED |
| CONTEXT | D-03 | Free forever diary/manual/barcode/stats | 01-T3 | COVERED |
| CONTEXT | D-04 | Paid AI analyze/refine | 01-T1/T3 | COVERED |
| CONTEXT | D-05 | T-Bank Init PayType=O no Recurrent | 01-T2 | COVERED |
| CONTEXT | D-06 | TBANK_MOCK / 503 | 01-T2 | COVERED |
| CONTEXT | D-07 | Payment model + billing API | 01-T1/T2 | COVERED |
| CONTEXT | D-08 | Quota hasActiveSubscription only | 01-T1 | COVERED |
| CONTEXT | D-09 | /subscribe + Settings + 402 | 01-T3 | COVERED |
| CONTEXT | D-10 | Backend then frontend then docs | 01-T1→T3 | COVERED |
| CONTEXT | — | Deferred Receipt/refunds | — | EXCLUDED (discretion) |

<output>
Create `.planning/quick/260804-3gx-one-time-t-bank-backend-ai-app-paywall-a/260804-3gx-SUMMARY.md` when done
</output>
