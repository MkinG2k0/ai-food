---
phase: quick-260716-1zl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
  - apps/mobile/src/entities/meal/ui/MealCard.tsx
  - apps/mobile/src/entities/meal/index.ts
  - apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx
  - apps/mobile/src/features/edit-meal/ui/EditableFoodItemCard.tsx
  - apps/mobile/src/features/edit-meal/index.ts
  - apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx
  - apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx
  - apps/mobile/src/pages/food-item-edit/index.ts
  - apps/mobile/src/app/router.tsx
autonomous: true
requirements:
  - QUICK-1zl

must_haves:
  truths:
    - "На MealDetailPage список «Состав» — display-only: имя, portion badge, цветные Ккал/Б/Ж/У как на MealCard; нет number-инпутов в карточках состава."
    - "Свайп влево и кнопка корзины на карточке состава открывают DeleteItemConfirmSheet; confirm вызывает removeMealItem; клик (без drag) ведёт на /meal/:mealId/item/:itemId."
    - "Страница редактирования: имя (read-only), number-инпуты К/Б/У/Ж с integer sanitize, persist через updateMealItem; optional delete с тем же confirm-sheet; back в header."
    - "MealSummaryEditor на деталях блюда остаётся без изменений."
  artifacts:
    - apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
    - apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx
    - apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx
    - apps/mobile/src/app/router.tsx
  key_links:
    - "FoodItemDisplayCard click (DRAG_CLICK_GUARD) -> navigate /meal/:mealId/item/:itemId"
    - "FoodItemEditPage inputs -> updateMealItem(mealId, itemId, patch) + sanitizeNutrient"
    - "MealCard + FoodItemDisplayCard -> shared FoodMacrosBadges (emerald/blue/red/amber)"
    - "router ProfileGuard wraps /meal/:mealId/item/:itemId"
---

<objective>
Убрать inline-редактор состава на MealDetailPage: показывать ингредиенты display-карточками с цветными макро-бейджами (как MealCard); по клику открывать отдельную страницу редактирования КБЖУ ингредиента (D-01..D-03).

Purpose: Состав на деталях должен читаться как на главной; правка — отдельный экран без случайных изменений при свайпе/скролле.

Output: `FoodMacrosBadges`, `FoodItemDisplayCard` (swipe+trash+navigate), `FoodItemEditPage` + route; `EditableFoodItemCard` удалён/заменён; `MealSummaryEditor` не трогать.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx
@apps/mobile/src/features/edit-meal/ui/EditableFoodItemCard.tsx
@apps/mobile/src/features/edit-meal/ui/MealSummaryEditor.tsx
@apps/mobile/src/features/edit-meal/index.ts
@apps/mobile/src/entities/meal/ui/MealCard.tsx
@apps/mobile/src/entities/meal/index.ts
@apps/mobile/src/entities/meal/model/mealNutritionMath.ts
@apps/mobile/src/features/delete-meal/ui/SwipeableMealCard.tsx
@apps/mobile/src/app/router.tsx
@apps/mobile/src/shared/lib/formatters.ts
@apps/mobile/src/pages/meal-detail/index.ts

## Locked decisions (do not reopen)

**D-01 — Composition display:** На деталях заменить `EditableFoodItemCard` на display-карточку: name + portion Badge + цветные макро-бейджи как MealCard (emerald ккал, blue Б, red Ж, amber У) через `formatCalories` / `formatMacro`. Без inline number-инпутов в списке состава.

**D-02 — Click vs delete:** Route `/meal/:mealId/item/:itemId`. Свайп + trash confirm остаются на карточке списка деталей. Клик (не после drag) открывает edit page. Guard как `SwipeableMealCard`: `DRAG_CLICK_GUARD = 10`, `didDrag` ref, `onClickCapture`.

**D-03 — Edit page:** FSD `pages/food-item-edit`: header back, имя item display-only, number inputs calories/protein/carbs/fat (integers через `sanitizeNutrient` из mealNutritionMath — экспортировать из `@/entities/meal` если ещё не экспортирован). Persist `updateMealItem`. Optional delete + `DeleteItemConfirmSheet` / `useConfirmDeleteMealItem`; после успешного удаления — `navigate` назад на `/meal/:mealId`. Dish-level редактор на этой странице не нужен. `MealSummaryEditor` на MealDetail оставить as-is.

**D-04 — Shared badges:** Извлечь `FoodMacrosBadges` в `entities/meal/ui`; MealCard и display-карточка состава используют его. Props: `{ calories, protein, fat, carbs }` (числа).

**D-05 — FSD:** barrels (`pages/food-item-edit/index.ts`, обновить `features/edit-meal`, `entities/meal`); route в `router.tsx` с `ProfileGuard`. Не трогать capacitorStorage, ProfileGuard logic, onboarding и прочий dirty unrelated.

## Out of scope

Backend/AI, MealSummaryEditor redesign, dish-level edit на новой странице, новые npm-зависимости.
</context>

<tasks>

<task type="auto">
  <name>Task 1: FoodMacrosBadges + display composition card</name>
  <files>apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx, apps/mobile/src/entities/meal/ui/MealCard.tsx, apps/mobile/src/entities/meal/index.ts, apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx, apps/mobile/src/features/edit-meal/ui/EditableFoodItemCard.tsx, apps/mobile/src/features/edit-meal/index.ts, apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx</files>
  <action>
1. Создать `FoodMacrosBadges` (per D-04): четыре `Badge variant="secondary"` с классами из MealCard (emerald ккал / blue Б / red Ж / amber У), текст через `formatCalories(calories)` и `Б|Ж|У {formatMacro(...)}`. Named export; добавить в `entities/meal/index.ts`.

2. В `MealCard` заменить дублированный блок бейджей на `<FoodMacrosBadges calories={meal.totalCalories} protein={totals.protein} fat={totals.fat} carbs={totals.carbs} />`. Поведение клика MealCard не менять.

3. Создать `FoodItemDisplayCard` в `features/edit-meal/ui` (per D-01, D-02):
   - Props: `mealId`, `item: FoodItem`, `onRequestDelete: (itemId) => void`.
   - Layout: swipe-reveal trash (те же SWIPE_OFFSET/VELOCITY/REVEAL что EditableFoodItemCard) + motion drag; header row: name truncate, portion outline Badge, trash button (stopPropagation на pointerdown корзины).
   - Макросы: `FoodMacrosBadges` из `@/entities/meal` с полями item.
   - Навигация: `useNavigate` → `/meal/${mealId}/item/${item.id}` по click на карточку; копировать DRAG_CLICK_GUARD паттерн из `SwipeableMealCard` (`didDrag` ref, onDragStart/onDrag/onClickCapture) чтобы после свайпа navigate не срабатывал.
   - Корзина и свайп-confirm вызывают только `onRequestDelete` — confirm sheet остаётся на MealDetailPage.

4. Удалить файл `EditableFoodItemCard.tsx`. В `features/edit-meal/index.ts` экспортировать `FoodItemDisplayCard` (+ props type); убрать экспорт EditableFoodItemCard.

5. В `MealDetailPage`: импорт `FoodItemDisplayCard` вместо Editable*; map items → display card; MealSummaryEditor, meal/item delete sheets, meal delete button — без изменений семантики.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm exec tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>Состав на деталях — display + цветные бейджи; swipe/trash delete confirm сохранены; клик открывает edit route; EditableFoodItemCard больше не экспортируется; MealSummaryEditor нетронут.</done>
</task>

<task type="auto">
  <name>Task 2: FoodItemEditPage + route</name>
  <files>apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx, apps/mobile/src/pages/food-item-edit/index.ts, apps/mobile/src/app/router.tsx, apps/mobile/src/entities/meal/index.ts</files>
  <action>
1. Экспортировать `sanitizeNutrient` из `@/entities/meal` (re-export из `mealNutritionMath`) если ещё нет — edit page импортирует только через barrel (per D-03, D-05).

2. Создать `pages/food-item-edit`:
   - `ui/FoodItemEditPage.tsx` + `index.ts` barrel.
   - `useParams`: `mealId`, `itemId`. Найти meal/item в `useDiaryStore`; если meal отсутствует, analyzing, или item не найден — `navigate` replace на `/` или `/meal/${mealId}` (item missing → назад на детали meal если meal есть).
   - Header: back button (`navigate(-1)` или `/meal/${mealId}`), title вроде «Ингредиент».
   - Body: display имени item (не text input), portion как muted text или outline Badge; четыре number input (Ккал, Б, У, Ж) — native input + Tailwind как MealSummaryEditor (`inputClassName` тот же паттерн). On change: `updateMealItem(mealId, itemId, { [field]: sanitizeNutrient(Number(raw)) })` — отрицательные/NaN → 0, целые.
   - Optional: destructive «Удалить ингредиент» + `useConfirmDeleteMealItem` / `DeleteItemConfirmSheet` из `@/features/edit-meal`; после confirm success — toast + `navigate(`/meal/${mealId}`, { replace: true })`.

3. В `router.tsx` добавить route `{ path: '/meal/:mealId/item/:itemId', element: <ProfileGuard><FoodItemEditPage /></ProfileGuard> }` (per D-05). Импорт page через `@/pages/food-item-edit`. Существующий `/meal/:id` не ломать.

Не менять store API, ProfileGuard internals, capacitorStorage, onboarding.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm exec vitest run src/entities/meal/model/useDiaryStore.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Route `/meal/:mealId/item/:itemId` доступен под ProfileGuard; правка КБЖУ персистится через updateMealItem+sanitizeNutrient; удаление с confirm возвращает на детали meal; type-check чистый.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Zustand diary | Пользователь правит локальные числа КБЖУ; данные только на устройстве |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1zl-01 | Tampering | FoodItemEditPage inputs | low | mitigate | sanitizeNutrient: finite, >=0, Math.round; no raw NaN в store |
| T-1zl-02 | Elevation | router ProfileGuard | low | accept | Тот же ProfileGuard что /meal/:id; auth вне MVP |
| T-1zl-SC | Tampering | npm installs | low | accept | Новых пакетов нет |
</threat_model>

<verification>
- type-check проходит
- существующие useDiaryStore tests зелёные
- визуально: детали — бейджи как главная; клик → edit; swipe не открывает edit
</verification>

<success_criteria>
- Composition list display-only with MealCard-colored badges
- Click opens `/meal/:mealId/item/:itemId`; drag does not
- Edit page persists integer KBJU; optional delete with confirm
- MealSummaryEditor unchanged; FSD barrels + ProfileGuard route
</success_criteria>

<output>
Create `.planning/quick/260716-1zl-composition-display-edit-page/260716-1zl-SUMMARY.md` when done
</output>
