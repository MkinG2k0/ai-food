---
phase: 260813-1bg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/model/useSettingsStore.ts
  - src/features/settings/model/appDataBackup.ts
  - src/features/settings/model/appDataBackup.test.ts
  - src/features/settings/index.ts
  - src/pages/settings/ui/SettingsPage.tsx
  - src/shared/lib/dateUtils.ts
  - src/shared/lib/dateUtils.test.ts
  - src/shared/lib/computeDayKbju.ts
  - src/shared/lib/computeDayKbju.test.ts
  - src/shared/lib/index.ts
  - src/widgets/daily-header/ui/DayCellRings.tsx
  - src/widgets/daily-header/ui/DayCellRings.test.tsx
  - src/widgets/daily-header/ui/WeekStrip.tsx
  - src/widgets/daily-header/ui/WeekStrip.test.tsx
  - src/widgets/daily-header/ui/DailyHeader.tsx
autonomous: true
requirements:
  - QUICK-260813-1bg
must_haves:
  truths:
    - "В Settings есть выбор колец календаря К / КБ / КБЖУ; дефолт КБ (D-01)"
    - "На ячейках недели и месяца — concentric progress rings по выбранному режиму (D-02)"
    - "Цвета колец: kcal #10B981, protein #FB7185, fat #FBBF24, carbs #0EA5E9 (D-03)"
    - "Кольца считаются только по ready-приёмам vs цели профиля; дни без ready — без колец (D-04)"
    - "Свайп вниз / хэндл (6 точек) раскрывает месяц Пн–Вс; вверх сворачивает в неделю (D-05)"
    - "В свёрнутом режиме горизонтальный swipe недель сохраняется (D-06)"
    - "Выбранный день — сплошной круг + кольца снаружи (D-07)"
  artifacts:
    - path: src/features/settings/model/useSettingsStore.ts
      provides: "calendarRingMode persist + setter, default kcal_protein (КБ)"
    - path: src/shared/lib/computeDayKbju.ts
      provides: "ready-meal day totals vs targets for calendar rings"
    - path: src/widgets/daily-header/ui/DayCellRings.tsx
      provides: "concentric SVG arcs by mode + colors"
    - path: src/widgets/daily-header/ui/WeekStrip.tsx
      provides: "week rings + expand/collapse month grid + handle"
  key_links:
    - from: useSettingsStore.calendarRingMode
      to: DayCellRings mode prop
      via: "WeekStrip / DailyHeader selector"
    - from: useDiaryStore.meals + useProfileStore.targets
      to: DayCellRings progress
      via: "computeDayKbju / ready-only filter"
    - from: vertical drag / handle
      to: month expanded state
      via: "framer-motion AnimatePresence or height animation in WeekStrip"
---

<objective>
Кольца КБЖУ на ячейках календаря (неделя + полный месяц) и настройка плотности К/КБ/КБЖУ (дефолт КБ).

Purpose: На home daily-header пользователь видит прогресс дня вокруг цифры даты и может свайпом вниз раскрыть месяц — без отдельного экрана календаря.

Output: persist setting + Settings UI; DayCellRings; WeekStrip с кольцами; expand/collapse month grid; тесты.

**Claude's Discretion (locked in this plan):**
- Store field `calendarRingMode`: `'kcal' | 'kcal_protein' | 'full'` (UI labels К / КБ / КБЖУ), default `'kcal_protein'`.
- Remove the old `hasFood` under-dot (rings replace it).
- After selecting a day in month view → collapse back to week strip.
- Month nav in expanded mode: chevron prev/next month (horizontal month swipe optional, not required).
- Pure helpers in `shared/lib`; ring UI stays in `widgets/daily-header/ui` (FSD + barrel rules).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260813-1bg-calendar-kbju-rings-month-swipe/260813-1bg-CONTEXT.md
@src/widgets/daily-header/ui/WeekStrip.tsx
@src/widgets/daily-header/ui/DailyHeader.tsx
@src/widgets/daily-header/ui/NutritionSummaryCard.tsx
@src/widgets/daily-header/ui/WeekStrip.test.tsx
@src/features/settings/model/useSettingsStore.ts
@src/features/settings/model/appDataBackup.ts
@src/features/settings/model/appDataBackup.test.ts
@src/pages/settings/ui/SettingsPage.tsx
@src/shared/lib/computeTodayKbjuSnapshot.ts
@src/shared/lib/dateUtils.ts

## Locked decisions (CONTEXT)

- **D-01** Settings: К / КБ / КБЖУ; default КБ → 1 / 2 / 4 concentric rings.
- **D-02** Concentric rings on week + month day cells (Apple Activity style, thin arcs around day number).
- **D-03** Colors: kcal `#10B981`, protein `#FB7185`, fat `#FBBF24`, carbs `#0EA5E9`.
- **D-04** Progress = ready diary meals vs profile targets (same aggregation idea as DailyHeader / computeTodayKbjuSnapshot); rings only when the day has ready meals.
- **D-05** Handle (6 dots) under strip; vertical drag down expands month grid Mon–Sun; up collapses.
- **D-06** Horizontal week swipe remains in collapsed mode.
- **D-07** Selected day: solid filled circle + rings outside (per reference screens).

## Patterns to mirror

- Week horizontal carousel: existing WeekStrip framer-motion `drag="x"` (do not break).
- Ready-meal filter: `(m.status ?? 'ready') === 'ready'` as in DailyHeader.
- Fallback targets: same numbers as DailyHeader / computeTodayKbjuSnapshot (kcal 2000, protein 150, fat 70, carbs 250).
- Settings persist: zustand `persist` + `capacitorStorage` key `ai-food-settings`.
- Backup: extend `AppDataExportSettings` + export/import wiring in SettingsPage + `appDataBackup.test.ts`.
- Cross-slice imports only via barrels (`@/features/settings`, `@/shared/lib`, …).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calendarRingMode setting + backup + Settings UI</name>
  <files>src/features/settings/model/useSettingsStore.ts, src/features/settings/model/appDataBackup.ts, src/features/settings/model/appDataBackup.test.ts, src/features/settings/index.ts, src/pages/settings/ui/SettingsPage.tsx</files>
  <behavior>
    - Default persisted `calendarRingMode` is `kcal_protein` (КБ) for fresh installs / missing key (D-01).
    - Setter accepts only `kcal` | `kcal_protein` | `full`; invalid values normalize to `kcal_protein`.
    - Export JSON includes `calendarRingMode`; import restores it; missing field in old backups defaults to `kcal_protein` without failing parse.
    - SettingsPage shows a segmented control or radio group labeled «Кольца календаря» with options К / КБ / КБЖУ bound to the store.
  </behavior>
  <action>
    Per D-01: add `calendarRingMode` + `setCalendarRingMode` to `useSettingsStore` with default `kcal_protein`. Export a small `CalendarRingMode` type and `normalizeCalendarRingMode` from the settings model barrel if useful for backup/UI.

    Extend `AppDataExportSettings` with `calendarRingMode`. In `parseAppDataExport`, if the field is absent (legacy backup), default to `kcal_protein`; if present but invalid, normalize. Update `appDataBackup.test.ts` for round-trip + legacy-missing-field.

    On SettingsPage: add UI near other display/analysis prefs (not buried in AI-only section) — three options К / КБ / КБЖУ. Wire export snapshot and import `setState` to include the new field (same places that copy featureVitamins etc.).

    Do not change AI feature flags or prompts.
  </action>
  <verify>
    <automated>cd apps/ai-food; pnpm exec vitest run src/features/settings/model/appDataBackup.test.ts --reporter=dot</automated>
  </verify>
  <done>
    Setting persists with default КБ; Settings UI can switch К/КБ/КБЖУ; backup export/import includes or safely defaults the field.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DayCellRings + day KBJU helper + week strip rings</name>
  <files>src/shared/lib/computeDayKbju.ts, src/shared/lib/computeDayKbju.test.ts, src/shared/lib/dateUtils.ts, src/shared/lib/dateUtils.test.ts, src/shared/lib/index.ts, src/widgets/daily-header/ui/DayCellRings.tsx, src/widgets/daily-header/ui/DayCellRings.test.tsx, src/widgets/daily-header/ui/WeekStrip.tsx, src/widgets/daily-header/ui/WeekStrip.test.tsx, src/widgets/daily-header/ui/DailyHeader.tsx</files>
  <behavior>
    - `computeDayKbju` for a date with only analyzing/error meals → hasReadyMeals false / zero consumed; ready meals sum kcal + item macros vs targets/fallback (D-04).
    - DayCellRings mode kcal → 1 arc (#10B981); kcal_protein → 2 arcs (kcal outer or inner consistent, protein #FB7185); full → 4 arcs adding fat #FBBF24 and carbs #0EA5E9 (D-01, D-03).
    - Progress clamped 0..1; no rings rendered when hasReadyMeals is false (D-04).
    - Selected day: filled circle for the number + rings outside (D-07).
    - WeekStrip day buttons show DayCellRings instead of the old meal-dot span; horizontal week swipe still works (D-02, D-06).
  </behavior>
  <action>
    Add `computeDayKbju(meals, targets, date)` in shared/lib (mirror DailyHeader / computeTodayKbjuSnapshot ready filter and fallback goals). Return consumed macros, goals, and `hasReadyMeals`. Re-export from `shared/lib/index.ts`.

    Optionally add `getMonthGridDays(year, month)` in dateUtils now if Task 3 needs it in the same wave (Mon-start grid with leading/trailing padding days); cover with a small unit test. If kept for Task 3 only, create it there — prefer creating in this task to avoid WeekStrip churn.

    Create `DayCellRings` presentational SVG: concentric thin tracks + progress arcs; props for mode, progress ratios, selected, day number, future styling. Colors hard-coded per D-03 (not Tailwind primary). Keep stroke thin so week cells (~32px) stay readable.

    Wire WeekStrip: accept `targets` (or precomputed map) + `calendarRingMode` from DailyHeader; for each day call computeDayKbju; render DayCellRings; remove hasFood under-dot. Fix current hasMeals to ready-only if any leftover indicator remains. Pass profile targets + mode from DailyHeader via useProfileStore / useSettingsStore (widget may import features already used by DailyHeader — keep same layering as existing DailyHeader imports).

    Update WeekStrip.test.tsx: drop meal-dot assertion; assert rings/SVG present for ready meal day and absent for empty day; keep 21-button and click tests.
  </action>
  <verify>
    <automated>cd apps/ai-food; pnpm exec vitest run src/shared/lib/computeDayKbju.test.ts src/widgets/daily-header/ui/DayCellRings.test.tsx src/widgets/daily-header/ui/WeekStrip.test.tsx --reporter=dot</automated>
  </verify>
  <done>
    Week day cells show mode-aware concentric rings from ready meals vs targets; colors match D-03; empty days have no rings; selected styling per D-07; old meal-dot gone.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swipe-down / handle → full month calendar</name>
  <files>src/widgets/daily-header/ui/WeekStrip.tsx, src/widgets/daily-header/ui/WeekStrip.test.tsx, src/widgets/daily-header/ui/DailyHeader.tsx, src/shared/lib/dateUtils.ts, src/shared/lib/dateUtils.test.ts</files>
  <behavior>
    - Collapsed: week carousel + drag handle (six dots) visible under the strip (D-05).
    - Drag down past threshold or tap/drag handle expands animated month grid Mon–Sun with same DayCellRings (D-02, D-05).
    - Drag up / handle up collapses to week (D-05).
    - While collapsed, horizontal week swipe still changes weeks (D-06); while expanded, horizontal week drag must not steal vertical expand (axis lock / disable x-drag when expanded).
    - Day pick in month updates selectedDate and collapses to week (discretion).
    - Expanded header shows month label + prev/next month controls.
  </behavior>
  <action>
    Per D-05/D-06: extend WeekStrip (or thin wrapper kept in DailyHeader) with local `expanded` state. Add a 6-dot handle `button`/`div` with aria-label like «Календарь на месяц». Use framer-motion vertical drag and/or AnimateHeight for expand/collapse.

    Month grid: `getMonthGridDays` → rows of 7; weekday headers Пн…Вс; days outside current month muted but selectable if desired (prefer selectable). Reuse DayCellRings + computeDayKbju + calendarRingMode.

    Interaction rules: expanded → disable horizontal week drag; collapsed → keep existing x-carousel. Selecting a month day calls onDaySelect then sets expanded false. Month chevrons change displayed month independently of weekOffset; when collapsing, sync weekOffset/selectedDate so the week strip shows the selected week (DailyHeader already owns weekOffset — either lift month cursor to DailyHeader or derive weekOffset on select via existing onDaySelect/onWeekChange parent handlers).

    Tests: handle present; expand reveals month grid test id; day click in month calls onDaySelect; optional collapse assertion. Prefer user-event/fireEvent over fragile pixel drag if drag is hard to simulate — at minimum test expand via handle click/toggle if you also allow click-to-toggle on the handle (recommended for a11y alongside drag).
  </action>
  <verify>
    <automated>cd apps/ai-food; pnpm exec vitest run src/widgets/daily-header/ui/WeekStrip.test.tsx src/shared/lib/dateUtils.test.ts --reporter=dot</automated>
  </verify>
  <done>
    User can open full month from week strip (handle/swipe), see same KBJU rings, pick a day (collapses), and return to week swipe without regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage / Capacitor Preferences → settings UI | Persisted `calendarRingMode` is client-trusted display preference only |
| diary meals → calendar rings | Untrusted only insofar as local diary data shapes progress visuals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260813-1bg-01 | Tampering | calendarRingMode persist / backup import | low | mitigate | normalizeCalendarRingMode; reject unknown enums; legacy missing → default |
| T-260813-1bg-02 | Information disclosure | DayCellRings | low | accept | Progress is user’s own diary on-device; no new network |
| T-260813-1bg-03 | Denial of service | month grid render | low | mitigate | Memoize per-day compute for visible month only; no unbounded loops |
| T-260813-1bg-SC | Tampering | npm installs | low | accept | No new packages in this quick task |
</threat_model>

<verification>
- vitest: settings backup, computeDayKbju, DayCellRings, WeekStrip (week + month expand)
- Manual smoke (executor notes in SUMMARY): home week rings for a day with ready meals; Settings К→КБ→КБЖУ changes arc count; swipe/handle opens month; select day collapses
</verification>

<success_criteria>
- D-01…D-07 all observable on home + settings
- Default mode КБ without prior setting
- Ready-only aggregation; empty days ringless
- Week horizontal swipe intact when collapsed
- FSD barrels updated for any new public shared/lib exports; no deep cross-slice imports
</success_criteria>

<output>
Create `.planning/quick/260813-1bg-calendar-kbju-rings-month-swipe/260813-1bg-SUMMARY.md` when done
</output>
