---
phase: 260807-k0v-android-home-widget-weekly-calorie-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/kbju-widget/model/buildWeekKcalWidgetSnapshot.ts
  - src/features/kbju-widget/model/buildWeekKcalWidgetSnapshot.test.ts
  - src/features/kbju-widget/model/syncKbjuWidget.ts
  - src/features/kbju-widget/index.ts
  - src/app/DeepLinkHandler.tsx
  - src/shared/lib/parseAppDeepLink.ts
  - src/shared/lib/parseAppDeepLink.test.ts
  - src/shared/lib/index.ts
  - android/app/src/main/java/com/aifood/app/WeeklyCaloriesWidgetProvider.java
  - android/app/src/main/java/com/aifood/app/KbjuWidgetPlugin.java
  - android/app/src/main/res/layout/widget_weekly_calories.xml
  - android/app/src/main/res/layout/widget_preview_weekly_calories.xml
  - android/app/src/main/res/xml/widget_weekly_calories_info.xml
  - android/app/src/main/res/drawable/widget_preview_weekly_calories.png
  - android/app/src/main/res/values/strings.xml
  - android/app/src/main/res/values/colors.xml
  - android/app/src/main/AndroidManifest.xml
autonomous: true
requirements:
  - QUICK-260807-k0v
must_haves:
  truths:
    - "Widget picker lists a new ~4×2/4×3 weekly calorie chart widget; add-food and KBJU rings widgets still present and unchanged in behavior"
    - "Chart shows current calendar week Mon→Sun with stacked carbs/fat/protein bars (sky/amber/rose), optional goal line, Б/Ж/У legend, Russian day labels, range + average summary"
    - "After diary/profile changes (and app foreground), Preferences key ai-food-widget-week-kcal updates and both home widgets refresh via one KbjuWidget.refresh()"
    - "Tap on weekly chart widget opens app at /stats via aifood://stats deep link"
  artifacts:
    - path: src/features/kbju-widget/model/buildWeekKcalWidgetSnapshot.ts
      provides: Lean week series JSON from getWeeklyCalorieSeries(meals, 0) + goalKcal
    - path: src/features/kbju-widget/model/syncKbjuWidget.ts
      provides: Writes today KBJU + week-kcal snapshots then single native refresh
    - path: android/app/src/main/java/com/aifood/app/WeeklyCaloriesWidgetProvider.java
      provides: Canvas stacked-bar chart from CapacitorStorage snapshot + tap → aifood://stats
    - path: android/app/src/main/java/com/aifood/app/KbjuWidgetPlugin.java
      provides: refresh() updates KbjuRingsWidgetProvider AND WeeklyCaloriesWidgetProvider
  key_links:
    - from: buildWeekKcalWidgetSnapshot / Preferences.set
      to: SharedPreferences group CapacitorStorage key ai-food-widget-week-kcal
      via: "@capacitor/preferences (same group as jk6)"
    - from: KbjuWidget.refresh (JS)
      to: both AppWidgetProviders.onUpdate
      via: KbjuWidgetPlugin dual ComponentName notify
    - from: weekly widget root click
      to: MainActivity /stats
      via: PendingIntent VIEW aifood://stats + DeepLinkHandler
---

<objective>
Ship a new Android home-screen widget with a weekly KBJU calorie chart matching analytics `WeeklyCaloriesChart` (weekOffset 0), synced via lean Preferences snapshot — without breaking add-food or KBJU rings widgets (D-01–D-09).

Purpose: Glanceable weekly macros on the launcher; tap opens Stats.
Output: Week snapshot builder + extended kbju-widget sync + dual-refresh plugin + native chart provider + stats deep link + picker preview.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260807-jk6-android-home-widget-today-kbju-progress-/260807-jk6-SUMMARY.md
@src/features/stats/ui/WeeklyCaloriesChart.tsx
@src/features/stats/model/getWeeklyCalorieSeries.ts
@src/features/stats/model/chartScale.ts
@src/pages/stats/ui/StatsPage.tsx
@src/features/kbju-widget/model/syncKbjuWidget.ts
@src/features/kbju-widget/ui/KbjuWidgetSync.tsx
@src/features/kbju-widget/api/kbjuWidgetPlugin.ts
@src/app/AppShell.tsx
@src/app/DeepLinkHandler.tsx
@src/shared/lib/addFoodDeepLink.ts
@android/app/src/main/java/com/aifood/app/KbjuRingsWidgetProvider.java
@android/app/src/main/java/com/aifood/app/KbjuWidgetPlugin.java
@android/app/src/main/java/com/aifood/app/MainActivity.java
@android/app/src/main/AndroidManifest.xml
@android/app/src/main/res/xml/widget_kbju_rings_info.xml

## Locked decisions

- **D-01** New Android home widget: weekly calorie chart with KBJU stacked bars like `WeeklyCaloriesChart`
- **D-02** Tap opens app at `/stats` via `aifood://stats` deep link (wire JS DeepLinkHandler + manifest host)
- **D-03** Do not break existing add-food App Widgets or KBJU rings widget (providers, layouts, receivers, sync for rings stay working)
- **D-04** Current calendar week Mon→Sun (`weekOffset` 0); stack bottom→top carbs `#0EA5E9` / fat `#FBBF24` / protein `#FB7185`; bar height = Atwater macro kcal when macros &gt; 0 else `totalCalories` (same as `barHeightKcal`); optional horizontal goal kcal line when goal present
- **D-05** Compact legend Б/Ж/У + week date-range label + average kcal summary line
- **D-06** White card, ~4×2 or 4×3 cells; `previewLayout` + `previewImage` for picker; Russian day labels (Пн…Вс)
- **D-07** Preferences key `ai-food-widget-week-kcal`; lean snapshot `{ weekStart, goalKcal, days:[{ date, calories, protein, carbs, fat }] }` with YYYY-MM-DD date strings
- **D-08** Extend existing `syncKbjuWidget` / `KbjuWidgetSync` to also write week snapshot; expand `KbjuWidgetPlugin.refresh` to update both rings + weekly chart providers (no duplicate App listeners; no second plugin)
- **D-09** JS computes raw series + goal only; native reimplements scale lightly (`niceChartMax` / bar heights) from snapshot days — do not parse zustand diary blobs in Java

## Capacitor Preferences (same as jk6)

- Group/file: `CapacitorStorage`
- Keys: keep `ai-food-widget-kbju` (rings); add `ai-food-widget-week-kcal` (chart)
- `weekStart`: Monday of current week as local `YYYY-MM-DD`
- `goalKcal`: `targets?.kcal ?? 2000` (same FALLBACK as StatsPage)
- `days`: length 7 from `getWeeklyCalorieSeries(meals, 0)` with `date` stringified locally

## Architecture

```
getWeeklyCalorieSeries(meals, 0) + goal → Preferences ai-food-widget-week-kcal
computeTodayKbjuSnapshot → Preferences ai-food-widget-kbju
         ↘ KbjuWidget.refresh() → rings + weekly chart AppWidget updates
WeeklyCaloriesWidgetProvider → Canvas bitmap chart; tap aifood://stats → DeepLinkHandler → /stats
```

Reuse pattern from jk6: one sync mount in AppShell (`KbjuWidgetSync` already mounted — do not add a second listener component).

FSD: week snapshot builder lives in `features/kbju-widget/model` and may import `getWeeklyCalorieSeries` from `@/features/stats` (same pattern as settings→stats type import). Export new symbols via `features/kbju-widget/index.ts` barrel. Do not deep-import across slices.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Week snapshot + dual Preferences sync + stats deep link</name>
  <files>src/features/kbju-widget/model/buildWeekKcalWidgetSnapshot.ts, src/features/kbju-widget/model/buildWeekKcalWidgetSnapshot.test.ts, src/features/kbju-widget/model/syncKbjuWidget.ts, src/features/kbju-widget/index.ts, src/shared/lib/parseAppDeepLink.ts, src/shared/lib/parseAppDeepLink.test.ts, src/shared/lib/index.ts, src/app/DeepLinkHandler.tsx</files>
  <behavior>
    - buildWeekKcalWidgetSnapshot uses getWeeklyCalorieSeries(meals, 0, now); weekStart is Monday YYYY-MM-DD; seven days with local date strings and macro/calorie totals from series
    - goalKcal uses targets?.kcal with fallback 2000 (StatsPage parity)
    - only ready meals affect day totals (delegated to getWeeklyCalorieSeries)
    - parseAppDeepLink('aifood://stats') → { kind: 'route', path: '/stats' }; existing aifood://add/* results unchanged
  </behavior>
  <action>
    Per D-07/D-09: add `buildWeekKcalWidgetSnapshot(meals, targets, now = new Date())` in `features/kbju-widget/model`. Call `getWeeklyCalorieSeries(meals, 0, now)` from `@/features/stats`. Map each `DailyCaloriePoint` to `{ date: local YYYY-MM-DD, calories, protein, carbs, fat }` (reuse local date formatting style from `computeTodayKbjuSnapshot`). Set `weekStart` from series[0].date. Set `goalKcal` from `targets?.kcal ?? 2000`. Export type `WeekKcalWidgetSnapshot`. Unit-test: ready meal on a weekday appears in that day; analyzing meal ignored; empty week zeros; weekStart is Monday.

    Per D-08: extend `writeSnapshotAndRefresh` in `syncKbjuWidget.ts` to also `Preferences.set` key `ai-food-widget-week-kcal` with JSON.stringify(week snapshot) alongside existing `ai-food-widget-kbju`. Keep single debounce and single `KbjuWidget.refresh()` call. Export `WEEK_KCAL_WIDGET_PREFS_KEY` from barrel. Do not add a second sync component in AppShell.

    Per D-02: add `parseAppDeepLink` in `shared/lib` that returns stats route for scheme `aifood` / `com.aifood.app` with host `stats` (empty path OK), otherwise delegates to existing `parseAddFoodDeepLink`. Update `DeepLinkHandler` to use `parseAppDeepLink` instead of calling add-food parser alone. Re-export from `shared/lib/index.ts`. Do not change add-food action tokens or Android add-food PendingIntents.
  </action>
  <verify>
    <automated>cd apps/ai-food &amp;&amp; pnpm exec vitest run src/features/kbju-widget/model/buildWeekKcalWidgetSnapshot.test.ts src/shared/lib/parseAppDeepLink.test.ts</automated>
  </verify>
  <done>Week snapshot tests pass; sync writes both Preference keys then one refresh; DeepLinkHandler routes aifood://stats to /stats; add-food deep links still parse; barrels updated.</done>
</task>

<task type="auto">
  <name>Task 2: Native weekly chart widget + dual refresh + manifest</name>
  <files>android/app/src/main/java/com/aifood/app/WeeklyCaloriesWidgetProvider.java, android/app/src/main/java/com/aifood/app/KbjuWidgetPlugin.java, android/app/src/main/res/layout/widget_weekly_calories.xml, android/app/src/main/res/layout/widget_preview_weekly_calories.xml, android/app/src/main/res/xml/widget_weekly_calories_info.xml, android/app/src/main/res/drawable/widget_preview_weekly_calories.png, android/app/src/main/res/values/strings.xml, android/app/src/main/res/values/colors.xml, android/app/src/main/AndroidManifest.xml</files>
  <action>
    Per D-01/D-04/D-05/D-06: add white-card layout `widget_weekly_calories.xml` with TextViews for range label + average summary + compact Б/Ж/У legend, and one ImageView for the chart bitmap. Info XML: targetCellWidth 4, targetCellHeight 2 or 3, minWidth ≈250dp / minHeight ≈110–180dp (40dp×cells convention), `previewLayout` → `widget_preview_weekly_calories`, `previewImage` → `@drawable/widget_preview_weekly_calories` PNG (stacked bars mock), `updatePeriodMillis="0"`, `widgetCategory="home_screen"`. Picker string e.g. «Калории за неделю». Colors: carbs `#0EA5E9`, fat `#FBBF24`, protein `#FB7185`, goal line muted dark, card white, labels existing widget label color.

    `WeeklyCaloriesWidgetProvider`: read `CapacitorStorage` key `ai-food-widget-week-kcal`; parse JSON; for each day compute bar height like `barHeightKcal` (Atwater 4/4/9 when macro kcal total &gt; 0 else calories); compute chartMax mirroring `niceChartMax(dataMax, goalKcal)`; draw seven stacked bars (carbs bottom, fat, protein top) + optional goal horizontal line into a Bitmap; set Russian day labels under bars (Пн Вт Ср Чт Пт Сб Вс); set range label (Locale ru from weekStart..+6) and average summary («N ккал · среднее за день» or «Нет данных за период» using same averageLoggedCalories rule: mean of days with calories &gt; 0). Missing/malformed JSON → empty zeros chart, no crash.

    Per D-02: root click PendingIntent VIEW `aifood://stats` with FLAG_IMMUTABLE on M+, NEW_TASK|CLEAR_TOP to MainActivity. Add MainActivity intent-filter data `scheme=aifood` `host=stats` (keep existing `host=add` filter). Distinct requestCode from rings widget.

    Per D-03/D-08: extend `KbjuWidgetPlugin.refresh` to notify AppWidgetManager for BOTH `KbjuRingsWidgetProvider` and `WeeklyCaloriesWidgetProvider` ComponentNames (broadcast UPDATE with each id array). Do not remove rings refresh behavior. Register only the new weekly receiver in AndroidManifest — do not modify add-food or KBJU rings receiver entries beyond appending the new one. No new npm packages. MainActivity plugin registration already present — leave as-is.
  </action>
  <verify>
    <automated>cd apps/ai-food/android &amp;&amp; ./gradlew :app:compileDebugJavaWithJavac --quiet</automated>
  </verify>
  <done>Debug Java compiles; weekly widget receiver + info/preview present; plugin refresh updates both providers; rings + add-food receivers untouched; tap intent uses aifood://stats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS → Preferences | App-authored week + today snapshots in CapacitorStorage |
| Widget process → SharedPreferences | Same-app UID read of snapshot JSON |
| Widget → MainActivity | PendingIntent VIEW aifood://stats |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k0v-01 | Tampering | ai-food-widget-week-kcal JSON | low | accept | Same-UID SharedPreferences; malformed JSON → empty chart |
| T-k0v-02 | Information Disclosure | Weekly chart RemoteViews | low | accept | Shows user nutrition already visible in-app Stats; no tokens |
| T-k0v-03 | Elevation of Privilege | PendingIntent aifood://stats | medium | mitigate | FLAG_IMMUTABLE on M+; fixed URI host stats only; DeepLinkHandler ignores unknown hosts |
| T-k0v-04 | Denial of Service | Dual-widget refresh storms | low | mitigate | Keep existing 200ms debounce; refresh only own ComponentNames |
| T-k0v-05 | Spoofing | Deep link host stats | low | mitigate | Only handle aifood/com.aifood.app schemes; navigate fixed /stats path |
| T-k0v-SC | Tampering | npm/pip installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- Vitest: buildWeekKcalWidgetSnapshot + parseAppDeepLink
- Android compileDebugJavaWithJavac
- Manual device smoke (executor note in SUMMARY): pin weekly widget; log meals across week; background — chart updates; tap opens /stats; rings + add-food widgets still work
</verification>

<success_criteria>
- New ~4×2/4×3 weekly calorie chart widget in picker with previewLayout + previewImage
- Stacked Б/Ж/У bars Mon→Sun match WeeklyCaloriesChart stack order and colors; goal line when goal present
- Preferences ai-food-widget-week-kcal written by existing KbjuWidgetSync path; KbjuWidget.refresh updates rings + weekly chart
- Tap → /stats via aifood://stats; add-food and rings widgets unchanged
</success_criteria>

## Source Audit

| ID | Source | Item | Plan |
|----|--------|------|------|
| GOAL | user request | Weekly KBJU chart home widget, tap → stats | COVERED 01 |
| D-01 | CONTEXT | Chart like WeeklyCaloriesChart | COVERED Task 2 |
| D-02 | CONTEXT | Tap → /stats deep link | COVERED Task 1+2 |
| D-03 | CONTEXT | Keep add-food + rings | COVERED Task 2 |
| D-04 | CONTEXT | Mon–Sun stack + Atwater + goal | COVERED Task 2 |
| D-05 | CONTEXT | Legend + range + average | COVERED Task 2 |
| D-06 | CONTEXT | White 4×2/4×3 + previews + RU labels | COVERED Task 2 |
| D-07 | CONTEXT | Lean week Preferences snapshot | COVERED Task 1 |
| D-08 | CONTEXT | Extend sync + dual refresh | COVERED Task 1+2 |
| D-09 | CONTEXT | JS raw series; native scale | COVERED Task 1+2 |
| REQ | QUICK-260807-k0v | Quick deliverable | COVERED 01 |

<output>
Create `.planning/quick/260807-k0v-android-home-widget-weekly-calorie-chart/260807-k0v-SUMMARY.md` when done
</output>
