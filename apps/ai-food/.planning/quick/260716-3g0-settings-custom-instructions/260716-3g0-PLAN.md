---
phase: quick-260716-3g0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/features/settings/model/useSettingsStore.ts
  - apps/mobile/src/features/settings/model/useSettingsStore.test.ts
  - apps/mobile/src/features/settings/index.ts
  - apps/mobile/src/pages/settings/ui/SettingsPage.tsx
  - apps/mobile/src/pages/settings/index.ts
  - apps/mobile/src/app/router.tsx
  - apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
  - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
  - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts
  - apps/mobile/src/features/analyze-food/api/refineMealApi.ts
  - apps/mobile/src/features/analyze-food/api/refineMealApi.test.ts
  - apps/mobile/src/features/analyze-food/model/useAnalyzeFood.ts
  - apps/mobile/src/features/refine-meal/model/useRefineMeal.ts
  - apps/mobile/src/features/refine-meal/model/useRefineMeal.test.ts
autonomous: true
requirements:
  - QUICK-3g0

must_haves:
  truths:
    - "User can open Settings from Home (profile control) and edit a free-text custom instructions field."
    - "customInstructions persist across reload via Capacitor Preferences (same pattern as diary/profile)."
    - "Non-empty trimmed customInstructions are appended to the AI Gateway system prompt for both analyzeFoodApi and refineMealApi."
    - "Empty/whitespace-only instructions leave the base system prompt unchanged."
    - "apps/backend is not modified (client AI Gateway path per 260716-05y)."
  artifacts:
    - apps/mobile/src/features/settings/model/useSettingsStore.ts
    - apps/mobile/src/pages/settings/ui/SettingsPage.tsx
    - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
    - apps/mobile/src/features/analyze-food/api/refineMealApi.ts
  key_links:
    - "DailyHeader profile button → /settings → useSettingsStore.customInstructions"
    - "useAnalyzeFood → analyzeFoodApi(image, { customInstructions }) → system message append"
    - "useRefineMeal → refineMealApi({ ..., customInstructions }) → system message append"
---

<objective>
Add Settings with persisted custom instructions that are mixed into every client-side food AI prompt (analyze + refine).

Purpose: Let the user specify diet/preferences/units once; every food analysis and refine call respects those instructions.

Output: `features/settings` store + `/settings` page + nav entry; analyze/refine APIs append non-empty instructions to the system prompt; unit tests for store and prompt passthrough. Backend untouched.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/mobile/src/features/onboarding/model/useProfileStore.ts
@apps/mobile/src/entities/meal/model/useDiaryStore.ts
@apps/mobile/src/shared/lib/capacitorStorage.ts
@apps/mobile/src/app/router.tsx
@apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
@apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
@apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts
@apps/mobile/src/features/analyze-food/api/refineMealApi.ts
@apps/mobile/src/features/analyze-food/api/refineMealApi.test.ts
@apps/mobile/src/features/analyze-food/model/useAnalyzeFood.ts
@apps/mobile/src/features/refine-meal/model/useRefineMeal.ts
@apps/mobile/src/shared/ui/textarea.tsx

## Architecture facts (must follow)

- Food AI runs on the **client** via `VITE_AI_GATEWAY_URL` / `VITE_AI_GATEWAY_API_KEY` (`analyzeFoodApi`, `refineMealApi`). Do **not** change `apps/backend` or shared-types request shapes for this task (decision 260716-05y).
- Persist client settings with Zustand `persist` + `createJSONStorage(() => capacitorStorage)` — mirror `useProfileStore` / `useDiaryStore`. Persist key: `ai-food-settings`.
- Cross-slice imports only through FSD `index.ts` barrels.
- Reuse existing `Textarea` from `@/shared/ui`. Russian UI copy.
- DailyHeader already has a profile icon button (`aria-label="Профиль"`) with no navigation — wire it to `/settings`.
- Do not store API responses in Zustand; settings are client prefs only.

## Out of scope

Backend OpenAI route changes; statistics; meal refine UI redesign; onboarding/profile editing; new npm dependencies; auth.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings store, page, route, and nav</name>
  <files>apps/mobile/src/features/settings/model/useSettingsStore.ts, apps/mobile/src/features/settings/model/useSettingsStore.test.ts, apps/mobile/src/features/settings/index.ts, apps/mobile/src/pages/settings/ui/SettingsPage.tsx, apps/mobile/src/pages/settings/index.ts, apps/mobile/src/app/router.tsx, apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx</files>
  <behavior>
    - useSettingsStore defaults customInstructions to empty string
    - setCustomInstructions updates state; persist rehydrates the same string under storage key ai-food-settings
    - SettingsPage shows a labeled Textarea bound to customInstructions; changes persist via setter (auto-save on change or blur — choose one; no separate Save button required)
    - Route /settings exists behind ProfileGuard; DailyHeader profile button navigates to /settings
  </behavior>
  <action>
    1. Create `features/settings/model/useSettingsStore.ts` with interface `{ customInstructions: string; setCustomInstructions: (value: string) => void }`. Use Zustand `persist` + `createJSONStorage(() => capacitorStorage)` (same as `useProfileStore`). Storage name: `ai-food-settings`. Default `customInstructions: ''`. Cap input length at 2000 characters inside the setter (truncate or reject longer — truncate quietly). Export via `features/settings/index.ts`.

    2. Add co-located `useSettingsStore.test.ts`: default empty; setCustomInstructions updates; length cap at 2000; follow existing profile store test reset/rehydrate patterns where practical.

    3. Create `pages/settings/ui/SettingsPage.tsx` (named export `SettingsPage`): simple full-page layout with back control to `/` (Link or navigate), title «Настройки», section for custom instructions — label «Кастомные инструкции», short helper (e.g. diet/preferences/units examples), `Textarea` from `@/shared/ui` bound to store. Auto-persist via `setCustomInstructions` on change (or blur). Barrel `pages/settings/index.ts`.

    4. Register `{ path: '/settings', element: <ProfileGuard><SettingsPage /></ProfileGuard> }` in `apps/mobile/src/app/router.tsx`.

    5. In `DailyHeader`, make the existing profile button navigate to `/settings` (useNavigate or Link). Keep `aria-label="Профиль"` or update to «Настройки» if clearer — prefer «Настройки» to match destination.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/settings/model/useSettingsStore.test.ts</automated>
  </verify>
  <done>
    Settings store persists customInstructions; /settings page editable; Home profile control opens Settings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append custom instructions to analyze and refine prompts</name>
  <files>apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts, apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts, apps/mobile/src/features/analyze-food/api/refineMealApi.ts, apps/mobile/src/features/analyze-food/api/refineMealApi.test.ts, apps/mobile/src/features/analyze-food/model/useAnalyzeFood.ts, apps/mobile/src/features/refine-meal/model/useRefineMeal.ts, apps/mobile/src/features/refine-meal/model/useRefineMeal.test.ts</files>
  <behavior>
    - analyzeFoodApi(image, options?) with non-empty trimmed customInstructions appends a clearly labeled block to the system message content sent to the gateway
    - Empty or whitespace-only customInstructions leave SYSTEM_PROMPT content identical to today
    - refineMealApi accepts optional customInstructions and applies the same append rules to its system message
    - useAnalyzeFood reads useSettingsStore.customInstructions, passes it to analyzeFoodApi, and includes it in queryKey so cache separates by instructions text
    - useRefineMeal reads store and passes customInstructions into refineMealApi
  </behavior>
  <action>
    1. Extend `analyzeFoodApi` signature to `analyzeFoodApi(image: File, options?: { customInstructions?: string })`. Build system content from existing SYSTEM_PROMPT; when `options?.customInstructions?.trim()` is non-empty, append a second block such as a heading plus the trimmed text instructing the model to follow user preferences for diet, units, or style. Do not put instructions into Zustand from API layer — callers pass the string.

    2. Update `useAnalyzeFood` to select `customInstructions` from `useSettingsStore` (import via `@/features/settings`). Pass into `analyzeFoodApi`. Include the value in `queryKey` (e.g. after file identity fields).

    3. Extend `RefineMealInput` with optional `customInstructions?: string` and append the same way in `refineMealApi` system message.

    4. Update `useRefineMeal` to pass `useSettingsStore.getState().customInstructions` (or selector) into `refineMealApi`. Adjust `useRefineMeal.test.ts` mocks/expectations so the call includes `customInstructions` (empty string default is fine).

    5. Tests: in `analyzeFoodApi.test.ts`, assert gateway POST system content contains the custom text when provided and does not when omitted/blank; keep existing COMPOSITION/FOOD_NAME prompt tests green. Mirror at least one positive + empty case in `refineMealApi.test.ts`.

    6. Do not modify `apps/backend` or `packages/shared-types` for this wiring.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/analyze-food/api/analyzeFoodApi.test.ts src/features/analyze-food/api/refineMealApi.test.ts src/features/refine-meal/model/useRefineMeal.test.ts</automated>
  </verify>
  <done>
    Non-empty saved instructions appear in analyze and refine system prompts; empty leaves prompts unchanged; hooks read from settings store.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Settings store | Free-text instructions persisted locally on device |
| Settings → AI Gateway | Custom text appended into system prompt sent with API key from client env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3g0-01 | Tampering / Elevation | customInstructions in system prompt | low | accept | User-controlled local prefs only; length capped at 2000; affects only own AI calls |
| T-3g0-02 | Information Disclosure | VITE_AI_GATEWAY_API_KEY client usage | medium | accept | Pre-existing client gateway design (260716-05y); this task does not expand key exposure |
| T-3g0-03 | Denial of Service | Oversized instruction payload | low | mitigate | Cap customInstructions at 2000 chars in store setter and Textarea maxLength |
</threat_model>

<verification>
- Settings store unit tests pass
- analyzeFoodApi + refineMealApi tests prove prompt append / no-append
- useRefineMeal still green with customInstructions field
- Manual smoke (executor or later UAT): set instruction → analyze food → confirm behavior; clear field → prompts unchanged
</verification>

<success_criteria>
1. `/settings` reachable from Home profile control with editable custom instructions textarea
2. Value survives reload (Capacitor Preferences / persist)
3. Every analyze and refine AI call includes trimmed non-empty instructions in the system prompt
4. Backend unchanged
</success_criteria>

<output>
Create `.planning/quick/260716-3g0-settings-custom-instructions/260716-3g0-SUMMARY.md` when done
</output>
