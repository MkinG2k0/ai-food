---
phase: quick-260716-16d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/index.ts
  - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
  - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts
  - apps/mobile/src/features/save-meal/model/useSaveMeal.ts
  - apps/mobile/src/features/save-meal/model/useSaveMeal.test.ts
  - apps/mobile/src/entities/meal/model/mealDisplayName.ts
  - apps/mobile/src/entities/meal/model/mealDisplayName.test.ts
  - apps/mobile/src/entities/meal/index.ts
  - apps/mobile/src/entities/meal/ui/MealCard.tsx
  - apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx
autonomous: true
requirements:
  - QUICK-16d
must_haves:
  truths:
    - "После успешного анализа в дневнике у Meal есть name = NutritionResult.foodName (краткое название блюда)."
    - "MealCard показывает одно название блюда + время, а не перечень всех items[].name как заголовок."
    - "MealDetailPage показывает название блюда отдельно; секция «Состав» по-прежнему рендерит meal.items."
    - "Старые persisted meals без name отображают fallback (join items или первый item), без падения UI."
    - "SYSTEM_PROMPT жёстко разделяет foodName (название блюда) и items[].name (компоненты состава)."
  artifacts:
    - packages/shared-types/src/index.ts
    - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
    - apps/mobile/src/features/save-meal/model/useSaveMeal.ts
    - apps/mobile/src/entities/meal/model/mealDisplayName.ts
    - apps/mobile/src/entities/meal/ui/MealCard.tsx
    - apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx
  key_links:
    - "SYSTEM_PROMPT foodName → AnalyzeFoodResponse.result.foodName → useSaveMeal updateMeal name → Meal.name"
    - "mealDisplayName(meal) → MealCard title / MealDetail header; items remain composition-only"
---

<objective>
Сохранить и показать отдельное название блюда (`Meal.name` ← `NutritionResult.foodName`), а список ингредиентов оставить только в «Состав»; ужесточить client SYSTEM_PROMPT, чтобы модель не писала перечень состава в `foodName`.

Purpose: После 260716-0vb карточка склеивает `items[].name` в заголовок; `foodName` от AI есть, но не персистится на `Meal`.

Output: поле `name` на Meal, persist в useSaveMeal, UI fallback helper, обновлённый SYSTEM_PROMPT, тесты. Backend не трогать.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260716-0vb-split-meal-composition-kbzhu/260716-0vb-SUMMARY.md
@packages/shared-types/src/index.ts
@apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
@apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts
@apps/mobile/src/features/save-meal/model/useSaveMeal.ts
@apps/mobile/src/features/save-meal/model/useSaveMeal.test.ts
@apps/mobile/src/entities/meal/ui/MealCard.tsx
@apps/mobile/src/entities/meal/index.ts
@apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx

## Locked constraints

- Не трогать `apps/backend` — анализ только через client AI Gateway (`VITE_AI_GATEWAY_*`).
- FSD: импорты через barrels; API-ответы не класть в Zustand.
- Не включать несвязанные локальные изменения (capacitorStorage, profile, ProfileGuard), если они не нужны для type-check этого diff.
- Секция «Состав» на деталке остаётся списком `meal.items` (не убирать per-item КБЖУ).

## Target Meal field

В `packages/shared-types/src/index.ts` на `Meal` добавить optional field (legacy persist без имени):

`name?: string` — краткое название блюда; при записи после анализа всегда `result.foodName`.

## Display fallback (discretion locked)

Хелпер `mealDisplayName(meal: Meal): string`:
1. Если `meal.name` непустой после trim — вернуть его.
2. Иначе если `meal.items.length > 0` — `items.map(i => i.name).join(', ')` (или первый item, если join пустой).
3. Иначе — `'Без названия'`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Meal.name + persist foodName + SYSTEM_PROMPT</name>
  <files>packages/shared-types/src/index.ts, apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts, apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts, apps/mobile/src/features/save-meal/model/useSaveMeal.ts, apps/mobile/src/features/save-meal/model/useSaveMeal.test.ts</files>
  <behavior>
    - Multi-item analyze success: updateMeal sets meal.name to result.foodName (e.g. «Бургер с картошкой») while items stay separate components
    - Empty items fallback: meal.name equals result.foodName and single FoodItem.name still uses foodName
    - No-image manual meal: meal.name from trimmed description (or «Без названия» when empty)
    - analyzeFoodApi.test: SYSTEM_PROMPT still produces valid NutritionResult; add assertion that the prompt text distinguishes dish-level foodName from composition items (read source string via existing module — if not exported, assert by grepping file content with fs read in test OR export a PROMPT_RULES constant; prefer reading analyzeFoodApi.ts in test is OK only if already patterned — otherwise add expect on a small exported `FOOD_NAME_PROMPT_RULE` substring constant used inside SYSTEM_PROMPT)
  </behavior>
  <action>
1) Shared types: add optional `name?: string` on `Meal` per Target Meal field. Do not change FoodItem / NutritionResult shapes.

2) SYSTEM_PROMPT in `analyzeFoodApi.ts` (per user request): tighten foodName vs items rules in Russian/English mix already used —
   - foodName = краткое название всего блюда/приёма (пример «Свежий овощной салат»), never a comma-separated ingredient list.
   - items[].name = отдельные видимые компоненты состава (Помидоры, Огурцы, …).
   - Keep existing totals-vs-items sum rule and Russian text requirement.
   - Do not soften: explicitly forbid putting the full composition listing into foodName.

3) `useSaveMeal`:
   - When image analyze succeeds with items.length &gt; 0: pass `name: result.foodName` in updateMeal together with items/totalCalories/status.
   - Empty-items branch: also set `name: result.foodName`.
   - No-image ready meal: set `name` to trimmed description or «Без названия».
   - Pending analyzing meal: optional — set `name` from description if present (or leave undefined); must not block READY path.

4) Tests (RED then GREEN, atomic commits):
   - Extend useSaveMeal.test: multi-item + empty-items cases assert `meal.name === foodName`; no-image case asserts name from description.
   - Extend analyzeFoodApi.test for prompt/rule coverage without breaking existing gateway mocks.

Do not modify apps/backend.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/save-meal/model/useSaveMeal.test.ts src/features/analyze-food/api/analyzeFoodApi.test.ts</automated>
  </verify>
  <done>
    Meal has optional name; successful saves persist result.foodName; SYSTEM_PROMPT forbids ingredient-list foodName; tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mealDisplayName + MealCard + MealDetail title</name>
  <files>apps/mobile/src/entities/meal/model/mealDisplayName.ts, apps/mobile/src/entities/meal/model/mealDisplayName.test.ts, apps/mobile/src/entities/meal/index.ts, apps/mobile/src/entities/meal/ui/MealCard.tsx, apps/mobile/src/pages/meal-detail/ui/MealDetailPage.tsx</files>
  <behavior>
    - mealDisplayName({ name: 'Салат', items: [...] }) → 'Салат'
    - mealDisplayName({ items: [{ name: 'Помидоры' }, { name: 'Огурцы' }] }) without name → joined items
    - mealDisplayName({ items: [] }) without name → 'Без названия'
  </behavior>
  <action>
1) Create `mealDisplayName` per Display fallback; unit test co-located; export from `entities/meal/index.ts` barrel.

2) MealCard ready state:
   - Title row: single line with `mealDisplayName(meal)` + time (muted), not one row per item.
   - aria-label uses the same display name + time.
   - Keep badges (kcal / БЖУ totals) as today.
   - Do not map all items into the title block.

3) MealDetailPage:
   - Show dish name via mealDisplayName (CardTitle and/or prominent heading under header — executor choice: prefer CardTitle = dish name, keep date/time as subtitle text nearby).
   - Image alt = mealDisplayName(meal).
   - «Состав» section unchanged: map meal.items with per-item КБЖУ.

Do not touch delete-meal / SwipeableMealCard except through MealCard behavior.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/entities/meal/model/mealDisplayName.test.ts</automated>
  </verify>
  <done>
    Card and detail show dish name separately from composition; legacy meals without name still render via fallback; mealDisplayName tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI Gateway → client | Untrusted NutritionResult JSON crosses into diary persist |
| localStorage → UI | Legacy meals may lack name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16d-01 | Tampering | analyzeFoodApi isNutritionResult | medium | accept | Existing schema validation already rejects malformed results; name is display-only string |
| T-16d-02 | Information Disclosure | Meal.name / foodName in localStorage | low | accept | Local single-user MVP; same as existing diary content |
| T-16d-03 | Elevation of Privilege | N/A | low | accept | No auth surface in this change |
| T-16d-SC | Tampering | npm installs | high | accept | No new packages in this quick task |
</threat_model>

<verification>
- `pnpm --filter @ai-food/mobile exec vitest run src/features/save-meal/model/useSaveMeal.test.ts src/features/analyze-food/api/analyzeFoodApi.test.ts src/entities/meal/model/mealDisplayName.test.ts`
- `pnpm --filter @ai-food/mobile type-check` (or package equivalent `tsc --noEmit`) passes for Meal.name typing
</verification>

<success_criteria>
- New photo saves store meal.name from AI foodName
- MealCard title is dish name + time, not full composition list
- MealDetail shows dish name; «Состав» lists items
- SYSTEM_PROMPT explicitly separates foodName vs items[].name
- Backend untouched
</success_criteria>

<output>
Create `.planning/quick/260716-16d-sdelat-nazvanie-bluda-otdelno-ot-perechi/260716-16d-SUMMARY.md` when done
</output>
