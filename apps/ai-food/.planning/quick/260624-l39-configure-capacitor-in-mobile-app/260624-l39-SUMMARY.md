# Quick Task 260624-l39: configure Capacitor in mobile app

**Date:** 2026-06-24
**Status:** Complete
**Commit:** eec0222

## What was done

**Task 1 — Configure capacitor.config.ts and package.json scripts**

- `apps/mobile/capacitor.config.ts` — expanded from minimal stub to include:
  - `server.url: 'http://localhost:5173'` — native shell loads Vite dev server during development
  - `server.cleartext: true` — allows HTTP on Android debug builds
  - `android.allowMixedContent: true` — required when server.url is HTTP
  - Comment clarifying to remove server block before production native build
- `apps/mobile/package.json` — added scripts: `cap:sync`, `cap:open:android`, `cap:open:ios`
- `apps/mobile/package.json` — added `@capacitor/preferences: ^8.0.0` to dependencies

**Task 2 — Install packages**

- Ran `pnpm install` from repo root
- `pnpm-lock.yaml` updated with `@capacitor/preferences@8.0.1` resolution
- All three Capacitor packages now installed: `@capacitor/core`, `@capacitor/cli`, `@capacitor/preferences`

## What was NOT done

- No android/ or ios/ native platform folders added (web-first constraint preserved)
- No migration of useDiaryStore to @capacitor/preferences (separate task when needed)

## Next steps

- When ready to use native storage: migrate `useDiaryStore` persist layer from Zustand localStorage to `@capacitor/preferences`
- When ready for native builds: run `pnpm --filter @ai-food/mobile cap:sync` after removing `server.url` from config
