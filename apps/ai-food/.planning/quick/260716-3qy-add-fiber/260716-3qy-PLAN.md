---
phase: quick-260716-3qy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/index.ts
  - apps/mobile/src/entities/meal/model/mealNutritionMath.ts
  - apps/mobile/src/entities/meal/model/mealPortions.ts
  - apps/mobile/src/entities/meal/model/mealPortions.test.ts
  - apps/mobile/src/entities/meal/model/useDiaryStore.ts
  - apps/mobile/src/features/save-meal/model/useSaveMeal.ts
  - apps/mobile/src/features/refine-meal/model/useRefineMeal.ts
  - apps/mobile/src/features/onboarding/model/calculateTargets.ts
  - apps/mobile/src/features/onboarding/model/calculateTargets.test.ts
  - apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
  - apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx
  - apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx
  - apps/mobile/src/pages/settings/ui/SettingsPage.tsx
  - apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
  - apps/mobile/src/entities/meal/ui/MealCard.tsx
  - apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx
  - apps/mobile/src/features/edit-meal/ui/MealSummaryEditor.tsx
  - apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx
autonomous: true
requirements:
  - QUICK-3qy

must_haves:
  truths:
    - "New and refined meals persist fiber on every FoodItem; missing legacy fiber reads as 0 and never crashes."
    - "Daily header shows consumed vs goal fiber alongside protein/fat/carbs."
    - "Onboarding calculateTargets and OnboardingResult/Settings targets include a daily fiber goal (30 г)."
    - "MealCard, composition badges, FoodItemEditPage, and MealSummaryEditor show and edit fiber."
    - "Portion scaling multiplies fiber with other nutrients."
  artifacts:
    - packages/shared-types/src/index.ts
    - apps/mobile/src/entities/meal/model/mealNutritionMath.ts
    - apps/mobile/src/entities/meal/model/mealPortions.ts
    - apps/mobile/src/features/save-meal/model/useSaveMeal.ts
    - apps/mobile/src/features/refine-meal/model/useRefineMeal.ts
    - apps/mobile/src/features/onboarding/model/calculateTargets.ts
    - apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx
    - apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
    - apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx
  key_links:
    - "NutritionResult/NutritionItem.fiber → useSaveMeal/useRefineMeal map → FoodItem.fiber"
    - "FoodItem.fiber → DailyHeader sums → NutritionSummaryCard MacroColumn"
    - "calculateTargets.fiber → DailyTargets → OnboardingResult / Settings / DailyHeader FALLBACK"
    - "NutrientKey fiber → sanitizeFoodItemPatch / updateMealNutrition / MealSummaryEditor"
    - "scaleMealByPortionRatio includes fiber"
---

<objective>
Wire dietary fiber (клетчатка) through persistence, daily totals/targets, and diary/composition UI everywhere macros already appear.

Purpose: AI already returns fiber and NutritionCard shows it on the result screen, but diary save/totals/edit/onboarding drop it — user asked for fiber everywhere.

Output: FoodItem + DailyTargets fiber fields; save/refine/portion/math support; daily summary + onboarding/settings goals; badges and edit forms.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/shared-types/src/index.ts
@apps/mobile/src/entities/meal/model/mealNutritionMath.ts
@apps/mobile/src/entities/meal/model/mealPortions.ts
@apps/mobile/src/entities/meal/model/useDiaryStore.ts
@apps/mobile/src/features/save-meal/model/useSaveMeal.ts
@apps/mobile/src/features/refine-meal/model/useRefineMeal.ts
@apps/mobile/src/features/onboarding/model/calculateTargets.ts
@apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
@apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx
@apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx
@apps/mobile/src/entities/meal/ui/MealCard.tsx
@apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx
@apps/mobile/src/features/edit-meal/ui/MealSummaryEditor.tsx
@apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx
@apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx
@apps/mobile/src/pages/settings/ui/SettingsPage.tsx
@apps/mobile/src/widgets/nutrition-card/ui/NutritionCard.tsx

## Already done (do not redo)

- `NutritionResult.fiber: number` (required) and `NutritionItem.fiber?: number`
- analyze/refine prompts + validators already require top-level fiber
- `NutritionCard` already shows «Клетчатка» with max=30

## Decisions for this quick task

- **D-01 Types:** Add required `fiber: number` to `FoodItem` and `DailyTargets` in shared-types.
- **D-02 Legacy:** Persisted meals without fiber must not crash — all reads/sums/scales/displays use `(item.fiber ?? 0)`.
- **D-03 Targets:** `calculateTargets` returns `fiber: 30` (fixed g/day, matches NutritionCard max=30). Not derived from weight.
- **D-04 Labels:** Full «Клетчатка» in summary/onboarding/settings/edit meal totals; badge short label «Кл» (avoid lone «К» collision with ккал).
- **D-05 Mapping:** save/refine copy `item.fiber ?? 0` per composition item; empty-items fallback uses `result.fiber`; placeholders use `fiber: 0`; refine `mealContext.items` include fiber.
- **D-06 Math:** Extend `NutrientKey` and `MealNutritionPatch` with fiber; `scaleMealByPortionRatio` scales fiber; `sanitizeFoodItemPatch` sanitizes fiber.
- **Out of scope:** Backend mock, stats weekly chart (kcal-only), changing AI prompts (already ask for fiber), NutritionCard (already shows fiber).

## Patterns to follow

- FSD barrels for cross-slice imports
- MacroColumn / FoodMacrosBadges / NutrientKey extension style
- Russian UI copy
- Update co-located tests / FoodItem fixtures that omit fiber so type-check passes
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, math, persist fiber on save/refine</name>
  <files>packages/shared-types/src/index.ts, apps/mobile/src/entities/meal/model/mealNutritionMath.ts, apps/mobile/src/entities/meal/model/mealPortions.ts, apps/mobile/src/entities/meal/model/mealPortions.test.ts, apps/mobile/src/entities/meal/model/useDiaryStore.ts, apps/mobile/src/features/save-meal/model/useSaveMeal.ts, apps/mobile/src/features/refine-meal/model/useRefineMeal.ts, apps/mobile/src/features/onboarding/model/calculateTargets.ts, apps/mobile/src/features/onboarding/model/calculateTargets.test.ts</files>
  <behavior>
    - calculateTargets always returns fiber: 30
    - scaleMealByPortionRatio multiplies fiber by ratio (legacy missing fiber → 0 before scale)
    - NutrientKey includes fiber; sanitizeFoodItemPatch clamps fiber like other nutrients
  </behavior>
  <action>
    1. In shared-types: add `fiber: number` to `FoodItem` and `DailyTargets` (D-01). Keep `NutritionItem.fiber?` and `NutritionResult.fiber` unchanged.

    2. In `mealNutritionMath.ts`: extend `NutrientKey` with fiber; include fiber in sanitizeFoodItemPatch nutrientKeys loop (D-06).

    3. In `useDiaryStore.ts`: add `fiber?: number` to `MealNutritionPatch`; append fiber to `NUTRIENT_KEYS` so updateMealNutrition can scale fiber across items.

    4. In `mealPortions.ts`: when mapping scaled items, set fiber via sanitizeNutrient of `(item.fiber ?? 0) * ratio` (D-02, D-06). Extend `mealPortions.test.ts` with fiber on fixtures and assert scaled fiber.

    5. In `useSaveMeal.ts`: placeholders + manual (no-image) items get `fiber: 0`; map AI items with `fiber: item.fiber ?? 0`; empty-items fallback uses `fiber: result.fiber` (D-05).

    6. In `useRefineMeal.ts`: widen `mapResultToItems` input types to include optional item fiber + top-level fiber; map the same way as save; include `fiber: item.fiber ?? 0` in refine mealContext items (D-05).

    7. In `calculateTargets.ts`: return `fiber: 30` (D-03). Add/adjust `calculateTargets.test.ts` to assert fiber is 30.

    8. Fix any FoodItem object literals in nearby tests broken by the new required field (add fiber: 0 or realistic values) — prefer files touched by type-check failures from this task; leave pure UI fixtures for task 3 if they only fail after UI changes.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/entities/meal/model/mealPortions.test.ts src/features/onboarding/model/calculateTargets.test.ts</automated>
  </verify>
  <done>
    FoodItem/DailyTargets include fiber; save/refine persist it; NutrientKey/MealNutritionPatch/portion scale handle fiber; calculateTargets returns 30; listed tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Daily totals + onboarding/settings fiber goal</name>
  <files>apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx, apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx, apps/mobile/src/features/onboarding/ui/OnboardingResult.tsx, apps/mobile/src/pages/settings/ui/SettingsPage.tsx</files>
  <action>
    1. `DailyHeader`: add `fiber: 30` to FALLBACK_TARGETS; read `goalFiber` from targets; sum consumed fiber with `(i.fiber ?? 0)` over ready day meals; pass consumed/goal fiber into NutritionSummaryCard (D-02, D-03).

    2. `NutritionSummaryCard`: add consumedFiber + goalFiber props; add a fourth MacroColumn labeled «Клетчатка» (D-04); switch macro grid from 3 to 4 columns (grid-cols-4) keeping existing MacroColumn pattern; pick a distinct bar color (e.g. emerald/teal, not rose/amber/sky already used).

    3. `OnboardingResult`: add list row «Клетчатка» → `{targets.fiber} г`.

    4. `SettingsPage` profile targets dl: add клетчатка row using `targets.fiber` when targets present. Update useProfileStore.test mockTargets only if type-check requires fiber on DailyTargets mocks.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec tsc --noEmit</automated>
  </verify>
  <done>
    Daily summary and onboarding/settings show fiber consumed/goal; type-check clean for these surfaces.
  </done>
</task>

<task type="auto">
  <name>Task 3: Diary and composition UI for fiber</name>
  <files>apps/mobile/src/entities/meal/ui/FoodMacrosBadges.tsx, apps/mobile/src/entities/meal/ui/MealCard.tsx, apps/mobile/src/features/edit-meal/ui/FoodItemDisplayCard.tsx, apps/mobile/src/features/edit-meal/ui/MealSummaryEditor.tsx, apps/mobile/src/pages/food-item-edit/ui/FoodItemEditPage.tsx</files>
  <action>
    1. `FoodMacrosBadges`: add required `fiber: number` prop; render a fifth badge «Кл {formatMacro(fiber)}» with a distinct muted/emerald-style badge class (D-04).

    2. `MealCard` and `FoodItemDisplayCard`: include fiber in reduce totals / props (`item.fiber ?? 0`) and pass to FoodMacrosBadges (D-02).

    3. `FoodItemEditPage`: extend patchNumber field union with fiber; add fifth input in the macro grid (label «Кл», aria-label «Клетчатка»). Layout may stay 2-col grid (5th cell wraps).

    4. `MealSummaryEditor`: include fiber in totals reduce; add editable fiber field alongside protein/carbs/fat that calls updateMealNutrition with fiber; adjust grid (e.g. grid-cols-2 or grid-cols-4) so all four macros fit without cutting labels.

    5. Run type-check / focused tests and fix remaining FoodItem fixtures missing fiber across mobile package.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec tsc --noEmit && pnpm --filter @ai-food/mobile exec vitest run src/entities/meal src/features/save-meal src/features/refine-meal src/features/onboarding/model</automated>
  </verify>
  <done>
    Badges, meal card, composition cards, item edit, and meal summary edit all show/edit fiber; tsc and listed vitest suites pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI JSON → client FoodItem | Model-returned nutrition numbers cross into local diary persist |
| localStorage/Capacitor persist → UI | Legacy meals without fiber hydrate into typed FoodItem |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3qy-01 | Tampering | useSaveMeal / useRefineMeal map | low | mitigate | Coerce with sanitizeNutrient / `?? 0`; never trust raw NaN into totals |
| T-3qy-02 | Information Disclosure | fiber in diary UI | low | accept | Local-only nutrition; no new network exposure |
| T-3qy-03 | Elevation of Privilege | DailyTargets.fiber | low | accept | Client-calculated goal only; no server auth surface |
| T-3qy-SC | Tampering | npm installs | high | accept | No new packages in this plan |
</threat_model>

<verification>
- mealPortions + calculateTargets unit tests green
- package type-check passes after FoodItem.fiber required
- Manual smoke (executor notes in SUMMARY): save analyzed meal → diary badges show Кл; daily header fiber bar moves; edit ingredient fiber; change portions and fiber scales
</verification>

<success_criteria>
- Fiber persists on FoodItem through analyze and refine paths
- Daily totals and goals include fiber (fallback/onboarding 30 г)
- Diary list, composition, and edit surfaces expose fiber with Russian labels
- Legacy meals without fiber behave as 0 fiber
</success_criteria>

<output>
Create `.planning/quick/260716-3qy-add-fiber/260716-3qy-SUMMARY.md` when done
</output>
