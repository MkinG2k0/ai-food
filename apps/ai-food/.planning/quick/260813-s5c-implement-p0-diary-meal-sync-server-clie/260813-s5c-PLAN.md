---
phase: 260813-s5c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ai-app/prisma/schema.prisma
  - apps/ai-app/prisma/migrations/20260813120000_meal_sync/migration.sql
  - apps/ai-app/src/lib/mealSync.ts
  - apps/ai-app/src/lib/mealSync.test.ts
  - apps/ai-app/src/routes/userMeals.ts
  - apps/ai-app/src/routes/userMeals.sync.test.ts
  - apps/ai-app/src/app.ts
  - apps/ai-food/src/shared/types/index.ts
  - apps/ai-food/src/features/diary-sync/api/syncMealsApi.ts
  - apps/ai-food/src/features/diary-sync/model/mealSyncMerge.ts
  - apps/ai-food/src/features/diary-sync/model/mealSyncMerge.test.ts
  - apps/ai-food/src/features/diary-sync/model/syncDiaryMeals.ts
  - apps/ai-food/src/features/diary-sync/model/useSyncMealOnLeave.ts
  - apps/ai-food/src/features/diary-sync/index.ts
  - apps/ai-food/src/entities/meal/model/useDiaryStore.ts
  - apps/ai-food/src/features/save-meal/model/useSaveMeal.ts
  - apps/ai-food/src/features/scan-barcode/model/useSaveBarcodeMeal.ts
  - apps/ai-food/src/features/manual-entry/model/useSaveManualMeal.ts
  - apps/ai-food/src/features/favorites/model/useQuickAddFavorite.ts
  - apps/ai-food/src/features/delete-meal/model/useConfirmDeleteMeal.ts
  - apps/ai-food/src/pages/login/ui/LoginPage.tsx
  - apps/ai-food/src/features/onboarding/ui/steps/StepGender.tsx
  - apps/ai-food/src/app/DiarySyncOnAuthHydrate.tsx
  - apps/ai-food/src/app/App.tsx
  - apps/ai-food/src/pages/meal-detail/ui/MealDetailPage.tsx
  - apps/ai-food/src/pages/food-item-edit/ui/FoodItemEditPage.tsx
  - apps/ai-food/docs/USER-DATA-SYNC.md
autonomous: true
requirements:
  - QUICK-260813-s5c
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Authenticated user can POST /user/meals/sync with X-User-Token; guest diary stays local-only (D-05, D-06)"
    - "LWW by clientUpdatedAt; soft-delete tombstones win/lose by the same clock (D-06)"
    - "Leaving /meal/:id or item-edit upserts that meal once — not on every field edit (D-01)"
    - "addMeal paths and confirm-delete push immediately when userToken present (D-02, D-03)"
    - "After login success and on auth hydrate with token, full diary sync runs (D-04)"
    - "Sync is not gated by subscription; no photo blob upload; no favorites/weight/settings (D-06)"
  artifacts:
    - path: apps/ai-app/prisma/schema.prisma
      provides: "Meal model + User.meals relation"
    - path: apps/ai-app/src/routes/userMeals.ts
      provides: "POST /user/meals/sync"
    - path: apps/ai-food/src/features/diary-sync/api/syncMealsApi.ts
      provides: "client POST with X-User-Token"
    - path: apps/ai-food/src/features/diary-sync/model/mealSyncMerge.ts
      provides: "LWW merge + payload builders"
    - path: apps/ai-food/src/features/diary-sync/model/useSyncMealOnLeave.ts
      provides: "exit-from-meal-UI upsert (D-01)"
    - path: apps/ai-food/docs/USER-DATA-SYNC.md
      provides: "Sync triggers section matching locked decisions"
  key_links:
    - from: X-User-Token JWT sub
      to: Meal.userId
      via: "ownership only from token; never body userId"
    - from: useDiaryStore.meals
      to: POST /user/meals/sync upserts/deletes
      via: "syncDiaryMeals + syncMealsApi"
    - from: server meals + tombstones
      to: useDiaryStore.setState meals
      via: "mealSyncMerge apply response"
    - from: MealDetailPage / FoodItemEditPage unmount
      to: single-meal upsert
      via: "useSyncMealOnLeave"
---

<objective>
Implement P0 diary meal sync (server + client) per docs/USER-DATA-SYNC.md with locked sync triggers (D-01–D-06).

Purpose: Cross-device restore of diary after Telegram/demo login without gating on subscription and without breaking guest offline Preferences diary.

Output: Prisma Meal + POST /user/meals/sync; client diary-sync feature; wires for leave-UI / add / delete / login+hydrate; USER-DATA-SYNC.md Sync triggers section.
</objective>

<execution_context>
@C:/Users/mk/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/mk/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/ai-food/.planning/STATE.md
@apps/ai-food/.planning/quick/260813-s5c-implement-p0-diary-meal-sync-server-clie/260813-s5c-CONTEXT.md
@apps/ai-food/docs/USER-DATA-SYNC.md
@apps/ai-app/prisma/schema.prisma
@apps/ai-app/src/routes/auth.ts
@apps/ai-app/src/routes/auth.nutritionProfile.test.ts
@apps/ai-app/src/app.ts
@apps/ai-food/src/features/auth/api/putNutritionProfile.ts
@apps/ai-food/src/features/onboarding/model/reconcileNutritionProfileAfterLogin.ts
@apps/ai-food/src/entities/meal/model/useDiaryStore.ts
@apps/ai-food/src/features/delete-meal/model/useConfirmDeleteMeal.ts
@apps/ai-food/src/shared/types/index.ts
@apps/ai-food/src/app/router.tsx
@apps/ai-food/src/pages/login/ui/LoginPage.tsx

## Locked decisions (CONTEXT — do not revisit)

- **D-01** Upsert when leaving meal UI (`/meal/:id`, `/meal/:mealId/item/:itemId`) — not per-field while editing.
- **D-02** Immediate sync after `addMeal` (scan / manual / barcode / favorite).
- **D-03** Immediate sync after confirm-delete.
- **D-04** Full sync after login success and when auth hydrate has `X-User-Token`.
- **D-05** Guests: Preferences/`ai-food-diary` only — no diary sync without token.
- **D-06** Follow USER-DATA-SYNC.md: bulk `POST /user/meals/sync`, LWW `clientUpdatedAt`, soft-delete, no photo blobs, no subscription gate; out of scope: favorites/weight/settings/micronutrientTargets sync.

## Patterns to mirror

- Auth: `req.header('x-user-token')` + `verifyUserToken` → `payload.sub` as userId (auth.ts `/profile`).
- Client fetch: `putNutritionProfile` style — `VITE_AI_GATEWAY_URL` + `X-User-Token` from `useAuthStore`.
- Tests: vitest + supertest + vi.hoisted prisma/jwt mocks like `auth.nutritionProfile.test.ts`.
- Login reconcile: call diary full-sync next to `reconcileNutritionProfileAfterLogin` in LoginPage / StepGender.
- FSD: new feature slice `features/diary-sync` with barrel; pages import via `@/features/diary-sync`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prisma Meal + POST /user/meals/sync (LWW + tombstones)</name>
  <files>apps/ai-app/prisma/schema.prisma, apps/ai-app/prisma/migrations/20260813120000_meal_sync/migration.sql, apps/ai-app/src/lib/mealSync.ts, apps/ai-app/src/lib/mealSync.test.ts, apps/ai-app/src/routes/userMeals.ts, apps/ai-app/src/routes/userMeals.sync.test.ts, apps/ai-app/src/app.ts</files>
  <action>
  Per D-06 / USER-DATA-SYNC §3–4, implement the thin end-to-end server path for diary sync:

  1. Add Prisma `Meal` model with fields from the design table (id client-cuid @id, userId FK → User, timestamp, name?, items Json, totalCalories, portions?, totalGrams?, status?, healthiness?, confidence?, analyzeErrorCode?, micronutrients Json?, aiModel?, portionReference?, addedSugar?, confidenceReason?, healthinessReason?, disclaimers Json?, customContent?, customContentEntries Json?, imageUri?, imageUris Json?, clientUpdatedAt DateTime, deletedAt DateTime?, createdAt/updatedAt). Indexes: `@@index([userId, timestamp])`, `@@index([userId, clientUpdatedAt])`. Add `meals Meal[]` on User.

  2. Create SQL migration under `prisma/migrations/20260813120000_meal_sync/` (or next free timestamp) matching schema; run generate so client types exist.

  3. Extract pure merge helpers in `src/lib/mealSync.ts`: Zod schemas for body `{ since?: ISO string, upserts: MealPayload[], deletes: { id, clientUpdatedAt }[] }` and MealPayload (mirror client Meal + required `clientUpdatedAt` ISO); LWW compare; apply upsert (create/update when incoming clientUpdatedAt &gt;= stored, including undelete when newer than tombstone); apply soft-delete when delete clock wins; build response `{ meals: active MealPayload[], tombstones: id[] }` scoped to that user (optional `since` filters returned rows by clientUpdatedAt/deletedAt). Ownership: userId always from JWT sub — never accept userId from body.

  4. `userMealsRouter` POST `/sync`: require token like auth `/profile` (401 without/invalid); no subscription check; Zod 400 on bad body; call merge against prisma.meal for payload.sub; mount at `app.use('/user/meals', userMealsRouter)` in app.ts.

  5. Tests: unit tests for LWW/tombstone pure helpers; route tests (supertest style of auth.nutritionProfile.test.ts) covering 401, 400, successful upsert round-trip, older upsert ignored/winner returned, delete tombstone, and that subscriptionStatus is irrelevant.
  </action>
  <verify>
    <automated>cd apps/ai-app && pnpm exec vitest run src/lib/mealSync.test.ts src/routes/userMeals.sync.test.ts</automated>
  </verify>
  <done>
  POST /user/meals/sync with X-User-Token upserts/deletes with LWW + soft-delete; migration + Meal model exist; tests green; no subscription gate (D-05/D-06 server side).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client sync core — API, merge, add/delete/login/hydrate wires</name>
  <files>apps/ai-food/src/shared/types/index.ts, apps/ai-food/src/features/diary-sync/api/syncMealsApi.ts, apps/ai-food/src/features/diary-sync/model/mealSyncMerge.ts, apps/ai-food/src/features/diary-sync/model/mealSyncMerge.test.ts, apps/ai-food/src/features/diary-sync/model/syncDiaryMeals.ts, apps/ai-food/src/features/diary-sync/index.ts, apps/ai-food/src/entities/meal/model/useDiaryStore.ts, apps/ai-food/src/features/save-meal/model/useSaveMeal.ts, apps/ai-food/src/features/scan-barcode/model/useSaveBarcodeMeal.ts, apps/ai-food/src/features/manual-entry/model/useSaveManualMeal.ts, apps/ai-food/src/features/favorites/model/useQuickAddFavorite.ts, apps/ai-food/src/features/delete-meal/model/useConfirmDeleteMeal.ts, apps/ai-food/src/pages/login/ui/LoginPage.tsx, apps/ai-food/src/features/onboarding/ui/steps/StepGender.tsx, apps/ai-food/src/app/DiarySyncOnAuthHydrate.tsx, apps/ai-food/src/app/App.tsx</files>
  <behavior>
    - mealSyncMerge: newer clientUpdatedAt meal replaces older; tombstone removes local meal when delete clock wins; older server row does not overwrite newer local.
    - buildSyncPayload: upserts include clientUpdatedAt; deletes list pending delete ids with clocks; imageUri/imageUris passed as stubs only.
    - syncDiaryMeals no-ops when useAuthStore.userToken is null (D-05).
    - After successful sync response, diary meals match merged active set (tombstoned ids gone).
  </behavior>
  <action>
  Per D-02, D-03, D-04, D-05, D-06:

  1. Optional `clientUpdatedAt?: string` on Meal in shared types (ISO). Touch `clientUpdatedAt` to `new Date().toISOString()` inside diary store mutations that change meal data (addMeal/updateMeal/item/nutrition/portions/grams/remove paths that matter for LWW). Keep guest local persist working unchanged.

  2. Feature `features/diary-sync`: `syncMealsApi` POST `{gateway}/user/meals/sync` with Content-Type JSON + X-User-Token (mirror putNutritionProfile; throw if no URL/token). Pure `mealSyncMerge` + payload builders + vitest. `syncDiaryMeals({ mode: 'full' | 'upsert' | 'delete', mealIds?: string[] })`: if no token return; else build body from store (full = all meals as upserts + any pendingDeletes; upsert/delete = subset); await API; apply meals/tombstones into useDiaryStore; clear synced pendingDeletes. Track `pendingDeletes: { id, clientUpdatedAt }[]` either on diary store or a small module in diary-sync — confirmDelete must record tombstone clock before removeMeal.

  3. Wire immediate sync (fire-and-forget void …catch log/toast discreetly, do not block UX): after addMeal in useSaveMeal, useSaveBarcodeMeal, useSaveManualMeal, useQuickAddFavorite (D-02); in useConfirmDeleteMeal.confirmDelete after local remove (D-03).

  4. Full sync (D-04): after `reconcileNutritionProfileAfterLogin` in LoginPage.handleLoginSuccess and StepGender login success path; plus `DiarySyncOnAuthHydrate` component mounted from App that once auth+diary persist hydration finish and userToken present calls syncDiaryMeals({ mode: 'full' }) once per session hydrate. Guests never call API (D-05).
  </action>
  <verify>
    <automated>cd apps/ai-food && pnpm exec vitest run src/features/diary-sync/model/mealSyncMerge.test.ts</automated>
  </verify>
  <done>
  Authenticated add/delete/login/hydrate trigger sync; merge helpers tested; guests never hit /user/meals/sync (D-02–D-05).
  </done>
</task>

<task type="auto">
  <name>Task 3: Exit-from-meal-UI upsert + USER-DATA-SYNC Sync triggers</name>
  <files>apps/ai-food/src/features/diary-sync/model/useSyncMealOnLeave.ts, apps/ai-food/src/features/diary-sync/index.ts, apps/ai-food/src/pages/meal-detail/ui/MealDetailPage.tsx, apps/ai-food/src/pages/food-item-edit/ui/FoodItemEditPage.tsx, apps/ai-food/docs/USER-DATA-SYNC.md</files>
  <action>
  Per D-01 and D-06 docs update:

  1. Add `useSyncMealOnLeave(mealId: string)` — on unmount (and/or react-router navigate away), if userToken present and meal still exists locally, call syncDiaryMeals({ mode: 'upsert', mealIds: [mealId] }). Do not sync on each field keystroke/update while the page is mounted. Mount the hook in MealDetailPage and FoodItemEditPage (use mealId / mealId from params). Navigating detail → item-edit may double-upsert the same id; that is acceptable (idempotent LWW).

  2. Update USER-DATA-SYNC.md: mark Phase B / P0 as implementation in progress or done (remove “design-only / no routes” contradictions in §7); add a **Sync triggers** section that lists exactly the five locked behaviors (leave meal UI upsert; immediate add; immediate confirm-delete; full sync after login/auth hydrate; guests local-only) plus reminders: LWW clientUpdatedAt, soft-delete, no subscription gate, no photo blobs, favorites/weight/settings still out of scope.
  </action>
  <verify>
    <automated>cd apps/ai-food && pnpm exec vitest run src/features/diary-sync && cd ../ai-app && pnpm exec vitest run src/lib/mealSync.test.ts src/routes/userMeals.sync.test.ts</automated>
  </verify>
  <done>
  Leaving meal detail/item-edit upserts once when authenticated (D-01); docs Sync triggers match CONTEXT; regression tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /user/meals/sync | Untrusted body; auth only via X-User-Token |
| JWT sub → Meal.userId | Server must ignore any client-claimed owner |
| Guest device | No diary sync network path without token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-s5c-01 | Elevation | userMeals sync | high | mitigate | Bind all reads/writes to verifyUserToken payload.sub; never take userId from body |
| T-s5c-02 | Tampering | LWW clientUpdatedAt | medium | mitigate | Reject/ignore older clocks; return winner snapshot so client converges |
| T-s5c-03 | Information disclosure | GET-like response meals | medium | mitigate | Query prisma.meal only where userId = sub |
| T-s5c-04 | Denial of service | bulk upserts size | low | accept | P0 trust authenticated clients; optional soft cap discretionary if easy |
| T-s5c-05 | Elevation | subscription bypass | low | accept | Intentional: diary sync must remain free (D-06) |
| T-s5c-SC | Tampering | npm installs | high | accept | No new packages in this plan — reuse express/zod/prisma/vitest/supertest |
</threat_model>

<verification>
- ai-app: mealSync unit + userMeals.sync route tests pass
- ai-food: mealSyncMerge tests pass; type-check if touched types break consumers
- Manual smoke (executor notes in SUMMARY): guest add meal stays local; demo login pulls/pushes diary; edit fields without leaving does not spam network; leave detail syncs; confirm delete tombstones
</verification>

<success_criteria>
- Authenticated multi-device diary restore via POST /user/meals/sync works with LWW + soft-delete
- All five locked sync triggers (D-01–D-05) implemented; D-06 contract honored
- Guest offline diary unchanged; sync not subscription-gated; no photo blob upload
</success_criteria>

<output>
Create `apps/ai-food/.planning/quick/260813-s5c-implement-p0-diary-meal-sync-server-clie/260813-s5c-SUMMARY.md` when done
</output>
