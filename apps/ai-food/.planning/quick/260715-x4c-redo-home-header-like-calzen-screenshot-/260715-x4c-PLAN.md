---
phase: quick
plan: 260715-x4c
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/widgets/daily-header/ui/WeekStrip.tsx
  - apps/mobile/src/widgets/daily-header/ui/WeekStrip.test.tsx
  - apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx
  - apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
  - apps/mobile/src/pages/home/ui/HomePage.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Шапка главной — светлый CalZen-layout: фон страницы светло-серый, не сплошной зелёный header; по центру заголовок приложения «AI Food»."
    - "Горизонтальная лента дней сохраняет swipe-карусель (3 недели) и выбор дня; выбранный день — тёмный/чёрный круг с белой цифрой; подписи дней на русском (Пн…Вс)."
    - "Белая карточка КБЖУ: слева крупные «калории осталось» + «/цель» и подпись «Калорий осталось»; справа кольцо прогресса (съедено/цель); снизу Белки / Жир / Углеводы с цветными тонкими барами и значениями «N/Mг»; превышение цели подсвечено красным."
    - "Данные карточки из дневника за selectedDate (только ready-приёмы) + цели из useProfileStore.targets с fallback-константами; meal list и зелёный FAB без регрессий."
    - "Блоки Steps / Water / «Мой прогресс» НЕ добавляются."
  artifacts:
    - path: "apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx"
      provides: "Светлая композиция: top bar + WeekStrip + NutritionSummaryCard"
      contains: "AI Food"
    - path: "apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx"
      provides: "Карточка остатка калорий, SVG-кольцо, макро-бары Б/Ж/У"
      contains: "Калорий осталось"
    - path: "apps/mobile/src/widgets/daily-header/ui/WeekStrip.tsx"
      provides: "Светлая стилизация выбранного дня (тёмный круг)"
      contains: "bg-foreground"
  key_links:
    - from: "useDiaryStore.meals + selectedDate"
      to: "NutritionSummaryCard (consumed kcal + macros)"
      via: "DailyHeader aggregation filter isSameDay + status ready"
      pattern: "totalCalories"
    - from: "useProfileStore.targets"
      to: "NutritionSummaryCard goals (kcal/protein/fat/carbs)"
      via: "DailyHeader props or inline targets with fallbacks"
      pattern: "targets"
    - from: "WeekStrip onDaySelect / onWeekChange"
      to: "HomePage selectedDate / weekOffset + MealList"
      via: "unchanged WeekStripProps contract"
      pattern: "onDaySelect"
---

<objective>
Переделать шапку главной страницы под CalZen-референс (второй скрин в assets workspace): светлый top bar с названием приложения, недельная лента дней и белая карточка КБЖУ (остаток калорий, кольцо прогресса, бары Б/Ж/У). Без Steps/Water/«Мой прогресс».

Purpose: Текущий DailyHeader — full-bleed emerald; нужно совпадение с целевым светлым layout при сохранении существующей навигации по дням и данных дневника.

Output: Обновлённые WeekStrip + DailyHeader, новый NutritionSummaryCard в виджете daily-header, лёгкий фон HomePage; визуальная проверка человеком.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/mobile/src/pages/home/ui/HomePage.tsx
@apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
@apps/mobile/src/widgets/daily-header/ui/WeekStrip.tsx
@apps/mobile/src/widgets/daily-header/ui/WeekStrip.test.tsx
@apps/mobile/src/widgets/daily-header/index.ts
@apps/mobile/src/entities/meal/model/useDiaryStore.ts
@apps/mobile/src/features/onboarding/model/useProfileStore.ts
@apps/mobile/src/shared/lib/dateUtils.ts
@apps/mobile/src/app/styles/global.css
@packages/shared-types/src/index.ts

## Design constraints (TARGET — second CalZen screenshot)
- Light gray page background; NOT solid green header
- Centered app title «AI Food»; optional decorative profile icon top-right (no route required — profile page отсутствует)
- Week strip: Russian short labels via existing formatDayLabel; selected = solid dark circle + white number
- KBZhU white rounded card: remaining large + /goal muted + «Калорий осталось»; right = circular progress ring (consumed vs goal) with flame-ish center optional via lucide Flame
- Bottom: Белки / Жир / Углеводы thin bars (pink / amber-yellow / blue); values «N/Mг»; over goal → text-destructive
- Skip fiber column (no fiber goal in DailyTargets)
- Out of scope: Steps, Water, «Мой прогресс»
- Keep MealList + green FAB; brand green only where it already fits (FAB), not as header fill

## Data
- Day totals: sum meal.totalCalories and item protein/fat/carbs for selectedDate where (m.status ?? 'ready') === 'ready'
- Goals: useProfileStore targets (DailyTargets: kcal, protein, fat, carbs); if null use fallbacks e.g. kcal 2000, protein 150, fat 70, carbs 250
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle WeekStrip for light CalZen look</name>
  <files>apps/mobile/src/widgets/daily-header/ui/WeekStrip.tsx, apps/mobile/src/widgets/daily-header/ui/WeekStrip.test.tsx</files>
  <action>
Перекрасить `WeekStrip` под светлый фон родителя, не меняя механику карусели и контракт `WeekStripProps` (weekOffset, selectedDate, meals, onDaySelect, onWeekChange). Сохранить overflow-hidden viewport, 3-slot track, drag/animate recenter, meal-dot indicator.

Визуал выбранного дня (CalZen): номер дня в сплошном тёмном круге (`bg-foreground` или `bg-zinc-900`) с белым текстом; невыбранный — `text-foreground` / `text-muted-foreground` без emerald. Подписи дней (`formatDayLabel`) — muted. Meal-dot: мелкий primary/emerald или muted когда есть приёмы; прозрачный когда нет. Убрать классы под зелёный header (`text-emerald-100`, `bg-white text-emerald-600`, `hover:bg-emerald-400` и т.п.).

Обновить `WeekStrip.test.tsx` только если утверждения завязаны на старые классы; иначе оставить существующие тесты (21 buttons, viewport overflow-hidden, onDaySelect, meal-dot) зелёными.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/widgets/daily-header/ui/WeekStrip.test.tsx</automated>
  </verify>
  <done>
    WeekStrip на светлом фоне читается как CalZen-лента: тёмный selected circle, русские ярлыки, swipe/carousel и тесты без регрессий; WeekStripProps неизменён.
  </done>
</task>

<task type="auto">
  <name>Task 2: NutritionSummaryCard + rebuild DailyHeader + HomePage background</name>
  <files>apps/mobile/src/widgets/daily-header/ui/NutritionSummaryCard.tsx, apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx, apps/mobile/src/pages/home/ui/HomePage.tsx, apps/mobile/src/widgets/daily-header/index.ts</files>
  <action>
1. Создать `NutritionSummaryCard.tsx` внутри виджета daily-header (presentational; props: consumed kcal + macros и goals). Белая карточка (`bg-card` / white, rounded-2xl, лёгкая тень или border):
   - Ряд: слева крупное число остатка `Math.max(0, Math.round(goalKcal - consumedKcal))` (или показать сверх нормы отдельной веткой текста — по смыслу CalZen «осталось», при переедании можно показывать 0 осталось и визуально полное кольцо + destructive hint кратко), рядом muted `/goalKcal`, подпись «Калорий осталось».
   - Справа: SVG circular progress ring (consumed/goal, clamp 0–100%); в центре опционально иконка Flame из lucide-react. Не подключать новые npm-пакеты.
   - Низ: три колонки Белки / Жир / Углеводы — тонкий цветной бар (роза/розовый, жёлто-оранжевый, синий), подписи по-русски, значения `{consumed}/{goal}г`; если consumed > goal — класс `text-destructive` на значении.

2. Переписать `DailyHeader.tsx`: убрать `bg-emerald-500` full-bleed. Layout:
   - outer `header` на прозрачном/светлом фоне, `px-4 pt-10/12 pb-2`;
   - top bar: grid/flex с центрированным «AI Food» (font-semibold), справа декоративная кнопка/иконка User (aria-label «Профиль») без навигации;
   - затем `WeekStrip` с теми же props;
   - затем `NutritionSummaryCard` с агрегированными totals.

   Агрегация в DailyHeader: из `useDiaryStore` meals отфильтровать по `isSameDay(timestamp, selectedDate)` и `(status ?? 'ready') === 'ready'`; kcal = sum totalCalories; macros = sum по items protein/fat/carbs. Цели из `useProfileStore((s) => s.targets)` с fallback-константами на уровне модуля (не хардкодить разрозненно в JSX). Удалить старый прогресс-бар и `formatHeaderDate` блок, если они больше не нужны визуально.

3. `HomePage.tsx`: сменить корневой фон с чистого `bg-background` на светло-серый (`bg-muted` или `bg-zinc-50`), чтобы белая карточка контрастировала; MealList + FAB + AddFoodSheet не трогать по логике.

4. Barrel `index.ts`: по-прежнему экспортировать только `DailyHeader` (карточку наружу не обязательно). Cross-slice импорты только через barrels; внутри виджета — относительные импорты.

Не добавлять Steps/Water/«Мой прогресс». Не менять persist/diary store API.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile type-check && pnpm --filter @ai-food/mobile exec vitest run src/widgets/daily-header/</automated>
  </verify>
  <done>
    DailyHeader без зелёного full-bleed; видны title, WeekStrip, NutritionSummaryCard с остатком/кольцом/БЖУ; HomePage фон светло-серый; type-check и weekstrip tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual check against CalZen reference</name>
  <files>apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx, apps/mobile/src/pages/home/ui/HomePage.tsx</files>
  <action>
Человек сравнивает собранную шапку с целевым вторым CalZen-скрином в assets workspace. Код не менять, пока не описаны gaps. Ожидание: светлый фон, «AI Food», лента дней с тёмным selected, белая КБЖУ-карточка; без Steps/Water/«Мой прогресс».
  </action>
  <what-built>
    Светлая шапка главной с «AI Food», недельной лентой (тёмный selected day) и белой карточкой КБЖУ (остаток калорий, кольцо, бары Б/Ж/У). Meal list и FAB сохранены.
  </what-built>
  <how-to-verify>
    1. Запустить `pnpm --filter @ai-food/mobile dev` (для шапки достаточно дневника в localStorage).
    2. Открыть `/` — сравнить со вторым скрином в assets: светлый фон, не зелёный header; title по центру; лента дней; белая КБЖУ-карточка.
    3. Выбрать другой день — totals и meal list согласованы.
    4. При наличии приёмов с макросами — бары заполняются; при превышении цели — красная подсветка / полное кольцо.
    5. Нет блоков Steps/Water/«Мой прогресс»; FAB зелёный на месте.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @ai-food/mobile type-check</automated>
  </verify>
  <done>
    Пользователь подтвердил визуальное соответствие целевому CalZen-скрину (или gaps исправлены follow-up правками).
  </done>
  <resume-signal>Type "approved" or describe visual gaps vs screenshot</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → UI | Diary/profile Zustand persist renders on home header; no new network trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x4c-01 | Information Disclosure | DailyHeader totals | low | accept | Local-only MVP; no new server exposure of diary |
| T-x4c-02 | Tampering | Decorative profile control | low | mitigate | No navigation / no write side-effects on icon click |
| T-x4c-SC | Tampering | npm installs | high | accept | No new packages in this plan |
</threat_model>

<verification>
- vitest WeekStrip suite green
- mobile type-check green
- Human visual pass against CalZen target screenshot
</verification>

<success_criteria>
Home header matches CalZen light layout (title + week strip + KBZhU card with remaining/ring/macros); diary/day selection behavior preserved; Steps/Water/progress sections absent; FAB and meal list intact.
</success_criteria>

<output>
Create `.planning/quick/260715-x4c-redo-home-header-like-calzen-screenshot-/260715-x4c-SUMMARY.md` when done
</output>
