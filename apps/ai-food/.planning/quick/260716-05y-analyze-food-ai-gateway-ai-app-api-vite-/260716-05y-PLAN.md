---
phase: quick
plan: 260716-05y
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
  - apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts
  - apps/mobile/src/vite-env.d.ts
autonomous: true
requirements:
  - QUICK-AI-GATEWAY
user_setup: []

must_haves:
  truths:
    - "analyzeFoodApi(image) по-прежнему возвращает AnalyzeFoodResponse { result, processingTime } без смены хуков/UI."
    - "Запрос идёт на `${VITE_AI_GATEWAY_URL}/v1/chat/completions` с Bearer (или X-API-Key) из VITE_AI_GATEWAY_API_KEY, не на локальный /analyze-food."
    - "Промпт на клиенте совпадает по смыслу с backend SYSTEM_PROMPT (русский foodName + NutritionResult JSON)."
    - "Ошибки gateway/парсинга reject-ятся как ApiError с кодами INVALID_IMAGE | RATE_LIMITED | ANALYSIS_TIMEOUT | ANALYSIS_FAILED где уместно."
    - "apps/backend/** не изменён; secrets не коммитятся."
  artifacts:
    - path: "apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts"
      provides: "Gateway-backed analyzeFoodApi(File) → AnalyzeFoodResponse"
      contains: "VITE_AI_GATEWAY"
    - path: "apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts"
      provides: "Unit-тесты happy path + error mapping"
      contains: "analyzeFoodApi"
    - path: "apps/mobile/src/vite-env.d.ts"
      provides: "Типы VITE_AI_GATEWAY_URL и VITE_AI_GATEWAY_API_KEY"
      contains: "VITE_AI_GATEWAY"
  key_links:
    - from: "useAnalyzeFood / useSaveMeal"
      to: "AI Gateway POST /v1/chat/completions"
      via: "analyzeFoodApi(File) → base64 data URL → chat.completions → NutritionResult"
      pattern: "analyzeFoodApi"
    - from: "Gateway error body { message, code, status }"
      to: "TanStack Query isError / UI"
      via: "Promise.reject(ApiError) с маппингом кодов"
      pattern: "RATE_LIMITED|ANALYSIS_TIMEOUT|INVALID_IMAGE|ANALYSIS_FAILED"
---

<objective>
Переключить mobile `analyzeFoodApi` на задеплоенный AI Gateway (ai-app, OpenAI-compatible REST), сохранив контракт `analyzeFoodApi(image: File) => Promise<AnalyzeFoodResponse>` и не трогая `apps/backend/**` (D-01–D-07 из locked decisions).

Purpose: Анализ еды идёт через prod gateway с `VITE_AI_GATEWAY_URL` / `VITE_AI_GATEWAY_API_KEY`, без локального mock/OpenAI proxy.

Output: Переписанный `analyzeFoodApi.ts`, типы env в `vite-env.d.ts`, unit-тесты; хуки/UI без изменений контракта.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Current analyze path (replace gateway target only)
@apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts
@apps/mobile/src/shared/api/client.ts
@apps/mobile/src/features/analyze-food/model/useAnalyzeFood.ts
@apps/mobile/src/features/analyze-food/index.ts
@packages/shared-types/src/index.ts

# Prompt + schema reference — READ ONLY, DO NOT MODIFY
@apps/backend/src/routes/analyze-food.ts

# Env (gitignored — already set locally; do not commit)
# apps/mobile/.env → VITE_AI_GATEWAY_URL, VITE_AI_GATEWAY_API_KEY

## Locked decisions (non-negotiable)
- D-01: Do not modify apps/backend/**
- D-02: Call deployed AI Gateway from mobile (sibling ai-app prod URL in .env)
- D-03: Use VITE_AI_GATEWAY_URL + VITE_AI_GATEWAY_API_KEY
- D-04: OpenAI-compatible POST /v1/chat/completions with vision data URLs + json_object
- D-05: Preserve analyzeFoodApi(File) → AnalyzeFoodResponse
- D-06: Reuse SYSTEM_PROMPT / Russian foodName / NutritionResult shape from backend on the client
- D-07: Map gateway errors to ApiError codes used by mobile (INVALID_IMAGE, RATE_LIMITED, ANALYSIS_TIMEOUT, ANALYSIS_FAILED)

## USER OVERRIDE (security)
PROJECT.md still says OpenAI key server-only. For this quick task the user explicitly chose client-side gateway key via Vite env. Document in SUMMARY; do not re-open the decision.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit-тесты analyzeFoodApi под AI Gateway</name>
  <files>apps/mobile/src/features/analyze-food/api/analyzeFoodApi.test.ts</files>
  <behavior>
    - Happy path: mock HTTP success с assistant content = валидный NutritionResult JSON → resolve { result, processingTime } где processingTime &gt;= 0 и поля result совпадают
    - Пустой content / битый JSON / невалидная схема → reject ApiError с code ANALYSIS_FAILED
    - Gateway code RATE_LIMITED → ApiError RATE_LIMITED; UPSTREAM_TIMEOUT → ANALYSIS_TIMEOUT; BAD_REQUEST → INVALID_IMAGE; UNAUTHORIZED / UPSTREAM_ERROR / unknown → ANALYSIS_FAILED (или сохранить исходный code только если это один из четырёх app codes)
    - Запрос уходит на URL содержащий /v1/chat/completions; Authorization Bearer содержит значение из env; body включает model gpt-4o-mini, response_format.json_object, system+user messages с image_url data URL
    - Сигнатура analyzeFoodApi(image: File) не меняется — тест вызывает её с File/Blob
  </behavior>
  <action>
Создать co-located Vitest-файл `analyzeFoodApi.test.ts`. Замокать HTTP-слой, которым будет пользоваться реализация (axios instance или fetch — тот же, что в Task 2). Задать `import.meta.env.VITE_AI_GATEWAY_URL` и `VITE_AI_GATEWAY_API_KEY` через vi.stubEnv или vi.stubGlobal как принято в проекте.

Не импортировать и не трогать apps/backend. Не читать реальный `.env` в тестах. Не добавлять snapshot с секретами.

Сначала прогнать тесты — они должны падать (RED), пока Task 2 не реализует gateway path.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/analyze-food/api/analyzeFoodApi.test.ts</automated>
  </verify>
  <done>Тест-файл существует; после Task 2 все кейсы из behavior зелёные; useAnalyzeFood.test.ts по-прежнему мокает analyzeFoodApi и остаётся зелёным.</done>
</task>

<task type="auto">
  <name>Task 2: Переписать analyzeFoodApi на AI Gateway</name>
  <files>apps/mobile/src/features/analyze-food/api/analyzeFoodApi.ts, apps/mobile/src/vite-env.d.ts</files>
  <action>
Per D-01: никогда не изменять apps/backend/**.

Per D-03/D-04: в `analyzeFoodApi.ts` перестать вызывать `apiClient` / локальный `/analyze-food`. Реализовать:

1. Прочитать `File` → base64 data URL `data:{mime};base64,...` (ArrayBuffer + btoa или FileReader; mime из `image.type`, fallback `image/jpeg`).
2. POST JSON на `${import.meta.env.VITE_AI_GATEWAY_URL}/v1/chat/completions` с заголовком `Authorization: Bearer ${import.meta.env.VITE_AI_GATEWAY_API_KEY}` (достаточно Bearer; X-API-Key опционален как fallback только если Bearer недостаточно — не дублировать без нужды).
3. Body: model `gpt-4o-mini`, `response_format: { type: 'json_object' }`, messages: system = SYSTEM_PROMPT скопированный по смыслу из `apps/backend/src/routes/analyze-food.ts` (D-06: русский foodName + те же поля NutritionResult), user = multimodal content с `image_url` data URL и русским текстом вроде «Проанализируй это изображение еды…».
4. Замерить `processingTime` (Date.now вокруг запроса).
5. Распарсить `choices[0].message.content` как JSON; валидировать форму NutritionResult вручную (проверка типов полей / диапазон confidence 0..1) — без новой зависимости zod (её нет в mobile package.json). При провале → reject `{ message, code: 'ANALYSIS_FAILED', status: 500 }` as ApiError.
6. Успех → return `{ result, processingTime }` (D-05).

Per D-07: при HTTP/gateway ошибках маппить body.code:
- RATE_LIMITED → RATE_LIMITED (status 429)
- UPSTREAM_TIMEOUT → ANALYSIS_TIMEOUT (status 504)
- BAD_REQUEST → INVALID_IMAGE (status 400)
- иначе (UNAUTHORIZED, UPSTREAM_ERROR, network, missing env) → ANALYSIS_FAILED с понятным русским message; если env URL/key отсутствуют — сразу ANALYSIS_FAILED без сетевого вызова.

Timeout клиента ~30_000 ms (как у apiClient). Оставить `shared/api/client.ts` и `VITE_API_URL` нетронутыми для прочих нужд — analyze-food их больше не использует.

В `vite-env.d.ts` расширить `ImportMetaEnv` полями `VITE_AI_GATEWAY_URL` и `VITE_AI_GATEWAY_API_KEY` (string).

Не коммитить apps/mobile/.env. Не логировать API key. Barrel `features/analyze-food/index.ts` менять не нужно, если публичный экспорт прежний.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/analyze-food/api/analyzeFoodApi.test.ts src/features/analyze-food/model/useAnalyzeFood.test.ts &amp;&amp; pnpm --filter @ai-food/mobile type-check</automated>
  </verify>
  <done>analyzeFoodApi ходит на gateway chat/completions; контракт и хуки сохранены; backend untouched; тесты analyzeFoodApi + useAnalyzeFood зелёные; type-check проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AI Gateway | Untrusted client sends image + gateway API key in request headers |
| Vite build → bundle | VITE_* secrets inlined into client JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260716-01 | Information Disclosure | VITE_AI_GATEWAY_API_KEY in mobile bundle | high | accept | USER OVERRIDE: key intentionally client-side for this quick task; document in SUMMARY; do not commit .env |
| T-260716-02 | Spoofing | Authorization Bearer to gateway | medium | accept | Gateway enforces key validation; spoofing requires stolen client key (accepted for demo) |
| T-260716-03 | Tampering | Vision image payload | medium | mitigate | Reject missing/invalid image early; map BAD_REQUEST → INVALID_IMAGE; validate NutritionResult before UI save |
| T-260716-04 | Denial of Service | Large File upload / repeated calls | low | accept | Existing File picker + React Query retry; no new rate-limit UI in this task |
| T-260716-05 | Information Disclosure | Error messages / console | low | mitigate | Do not log API key or full Authorization header; Russian user-facing messages only |
| T-260716-SC | Tampering | npm installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- `pnpm --filter @ai-food/mobile exec vitest run src/features/analyze-food`
- `pnpm --filter @ai-food/mobile type-check`
- `git status` / diff: zero changes under apps/backend/
- Manual smoke (optional after execute): pnpm dev → Add Food → photo → nutrition result from gateway (requires .env already set)
</verification>

<success_criteria>
1. Mobile analyze path uses VITE_AI_GATEWAY_* and POST /v1/chat/completions, not local /analyze-food.
2. Public API analyzeFoodApi(File) → AnalyzeFoodResponse unchanged for hooks/UI.
3. Client prompt mirrors backend Russian foodName + NutritionResult schema.
4. Gateway/app errors surface as ApiError with mapped codes.
5. Backend tree unmodified; .env not committed.
</success_criteria>

<output>
Create `.planning/quick/260716-05y-analyze-food-ai-gateway-ai-app-api-vite-/260716-05y-SUMMARY.md` when done
</output>
