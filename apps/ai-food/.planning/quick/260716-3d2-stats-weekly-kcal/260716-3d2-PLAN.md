---
phase: quick-260716-3d2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/features/stats/model/getWeeklyCalorieSeries.ts
  - apps/mobile/src/features/stats/model/getWeeklyCalorieSeries.test.ts
  - apps/mobile/src/features/stats/ui/WeeklyCaloriesChart.tsx
  - apps/mobile/src/features/stats/index.ts
  - apps/mobile/src/pages/stats/ui/StatsPage.tsx
  - apps/mobile/src/pages/stats/index.ts
  - apps/mobile/src/app/router.tsx
  - apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
autonomous: true
requirements:
  - QUICK-3d2

must_haves:
  truths:
    - "User can open a dedicated Statistics page from Home (header navigation)."
    - "Statistics page shows a 7-bar weekly calorie chart: sum of meal.totalCalories per calendar day for today and the previous 6 days."
    - "Only ready meals count (status omitted or ready); analyzing/error meals are excluded."
    - "Days with no meals show 0 kcal (bar height 0 or minimal empty slot) — all 7 days always rendered."
    - "No new npm chart libraries; chart is lightweight SVG or CSS bars."
  artifacts:
    - apps/mobile/src/features/stats/model/getWeeklyCalorieSeries.ts
    - apps/mobile/src/features/stats/model/getWeeklyCalorieSeries.test.ts
    - apps/mobile/src/features/stats/ui/WeeklyCaloriesChart.tsx
    - apps/mobile/src/features/stats/index.ts
    - apps/mobile/src/pages/stats/ui/StatsPage.tsx
    - apps/mobile/src/pages/stats/index.ts
  key_links:
    - "useDiaryStore.meals -> getWeeklyCalorieSeries(meals, today) -> WeeklyCaloriesChart series"
    - "DailyHeader nav control -> /stats -> StatsPage"
    - "meal.timestamp (local calendar day via isSameDay) + meal.totalCalories drive bar values"
---

<objective>
Add a Statistics module: dedicated page with a weekly calorie chart (sum of kcal per day for the last 7 days) and a Home header entry point (D-01, D-02).

Purpose: Let users see calorie intake trend across recent days without leaving the local diary data model.

Output: pure aggregator + Vitest, SVG/CSS chart feature slice, StatsPage route `/stats`, DailyHeader navigation control. No macros, period switchers, or extra widgets (D-05).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/mobile/src/app/router.tsx
@apps/mobile/src/entities/meal/model/useDiaryStore.ts
@apps/mobile/src/entities/meal/index.ts
@apps/mobile/src/pages/home/ui/HomePage.tsx
@apps/mobile/src/pages/diary/ui/DiaryPage.tsx
@apps/mobile/src/pages/diary/index.ts
@apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx
@apps/mobile/src/shared/lib/dateUtils.ts
@apps/mobile/src/shared/lib/formatters.ts
@packages/shared-types/src/index.ts

## Locked decisions (do not reopen)

**D-01 — Scope:** First version is ONLY a weekly calorie chart — sum of kcal per day for the last 7 calendar days (today + previous 6). No macros split, no 14/30 switchers, no averages widgets, no other charts.

**D-02 — Entry:** Dedicated Statistics page (`/stats`) plus a navigation entry from Home that fits existing FSD patterns (mirror DiaryPage header style; add control in DailyHeader).

**D-03 — Data source:** Derive series from `useDiaryStore` meals — `timestamp` for day bucketing, `totalCalories` for sums. Client-only; not TanStack Query / not API.

**D-04 — Chart impl:** No new heavy chart libraries (none in package.json). Prefer lightweight SVG or CSS bar chart.

**D-05 — Out of scope:** Macros, period switchers, averages, secondary charts, backend/API, new npm deps for charts.

## Architecture facts (must follow)

- FSD: pages → widgets/features → entities → shared; cross-slice imports only via `index.ts` barrels.
- Named exports; page `StatsPage`; hook/helper camelCase.
- DailyHeader already filters day meals with `(m.status ?? 'ready') === 'ready'` — reuse that rule for stats.
- Use `isSameDay` / `formatDayLabel` from `@/shared/lib` for local-calendar day logic and short Russian weekday labels (Пн…Вс).
- UI language Russian (title «Статистика», chart caption about last 7 days / kcal).
- Match DiaryPage chrome: `min-h-screen`, header with back button + title, Tailwind + existing Button.

## Claude's discretion (locked-in here)

- Rolling window ending today (not Mon–Sun WeekStrip offset).
- Feature slice `features/stats` owns aggregator + chart; page composes them.
- Nav: icon button on the left of the DailyHeader title row (`aria-label="Статистика"`), Profile button stays on the right (may remain non-navigating). Use a lucide chart icon (e.g. `ChartColumn` or `BarChart2`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Weekly calorie series aggregator + tests</name>
  <files>apps/mobile/src/features/stats/model/getWeeklyCalorieSeries.ts, apps/mobile/src/features/stats/model/getWeeklyCalorieSeries.test.ts, apps/mobile/src/features/stats/index.ts</files>
  <behavior>
    - getWeeklyCalorieSeries(meals, today) returns exactly 7 items ordered oldest → newest (today is last)
    - Each item has date: Date (local midnight of that day), label usable via formatDayLabel, calories: number (sum of totalCalories)
    - Meals with status analyzing or error are ignored; omitted status counts as ready
    - Multiple meals same day sum totalCalories
    - Meal outside the 7-day window is ignored
    - Empty meals → 7 zeros
  </behavior>
  <action>
    1. Create `features/stats/model/getWeeklyCalorieSeries.ts` (per D-01, D-03). Export interface `DailyCaloriePoint { date: Date; calories: number }` and function `getWeeklyCalorieSeries(meals: Meal[], today?: Date): DailyCaloriePoint[]`.

    2. Build the window as 7 local calendar days: for i from 6 down to 0, day = today − i days at local midnight. Sum `meal.totalCalories` where `(meal.status ?? 'ready') === 'ready'` and `isSameDay(new Date(meal.timestamp), day)`. Import `isSameDay` from `@/shared/lib` (relative within slice only for local files; shared via `@/shared/lib`). Import `Meal` type from `@ai-food/shared-types`.

    3. Co-located Vitest covering: empty diary, multi-meal day sum, status filter, out-of-window exclusion, stable length 7 with today last. Freeze `today` via a fixed Date argument (do not rely on system clock for assertions).

    4. Barrel `features/stats/index.ts` exporting the function and type (chart export lands in Task 2 — update barrel there too). Do not add chart UI in this task.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/stats/model/getWeeklyCalorieSeries.test.ts</automated>
  </verify>
  <done>Aggregator returns a 7-point oldest→newest series of ready-meal calorie sums for the rolling last-7-days window; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Stats page, SVG bar chart, route + Home nav</name>
  <files>apps/mobile/src/features/stats/ui/WeeklyCaloriesChart.tsx, apps/mobile/src/features/stats/index.ts, apps/mobile/src/pages/stats/ui/StatsPage.tsx, apps/mobile/src/pages/stats/index.ts, apps/mobile/src/app/router.tsx, apps/mobile/src/widgets/daily-header/ui/DailyHeader.tsx</files>
  <action>
    1. Create presentational `WeeklyCaloriesChart` in `features/stats/ui/` (per D-04). Props: `series: DailyCaloriePoint[]`. Render 7 bars with `formatDayLabel(date)` under each bar and optional kcal value above or below when calories &gt; 0. Scale bar heights to max calories in the series (if max is 0, show flat empty baseline — still 7 slots). Use inline SVG or Tailwind height percentages — no recharts/chart.js/etc. Accessible: wrap chart region with a short `aria-label` describing weekly calories. Match zinc/muted app styling (Home uses `bg-zinc-50`; prefer semantic tokens where DiaryPage does: `bg-background`, `text-muted-foreground`).

    2. Create `pages/stats/ui/StatsPage.tsx` named export `StatsPage` (per D-02): header like DiaryPage (back → `/`, title «Статистика»), main section with short Russian subtitle (e.g. last 7 days calories), `useDiaryStore((s) => s.meals)`, `getWeeklyCalorieSeries(meals)`, pass to `WeeklyCaloriesChart`. Import feature symbols only via `@/features/stats` barrel. Export page from `pages/stats/index.ts`.

    3. Register route in `app/router.tsx`: path `/stats` wrapped in `ProfileGuard` like other app pages. Import `{ StatsPage }` from `@/pages/stats`.

    4. Wire Home entry in `DailyHeader.tsx` (per D-02): left-side icon button with lucide chart icon, `aria-label="Статистика"`, `useNavigate` to `/stats`. Keep the centered title and right Profile button. Do not implement profile navigation here.

    5. Update `features/stats/index.ts` to export `WeeklyCaloriesChart` alongside the aggregator. No macros/period UI (D-05). No new package.json dependencies.
  </action>
  <verify>
    <automated>pnpm --filter @ai-food/mobile exec vitest run src/features/stats/model/getWeeklyCalorieSeries.test.ts && pnpm --filter @ai-food/mobile exec tsc --noEmit</automated>
  </verify>
  <done>User can open `/stats` from DailyHeader, see a 7-day kcal bar chart from diary totals, and navigate back; type-check passes; no new chart deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage / Capacitor prefs → diary meals | Persisted meal data trusted as user-local MVP input for aggregates |
| UI render of kcal numbers | Untrusted only insofar as corrupted persisted numbers could skew display |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3d2-01 | Tampering | getWeeklyCalorieSeries / StatsPage | low | accept | Local-only diary; user can already edit meals; stats are a derived view with no privilege elevation |
| T-3d2-02 | Information Disclosure | StatsPage | low | accept | Calories already visible on Home/Diary; no network export of stats |
| T-3d2-03 | Denial of Service | WeeklyCaloriesChart | low | accept | Fixed 7 bars; O(n) scan of local meals; no remote calls |
| T-3d2-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — chart is hand-rolled SVG/CSS (D-04) |
</threat_model>

<verification>
- Aggregator unit tests pass for window length, sums, status filter, and out-of-window exclusion.
- `tsc --noEmit` clean for mobile.
- Manual smoke (executor optional note): Home → Stats icon → 7 bars reflect ready meals; back returns Home.
</verification>

<success_criteria>
- Dedicated `/stats` Statistics page exists with weekly kcal-by-day chart only (D-01, D-02).
- Series computed from diary `meals` totals and timestamps (D-03).
- No chart library added; no macros/period/average extras (D-04, D-05).
- Home DailyHeader has a Statistics navigation control.
</success_criteria>

## Source audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Weekly calorie chart + stats entry | 01 | COVERED |
| REQ | QUICK-3d2 | Stats module: 7-day kcal/day chart | 01 | COVERED |
| CONTEXT | D-01 | Only weekly kcal chart, last 7 days | 01 | COVERED |
| CONTEXT | D-02 | Dedicated page + nav | 01 | COVERED |
| CONTEXT | D-03 | Diary store aggregates | 01 | COVERED |
| CONTEXT | D-04 | No heavy chart libs | 01 | COVERED |
| CONTEXT | D-05 | No macros/switchers/averages | 01 | COVERED |

<output>
Create `.planning/quick/260716-3d2-stats-weekly-kcal/260716-3d2-SUMMARY.md` when done
</output>
