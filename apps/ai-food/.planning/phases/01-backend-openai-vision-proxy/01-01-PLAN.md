---
phase: 01-backend-openai-vision-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/package.json
  - apps/backend/src/routes/analyze-food.test.ts
  - apps/backend/vitest.config.ts
  - apps/backend/.env.example
autonomous: true
requirements:
  - AI-01
  - AI-02
  - ERR-03
must_haves:
  truths:
    - "Running `pnpm --filter @ai-food/backend test --run` exits with failures (RED — tests written before implementation)"
    - "Three packages are listed in apps/backend/package.json: openai, zod, dotenv"
    - "apps/backend/.env.example exists with OPENAI_API_KEY and PORT entries"
    - "apps/backend/vitest.config.ts exists and targets CommonJS-compatible test environment"
  artifacts:
    - path: "apps/backend/package.json"
      provides: "openai, zod, dotenv in dependencies; vitest in devDependencies"
    - path: "apps/backend/vitest.config.ts"
      provides: "Vitest config for backend (node environment, no browser)"
    - path: "apps/backend/src/routes/analyze-food.test.ts"
      provides: "Failing test stubs for AI-01, AI-02, ERR-03"
    - path: "apps/backend/.env.example"
      provides: "Environment variable documentation"
  key_links:
    - from: "apps/backend/src/routes/analyze-food.test.ts"
      to: "apps/backend/src/routes/analyze-food.ts"
      via: "vi.mock('openai') stubbing the OpenAI SDK"
      pattern: "vi\\.mock\\('openai'\\)"
---

<objective>
Install the three required packages (openai, zod, dotenv), set up the Vitest test framework for the backend, write failing test stubs for all three requirements, and create .env.example.

Purpose: Wave 0 — establish the test scaffold BEFORE implementation so Wave 1 can follow TDD (write code to make red tests green). All three requirement IDs (AI-01, AI-02, ERR-03) must have failing tests before the route is rewritten.

Output: installable packages, vitest config, red tests, .env.example template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-openai-vision-proxy/01-CONTEXT.md
@.planning/phases/01-backend-openai-vision-proxy/01-RESEARCH.md
@.planning/phases/01-backend-openai-vision-proxy/01-VALIDATION.md
</context>

<interfaces>
<!-- Existing shared-types contracts the tests must assert against -->

From packages/shared-types/src/index.ts:
```typescript
export interface NutritionResult {
  foodName: string;
  calories: number;
  protein: number;
  carbs: number;
  fat: number;
  fiber: number;
  confidence: number;
}

export interface AnalyzeFoodResponse {
  result: NutritionResult;
  processingTime: number;
}

export interface ApiError {
  message: string;
  code: string;
  status: number;
}
```

<!-- Current mock route being replaced (for test setup reference) -->

From apps/backend/src/routes/analyze-food.ts (current — mock):
```typescript
import { Router } from 'express';
import multer from 'multer';
// router.post('/', upload.single('image'), async handler)
// export default router;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create environment files</name>
  <files>apps/backend/package.json, apps/backend/.env.example</files>
  <read_first>
    - apps/backend/package.json (current deps to confirm what's missing)
    - apps/backend/tsconfig.json (CommonJS output — affects package choice)
  </read_first>
  <action>
Run the install command from the repo root (NOT inside apps/backend/):

```bash
pnpm --filter @ai-food/backend add openai zod dotenv
pnpm --filter @ai-food/backend add -D vitest @vitest/coverage-v8
```

Note: dotenv v17 ships its own TypeScript types — do NOT install @types/dotenv (package does not exist on npm).

Then create apps/backend/.env.example with exactly this content (per D-09):

```
OPENAI_API_KEY=your_key_here
PORT=3001
```

The actual apps/backend/.env file is NOT created here — that requires the user's real API key. The .env.example is committed to git as documentation.

Verify pnpm-lock.yaml was updated and node_modules for backend now contains openai, zod, dotenv.
  </action>
  <verify>
    <automated>cd D:/Project/Main/ai-food && node -e "const p = require('./apps/backend/package.json'); const allDeps = {...p.dependencies, ...p.devDependencies}; ['openai','zod','dotenv','vitest'].forEach(d => { if (!allDeps[d]) throw new Error('Missing: ' + d); }); console.log('All deps present')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/package.json `dependencies` contains `"openai"` with version starting `^6`
    - apps/backend/package.json `dependencies` contains `"zod"` with version starting `^4`
    - apps/backend/package.json `dependencies` contains `"dotenv"` with version starting `^17`
    - apps/backend/package.json `devDependencies` contains `"vitest"`
    - apps/backend/.env.example exists and contains the exact line `OPENAI_API_KEY=your_key_here`
    - apps/backend/.env.example exists and contains the exact line `PORT=3001`
    - No `@types/dotenv` entry in package.json (package does not exist on npm)
  </acceptance_criteria>
  <done>Three runtime packages installed, vitest in devDependencies, .env.example committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Vitest config and write failing test stubs</name>
  <files>apps/backend/vitest.config.ts, apps/backend/src/routes/analyze-food.test.ts</files>
  <read_first>
    - apps/backend/tsconfig.json (to understand module: CommonJS and rootDir: src)
    - apps/backend/src/routes/analyze-food.ts (the current mock — tests import this file)
    - packages/shared-types/src/index.ts (NutritionResult, AnalyzeFoodResponse, ApiError shapes)
  </read_first>
  <behavior>
    Tests must be RED (failing) when the mock route is in place, GREEN when the real implementation is in place.

    - Test 1 (AI-01): POST /analyze-food with a mocked OpenAI success response returns HTTP 200 with `result.foodName` as a string and `processingTime` as a number. The OpenAI SDK is mocked via `vi.mock('openai')` — no real API calls.
    - Test 2 (AI-02): The successful response body conforms exactly to `AnalyzeFoodResponse`: has `result` object with all 7 fields (foodName, calories, protein, carbs, fat, fiber, confidence) and `processingTime` number. Calories, protein, carbs, fat, fiber must be numbers; confidence must be between 0 and 1.
    - Test 3 (ERR-03a): POST /analyze-food with no file attached returns HTTP 400 with `code: 'INVALID_IMAGE'`.
    - Test 4 (ERR-03b): POST /analyze-food where mocked OpenAI throws `RateLimitError` returns HTTP 429 with `code: 'RATE_LIMITED'`.
    - Test 5 (ERR-03c): POST /analyze-food where mocked OpenAI throws `APIConnectionTimeoutError` returns HTTP 504 with `code: 'ANALYSIS_TIMEOUT'`.
    - Test 6 (ERR-03d): POST /analyze-food where mocked OpenAI returns non-JSON content returns HTTP 500 with `code: 'ANALYSIS_FAILED'`.
  </behavior>
  <action>
Create apps/backend/vitest.config.ts:

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
    include: ['src/**/*.test.ts'],
  },
});
```

Create apps/backend/src/routes/analyze-food.test.ts. Use supertest to make HTTP requests against the express app (or import the router directly and test via supertest). Mock the OpenAI SDK with vi.mock.

Key test implementation notes:
- Import the router from `./analyze-food` (relative path within src/routes/)
- Use `supertest` for HTTP-level assertions against an express app that mounts the router at `/`
- vi.mock('openai') MUST be called at the top of the file (hoisted), before imports
- Mock `OpenAI.prototype.chat.completions.create` to control success/error responses
- For error tests: throw `new OpenAI.RateLimitError(...)`, `new OpenAI.APIConnectionTimeoutError(...)` from the mock
- The mock success response should have `choices[0].message.content` set to a valid JSON string matching NutritionResult

Add supertest as a devDependency FIRST:

```bash
pnpm --filter @ai-food/backend add -D supertest @types/supertest
```

The test file must include the script addition to package.json — add `"test": "vitest run"` to `apps/backend/package.json` scripts if not present.

Also add `"type-check": "tsc --noEmit"` to scripts if missing.

Run `pnpm --filter @ai-food/backend test --run` after writing tests and confirm they FAIL with a reason related to the mock returning wrong data (not due to import errors or syntax). The current mock route always returns 200 with MOCK_RESPONSE, so tests 3-6 will fail as expected — ERR-03 tests will get 200 instead of 4xx/5xx.
  </action>
  <verify>
    <automated>cd D:/Project/Main/ai-food && pnpm --filter @ai-food/backend test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/vitest.config.ts exists and contains `environment: 'node'`
    - apps/backend/src/routes/analyze-food.test.ts exists and is non-empty
    - File contains `vi.mock('openai')` (hoisted mock of the OpenAI SDK)
    - File contains at least 6 test cases (it/test blocks)
    - File tests for HTTP status codes: 200, 400, 429, 504, 500
    - File tests for ApiError `code` values: 'INVALID_IMAGE', 'RATE_LIMITED', 'ANALYSIS_TIMEOUT', 'ANALYSIS_FAILED'
    - `pnpm --filter @ai-food/backend test --run` runs without crashing (even if tests fail — no import/syntax errors)
    - Test run shows failures, NOT zero tests found
    - apps/backend/package.json scripts contains `"test": "vitest run"` or `"test": "vitest"`
  </acceptance_criteria>
  <done>Vitest config created, 6+ failing test stubs written, test command runs and shows failures (RED state confirmed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → git repo | .env.example is safe to commit; .env must stay in .gitignore |
| CI environment | No real OPENAI_API_KEY needed — tests mock the SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | apps/backend/.env | mitigate | `.env` already in root `.gitignore`; `.env.example` contains only placeholder value `your_key_here` — safe to commit |
| T-01-02 | Information Disclosure | vitest test output | accept | Tests mock the OpenAI SDK; no real API key used in test suite; no key in test file |
| T-01-03 | Tampering | pnpm install | accept | Installing from npm registry with exact version pins via pnpm-lock.yaml; low-value target in dev context |
</threat_model>

<verification>
After completing both tasks:

1. `node -e "require('./apps/backend/package.json').dependencies['openai']"` prints a version string starting with `^6`
2. `cat apps/backend/.env.example` shows `OPENAI_API_KEY=your_key_here` and `PORT=3001`
3. `pnpm --filter @ai-food/backend test --run` exits non-zero (tests are RED — expected at this stage)
4. Test output shows test names, not "no tests found" or import errors
5. `apps/backend/vitest.config.ts` contains `environment: 'node'`
</verification>

<success_criteria>
- Three packages (openai, zod, dotenv) installed in apps/backend
- Vitest installed and configured for Node.js environment
- 6+ test stubs written covering AI-01, AI-02, ERR-03 (all RED — expected)
- .env.example committed with placeholder API key
- No real OPENAI_API_KEY committed anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-openai-vision-proxy/01-01-SUMMARY.md`
</output>
